# Supabase (Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Server-only: cron matching route, local admin scripts (never expose to the browser)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: OpenAI for Community Snapshot narrative (server-only)
# OPENAI_API_KEY=sk-...

# NCBI E-utilities (PubMed ingest, fit:backfill-pubmed-mesh, fit:load-mesh-descriptors).
# NCBI asks for tool= and email= on every call — the e-mail is where a courtesy note goes before a block —
# and rate-limits per IP (3 req/s without a key, 10 with). Bulk scripts pin >= 350 ms between calls regardless.
# NCBI_CONTACT_EMAIL=you@institution.edu
# NCBI_API_KEY=...
# NCBI_EUTILS_INTERVAL_MS=350

# ClinicalTrials.gov API v2 (public; no key required — https://clinicaltrials.gov/data-api/about-api)
# CLINICALTRIALS_MAX_RESULTS=500
# CLINICALTRIALS_MIN_INTERVAL_MS=550

# Simpler.Grants.gov (NIH funding sync + cron pipeline; server-only)
# SIMPLER_GRANTS_API_KEY=...
# Optional: SIMPLER_GRANTS_API_BASE_URL=https://api.simpler.grants.gov

# Vercel Cron + manual cron routes (GET/POST /api/cron/*; uses SUPABASE_SERVICE_ROLE_KEY above)
# CRON_SECRET=...   # long random string; set in Vercel Production env for nightly sync

# Optional: Signal cross-app people import (client-side OTP against Signal Supabase)
# NEXT_PUBLIC_SIGNAL_SUPABASE_URL=https://your-signal-project.supabase.co
# NEXT_PUBLIC_SIGNAL_SUPABASE_ANON_KEY=your-signal-anon-key

# Saved-search email digests (POST /api/cron/funding-search-notifications with same CRON_SECRET)
# RESEND_API_KEY=re_...
# RESEND_FROM_EMAIL="Prospera <onboarding@resend.dev>"
# Public site URL for links in emails (optional; falls back to VERCEL_URL in production)
# NEXT_PUBLIC_SITE_URL=https://your-app.example.com
