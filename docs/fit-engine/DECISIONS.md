# Fit engine — open decisions

Claude Code: when a PR depends on an item marked **OPEN**, stop and ask rather than assume. Record the answer here (date, who) before proceeding. 2026-09-05: every default was taken at once so the plan can run without a person in the loop between PRs; only D4 stays open.

| # | Decision | Status | Default if unanswered | Needed by |
|---|---|---|---|---|
| D4 | Who labels the gold set (two strategists + adjudicator) and by when | **OPEN** | — (blocks Phase 2 exit) | PR 2.4 |

## Decided

| # | Decision | Decided | Note |
|---|---|---|---|
| D2 | Models: `FIT_MODEL_CLASSIFY` / `FIT_MODEL_EXTRACT` / `FIT_MODEL_JUDGE` (names, vendor already in use: OpenAI-compatible) | 2026-09-05, Vincent Chan — default taken | classify = the small model already used in `profile.ts`; extract and judge = the strongest approved model (needed by PR 1.3, 1.5, 3.1) |
| D3 | Data governance: confirm investigator evidence + full FOA text may be sent to the configured model endpoint (today: abstracts already go to OpenAI via embeddings and `profile.ts`) | 2026-09-05, Vincent Chan — default taken | Proceed as today; no new vendors (needed by PR 1.3) |
| D5 | Self-declared paradigm question wording for onboarding | 2026-09-05, Vincent Chan — default taken; built in PR 0.7 | Seven-family grid with the plan's wording (Discovery/mechanistic · Preclinical/animal · Translational human biology · Clinical (patients, trials) · Population/epidemiology · Health systems/implementation · Computational/methods), ratings Not my work / Some / Core. Stored contract for PR 1.4: `self_declared_axes.paradigm` keys are `taxonomy.json › paradigm.families` ids verbatim, values **0 / 1 / 3** (2 accepted by the schema but reserved; a family absent from the map = unrated, not 0); `materials` ⊆ `taxonomy.json › materials.kinds`; `capabilities` reserved (always `[]`); `aspirations` free text, one direction per element — PR 1.4 classifies it before the aspiration floor exception can use it; `do_not_suggest` is paradigm **family** ids (a superset of the spec's category), written only from the edit sheet. Import-derived materials (intake `Clinical Samples` / `Biobanks`) carry no provenance flag and make `self_declared_present` true with `paradigm {}`; the raw answers stay on `raw_profile_json`, so PR 1.2's `intake_field` rules and `assign_from_self_declared` can both fire on the same fact — PR 1.2/1.4 must pick one source. ORCID is validated (MOD 11-2) on every write path and mirrored to the orcid source row with provenance in `meta.note`. |
| D6 | Whether strategist confirmation is required before *investigator* profile-weight corrections persist (spec §16 rule 3) | 2026-09-05, Vincent Chan — default taken | Required for the pilot; revisit after 100 corrections (needed by PR 3.1) |
| D7 | Investigator-facing surfaces: should PIs ever see Exploratory items, or strategists only? | 2026-09-05, Vincent Chan — default taken | Strategists only during pilot; PIs see Strong + Moderate (needed by PR 3.2) |
| D8 | Retire `lib/quick-match` immediately in PR 2.3 or keep behind flag one more release | 2026-09-05, Vincent Chan — default taken | Retire in PR 2.3 (nothing else depends on it) (needed by PR 2.3) Done in PR 2.3 (2026-09-06): the quick-match code is deleted for every team; the replacement display is flag-gated (D31). |
| D9 | RCDC research-type category names to map | 2026-09-05, Vincent Chan — default taken, then confirmed against NIH's list by the PR 0.4 validator | Source: https://grants.nih.gov/json/public-def-categories.json (331 categories, June 2026). Official and mapped: `Clinical Research`, `Health Services`, `Comparative Effectiveness Research`, `Prevention`, `Behavioral and Social Science`, `Basic Behavioral and Social Science`, `Dissemination and Implementation Research` (2021), `Translational Research` (2025). **`Clinical Trials` is not a category** — renamed `Clinical Trials and Supportive Activities` in 2015 (19 corpus rows; RePORTER back-applies the rename); `reporter_rcdc_clinical_trials` now keys on that plus `Gene Therapy Clinical Trials`. **`Epidemiology and Longitudinal Studies` does not exist** (no category mentions epidemiology); `reporter_rcdc_epi` removed — the epidemiology paradigm comes from MeSH and the study-section family table. Corpus: 184 distinct values over 508 categorized rows (INVENTORY § 12); retired categories keep their old string in RePORTER (`Women's Health`, `Biodefense`, the `Stem Cell Research – …` family), so exact-string matching is safe today — re-check when the roster grows (the D12 lesson). |
| D1 | Pilot scope: **ImmunoX** | 2026-09-04, Vincent Chan | Settled by `INVENTORY.md`: all non-archived investigators are ImmunoX; other communities have zero members. Re-open when a population- or clinical-heavy roster exists. |
| D10 | MeSH descriptor source | 2026-09-04, Vincent Chan | Default taken: download the NLM MeSH descriptor XML once via script (PR 0.2) into `mesh_descriptors`; refresh yearly. |
| D11 | PubMed identity: ORCID and RePORTER linkage | 2026-09-04, Vincent Chan | ORCID- and RePORTER-linked publications are verified without affiliation matching and are additive to name-based results; RePORTER links require the investigator's last name on the author list. Name rungs (override → strict → initials) still stop at the first match. Per row: orcid > affiliation > reporter_link. RePORTER profile ids are guarded: the PI RePORTER returns must have the roster surname (equal, or containing it as a whole word — "Prakash Budde" matches "Prakash"; "Leech" does not match "Lee") and a matching first name or initial, else nothing is stored and the source row records "profile id <id> resolves to <name>". |
| D12 | MeSH tree numbers are positional, not identifiers; every hard-coded prefix in `signal-mapping.json` is a dated assertion | 2026-09-05, Vincent Chan | NLM renumbers trees between annual releases — in 2026 the Humans tree moved from `…649.801.400.112.400.400` to `…649.313.988.400.112.400.400` and Cell Physiological Phenomena left `G02.149` for its own top-level tree `G04` (G02 is Chemical Phenomena); `triangleClass` would never have seen a human paper. Rule: (1) a single descriptor is matched by **UI**, never by its tree number — Humans is `triangle_of_biomedicine.human_uis: [D006801]`, immune to renumbering; (2) a **prefix** is used only where a whole subtree is genuinely meant, and stays a dated assertion — prefix-matched today: `M01` persons, `B01` animals (minus the Humans UI), `A11` cells, `B02` / `B03` / `B04` archaea / bacteria / viruses, `G02.111.570` molecular structure, `G04` cell physiological phenomena; (3) `mesh_descriptors.year` records the edition loaded; (4) `fit:load-mesh-descriptors` runs `validateSignalMapping` after **every** reload and exits non-zero when any name, UI or prefix has nothing under it, and `mesh.test.ts` runs the same check against the fixture on every `npm test` — regenerate the fixture (`--write-fixture`) with each yearly load so CI sees the new edition. The next release will do this again; the check is what makes it a five-minute fix instead of a silent one. |
| D13 | RePORTER announcement filter for exemplars (PR 0.6) | 2026-09-05, Vincent Chan — builder finding, validator-confirmed | `/v2/projects/search` filters by announcement with `criteria.opportunity_numbers` (the name in the API's Implementation Notes and its "Opportunity Number Search" example); `criteria.foa` is also declared on `ProjectSearchCriteriaDto` in the swagger schema but appears in no documentation text and returns identical results (PAR-25-122: total 21, same rows in the same order). Matching is exact per number, several numbers OR together (PAR-25-122 + RFA-TR-22-030 → 79 = 21 + 58), case-insensitive per the builder's probe, and the reissue lineage is not expanded by the API — Prospera walks `reissue_of` itself (≤ 4 steps) and records `awarded_under` / `lineage_depth` per exemplar. Exemplars are one row per core project (the parent award, else the newest), newest fiscal year first, cap 60, subprojects excluded; refreshed monthly per notice through a daily 07:00 UTC cron with a 30-day staleness stamp (a single monthly run cannot cover ~657 notices in 300 s). Spec §6 blend thresholds (≥ 15 → 0.6/0.4, 5–14 → 0.4/0.6) count these rows. |
| D14 | `OpportunityParadigm.required_any` — any-of paradigm requirement | 2026-09-05, Vincent Chan — default taken (PR 1.1 validator finding) | A notice can be satisfied by any one of a set of paradigm categories (BESH: `early_phase_human_experimental` or `human_biospecimen`; taxonomy `opportunity_profile.clinical_trial_designation.besh_required.paradigm_required_any`; adversarial case 6a). Stage-2 support for the set is the **max** over its members; it is a separate requirement term from `required` (weighted mean), combined by PR 2.1 as another term whose support is that max. `prompts/notice-extractor.md`'s output schema gains `paradigm.required_any` (its expectation #2 already assumed it). Folding the set into `required` would score case 6a at P ≈ 0.64 against its ≥ 0.80 floor. |
| D15 | `guide_fetch_status = 'not_applicable'` | 2026-09-05, Vincent Chan — PR 0.5 builder, validator-confirmed | Forecasts (`forecasted = true`), `-000` umbrella placeholders and numbers outside `^(RFA-[A-Z]{2}-\d{2}-\d{3}\|PA[RS]?-\d{2}-\d{3})$` (`NOT-` excepted) can have no Guide page. The sync stamps them once (`guide_fetched_at` = stamp time, `guide_url` / `guide_source` NULL), never fetches them, and re-queues them the night they become fetchable (a forecast that posts is due as `was_not_applicable`). 365 rows on 2026-09-05 (344 forecasts, 21 numbers; 215 open = the 0.5a set). INVENTORY § 4 / `inventory.sql` count them apart from `not_found`. |
| D16 | `guide_source` records the Guide host | 2026-09-05, Vincent Chan — PR 0.5 builder, validator-confirmed | `grants_nih_gov` (classic path) or `simpler_attachment` (`<number>-Full-Announcement.html` / `-Revised-Full-Announcement.html`, revised preferred, on files.simpler.grants.gov, resolved by one `GET /v1/opportunities/{source_opportunity_id}` on a classic 404 or a plain-template classic page). The attachment URL lives in `guide_url` and is read first on later runs; a 404 there re-resolves (attachment UUIDs change on re-upload) and falls back to classic once. Merged `attachments` do not survive the nightly Simpler upsert and nothing relies on them; `guide_url` / `guide_source` are the load-bearing state. NULL for `not_found` / `error` / `not_applicable`. |
| D17 | Guide re-queue predicate | 2026-09-05, Vincent Chan — 0.5a validator finding, fixed in PR 0.5 | `funding_opportunities.updated_at` is trigger-maintained and stamps the Guide sync's own write, so it never means "Simpler changed this" (920 of 920 due nightly; 399 fetches, 393 404s, 283 s per run on 09-04/09-05). The sync re-queues on `source_updated_at > guide_fetched_at`; `source_updated_at` is written only by the Simpler sync from Simpler's `summary.updated_at` / detail `updated_at` (later wins, `created_at` fallback), never the wall clock. Never-fetched, `was_not_applicable`, `--force` and the 7-day refresh / retry cadences remain; `guide_html_hash` (SHA-256 of visible text) skips re-parsing unchanged pages (`--force` bypasses it). Steady state ≈ 70–75 page GETs and 2–4 Simpler GETs a night; one weekly refresh night reaches the 400 cap. Reissue numbers NIH publishes without an IC (`RFA-20-351`) are stored faithfully; PR 0.6's lineage walk simply finds nothing. |
| D18 | Evidence-role and self-declared maps live in signal-mapping.json | 2026-09-05, Vincent Chan — PR 1.2 validator finding | `NormalizedItem.role` carries taxonomy `aggregation.role` ids, never raw row values: `author_position` (first / last / corresponding → `first_last_corresponding`, middle → `middle_author`, unknown → `unknown`) and the new `investigator_role` map (PRINCIPAL_INVESTIGATOR / STUDY_CHAIR / STUDY_DIRECTOR / RESPONSIBLE_PARTY_PI → `trial_pi`, LISTED → `sub_investigator`, UNKNOWN → `unknown`); grants keep `contact_pi` / `mpi`. Raw values stay in `signals`. `self_declared_rating_max` (3) is the D5 scale (Core 1.0, Some 0.33). Mapping edits taken with the validator's evidence: `mesh_primary_human_cells` (Humans + Primary Cell Culture → `human_primary_cells .8`, L1 .8, wet_lab .6; 35 rows), `Biological Specimen Banks` removed from `human_biospecimen` (0 rows fire solely on it; biobank-data papers are not bench assays). Deferred: the allocation / intervention_model refine restructuring (recorded in the PR 1.2 section). |
| D19 | Item-classifier prompt: vocabulary glosses kept, graded-value guidance rejected; array-shaped replies normalized | 2026-09-05, coordinator (decision defaults delegated by Vincent Chan) — measured on the six spec fixtures, two runs per variant, `gpt-4o-mini` | Baseline prompt: 18–19 of 23 axes. Adding short definitions (`human_biospecimen`, `enrolled_participants` vs `patients_under_care`, `perturbation`, "use a design key the text itself names") plus graded-value guidance (≈ 0.9 / 0.4–0.6 / ≤ 0.3, omit zeros) scored 13/23 twice: the model committed to one category per axis at 0.9 and dropped the secondary one the spec wants (`cohort_biobank_datasets` instead of `genomic_datasets`, `human_blood_fluids` instead of `human_primary_cells`), so that guidance is rejected. Definitions alone (with the parser fix below): 18/23 and 18/23 over two runs, with `perturbation` now found on fixture 1 (0.5 or absent before → 1.0), `human_biospecimen` found once; the `enrolled_participants` and hybrid-design glosses had no measurable effect and stay only as documentation. The bigger finding: in 3 of 4 runs with the longer prompt the model returned an axis as an **array** (`[{id, value}]`), every entry was rejected as an unknown id, and the all-empty reply was cached as usable — the parser now normalizes array-shaped axes and marks a reply whose every axis value was rejected `usable: false` (never cached). The classify default stays `gpt-4o-mini` (D2); a `gpt-4o` probe missed the same keys. `prompts/item-classifier.md` and `llm.ts` carry the identical prompt (checked byte-for-byte). |
| D20 | Investigator profile aggregation: per-axis denominators, priors excluded, partial profiles written | 2026-09-05, coordinator (decision defaults delegated by Vincent Chan) — PR 1.4 builder, confirmed by the 1.4 validator | `share_c` is computed over the items on which the axis was decided (`decided_by[axis]` present), not over every item: an item silent on an axis neither supports nor dilutes it, so the rules-only lower bound keeps its shape and per-axis confidence (decided mass, distinct source origins) carries the uncertainty; spec §5 sums over all items — the literal form would zero design / materials / objective until the model pass finishes. Thin-evidence support excludes the priors (`aggregation.thin_evidence.prior_sources` = profiles, directory_metadata); biosketch and self-declared items count, so a self-report plus one verified item lifts the cap. Confidence counts distinct source origins (`aggregation.source_origin`; `ctgov_pi` and `ctgov_listed` are one registry) excluding priors. A build the model could not finish is still written, with `pending_items > 0` on the row; it stays due, the inspector labels it partial, and PR 2.1 reads `pending_items` when applying `confidence_caps`. `confidence.high_min_sources` stays 3 — with PubMed + RePORTER only, "high" is unreachable for 140 of 144 investigators; Strong needs only ≥ medium, so the 1.6 spot check decides whether to lower it. The one-time pass is `scripts/fit-build-profiles.ts` (concurrent, resumable); the nightly cron is incremental, deadline-bounded, with in-build concurrency 4; a partial cron run is logged `success` with `details.outcome = "partial"` (the `sync_job_logs.status` CHECK allows only started / success / error) and the cursor resumes by position in the tiered due ordering. |
| D21 | Exemplar prior and blend semantics (spec §6 "0.6 / 0.4") | 2026-09-05, coordinator (decision defaults delegated by Vincent Chan) — PR 1.5 validator finding on PAR-25-167 and RFA-DA-26-055 | The exemplar prior per axis is the plain mean of the classified item vectors (no rescaling to a top of 1) — a weighted share of exemplars carrying the category; rescaling turned 3 informative exemplars of 46 into six `required` categories at 0.60 that outranked the Clinical-Trial-Required designation. Blend: categories the text (or an overlay) requires become `w_t·text + w_e·share`, a designation requirement never below its overlay weight (after verified overrides); exemplar-only categories enter `paradigm.allowed` at `w_e·share` (max with an existing entry), never `required`; a `required_any` member is neither blended nor moved; `objective` blends over the union; excluded / prohibited categories never re-enter. `n` for the weight row counts exemplars with a non-empty paradigm vector (`exemplars_informative`); `exemplar_count` keeps the D13 row count. List axes gain a category at `share ≥ exemplar_blend[row].list_min_share` (0.5 for both exemplar bands; 0 below 5). Spec-literal alternative (blend over the union into `required` with true shares and the overlay floor) is a one-line flip; PR 2.1's gate formula decides. |
| D22 | Notice-extractor operating rules | 2026-09-05, coordinator (decision defaults delegated by Vincent Chan) — PR 1.5 builder + validator, measured on eight notices | `FIT_MODEL_EXTRACT` default `gpt-4o`, `max_tokens` 4,000, JSON mode, temperature 0. The user template carries `Section group: n`, a verbatim-quote reminder and "every non-empty field needs its own evidence entry": with the spec prompt alone 1 of 21 group-1 quotes verified (the model elides with "..."); with the reminder 25 of 26 quotes on three unseen notices verified; the evidence-entry sentence cut claims dropped for lacking an evidence entry from 18/23 → 12/32 and 10/14 → 6/13 on two notices with verification still ≥ 90 %. A quote with text on both sides of an elision marker is rejected (an end marker is stripped); a claim needs a verified quote on its field or an ancestor; `required` entries may cite Part 1 Purpose or Section I; only a title-level designation requirement survives a quoted text exclusion (activity-code and division priors yield; both conflicts set `needs_review`). The extraction cache key is the hash of the full prompt plus the taxonomy version. A build with a skipped chunk, an unusable reply or a budget-skipped exemplar is stored `complete: false` and is due again next run; the nightly runner defers a notice when fewer than 3 model calls remain; `--force` rebuilds after a prompt edit without a taxonomy bump. The D19 vocabulary definitions are not spliced into the extractor prompt (it receives the vocabulary block only) — the measured configuration stands until the 1.6 spot check says otherwise. |
| D23 | Engine reading of §7 stage 2–4: investigator weights normalized to the dominant; design "allowed set" at group level | 2026-09-06, Vincent Chan ("A: normalized, B: group") after the PR 2.1 builder found the adversarial fixture inconsistent with the literal spec | **P and U**: `support(o) = max over investigator categories i of (w_i / w_max) · compat(i, o)` — the dominant category carries its full compatibility and secondary categories a proportional share, so the §10 floor ladder (0.75 / 0.50 / 0.45 / 0.25) lines up with the compatibility matrix (sibling 0.85, adjacent 0.60 …) as §13's worked numbers assume; under the literal `w_i · compat` a sibling-category investigator at dominant weight 0.85 scored 0.72 and could never reach Strong, and adversarial case 3 fell to Poor. **D**: the "allowed share" is the investigator's design mass inside the notice's allowed set taken at design-group level (a listed design admits its group), which reproduces §13's 7b "D 0.55" and keeps 7b Moderate. Fixture bands amended to the chosen reading: case 2 `U ≤ 0.15` → `U ≤ 0.20`; case 6b `P ≤ 0.20` → `P ≤ 0.45` and `D ≤ 0.25` → `D ≤ 0.45`, and its caps become `paradigm_gate` + `design_required_unsupported` (P 0.42 clears the Poor gate on its own, so the translational bridge has nothing to lift; tier Exploratory unchanged). The allowed set excludes explicitly prohibited designs. §7 stages 2 and 4 and §13 carry one clarifying sentence each. Consequences recorded: with normalization the translational bridge (§10) lifts a clinical notice only when the dominant weight exceeds 0.96 (at 0.96 the support is exactly 0.25 and clears the gate) or when the notice excludes the dominant category (the excluded rule sets P = 0.15); a population or health-systems investigator carrying `translational 0.40` reaches P ≈ 0.31 (Exploratory, not Poor) against a preclinical notice by the matrix alone. The biospecimen bridge stays live. |
| D24 | Engine defaults where §7–§10 are silent (PR 2.1) | 2026-09-06, coordinator (decision defaults delegated by Vincent Chan) — PR 2.1 builder, confirmed or amended by the 2.1 validator | (1) **Moderate gap rule**: P, U and the required-design minimum are governed by their own Moderate floors and never count as the one allowed gap (the literal "not P, U or a required design" would make those floors dead and contradicts §13 7b). (2) **Vacuous requirement → 1** for P, U, M, O and D's requirement term; a notice whose paradigm axis is entirely empty — no `required`, `required_any`, `allowed` or `excluded` — is capped `low_notice_confidence` ("notice names no paradigm requirement") so a structureless notice cannot pass everyone; a broad notice that at least allows or excludes something is not capped by this rule (2026-09-06: 76 % of notices state no requirement and are genuinely broad). (3) **Profile confidence for caps** = the weakest of paradigm / unit / design / topic; `pending_items > 0` (D20) and `sources.complete === false` (D22) cap like low confidence. (4) **`required_any` term weight** = the set's largest weight (D14 fixed the support, not the weight). (5) **Required designs count as allowed**; the allowed set is group-level minus prohibited designs (D23). (6) **M pool** = required design groups + required / `required_any` / expected materials + named infrastructure, credited at `compose.methods.evidence_min`; exemplar-derived `expected` materials count. (7) **RCDC matches at depth 1** — an RCDC-only match can never satisfy the Strong specific-depth rule; coded overlap's denominator is floored at `min_specific_depth_for_strong` so a broad code (Neoplasms) cannot carry T on its own. (8) **Item compatibility for topic**: stage-2 P ≥ `paradigm.gates.poor_below` on the item's (normalized) paradigm vector, some required design group ≥ 0.20 and prohibited share < 0.6; items with no designs are compatible, items with an empty paradigm vector are not (conservative); the excluded rule is not applied per item. (9) **Recent-view thinness proxy**: the recent view is used unless no recent category exceeds `thin_evidence.cap`. (10) **Aspiration relaxation** counts the aspiration at `within_family.same_category` weight and emits `paradigm_gate_relaxed_aspiration`. (11) **Exploratory bridges are scoped on both sides** (`exploratory_exceptions.*.notice_families` and `investigator_families`: translational bridge = discovery / preclinical investigator → clinical notice; biospecimen bridge = clinical investigator → discovery / preclinical notice, the two §9 rows verbatim) so no bridge reopens a forbidden cell; a notice's families include its allowed-fallback terms, so a notice allowing clinical + population blocks the translational bridge. At the current matrix the investigator-side check on the translational bridge only bites a cross-cutting-dominant investigator (P = √(U·D) < 0.25), who is therefore no longer lifted by a trialist collaborator — §9's row names the basic scientist only. (12) **Track**: `far_above` is row-based (notice on the U01/P01 row while nothing on the R01 row is held), `runway_short` applies to R01-scale rungs only, an unknown deadline gives A = 1 ("unknown is not fail"), an unlisted activity code reads `track.readiness.unknown`. (13) **Cross-cutting**: a dominant cross-cutting investigator scores P = √(U·D); a cross-cutting category on either side contributes w·√(U·D). (14) Fixture `caps` expectations are read as "must include". Spec note for the author: §6's "confidence capped at low or medium caps tiers at Moderate" contradicts §10's "≥ medium" for Strong; the taxonomy follows §10. |
| D25 | NIH boilerplate is not an eligibility rule | 2026-09-06, coordinator (decision defaults delegated by Vincent Chan) — found by the PR 2.2 dry run (every pair carried `eligibility_unknown`), fixed in PR 1.5b | The extractor stored NIH's template sentences — "Any individual(s) with the skills, knowledge, and resources necessary to carry out the proposed research…" (338 of 436 notices, the only rule on 264), the multiple-PD/PI pointer, the diversity encouragement, and the R25 / T32 / K12 template's "The PD/PI should be an established investigator in the scientific area…" (21; a "should", identical across ICs) — as verbatim `eligibility.investigator_rules`, and the engine treats an unclassifiable verbatim rule as unknown. Items matching `signal-mapping.json › notice_boilerplate` (prefix or regex over the normalized text) are dropped when the profile is assembled, after the extraction-cache read, and logged in `sources.merge_log`, so a forced rebuild needs no model calls; real rules ("Multiple PDs/PIs are not allowed", the K citizenship sentence, R01-holder exclusions, K99/R00 conditions) survive. Audit of the 79 notices the "no paradigm requirement" cap bites: 9 of 12 sampled are career awards, SBIR/STTR parents, revisions, centers or coordinating centers (genuinely structureless); the three extractor misses are recorded as prompt Follow-ups, not applied. |
| D26 | BM25 statistics for the topic stage | 2026-09-06, coordinator (decision defaults delegated by Vincent Chan) — PR 2.2 builder, confirmed by the 2.2 validator | Document frequencies are computed over the open-notice corpus (§11 rule 2), `avgdl` over the investigator's items; `k1 = 1.2`, `b = 0.75` live in `compose.topic.bm25` (priors) and reach the engine through `ctx`. |
| D27 | Notice MeSH codes are mapped at load, not stored | 2026-09-06, coordinator — PR 2.2 builder + validator | `opportunity_fit_profiles.profile.topic.mesh` stays the extractor's output (empty today); at corpus load the extractor's `topic.terms` and RCDC names are mapped through `mesh_descriptors` by folded name (exact, singular/plural of the last word, possessive dropped; no fuzzy match, no entry terms), keeping only descriptors with a tree number at depth ≥ `compose.topic.min_specific_depth_for_strong`, so generic one-word terms (lung, cells, diagnosis) do not enter the topic denominator. A descriptor reload therefore changes every notice at once and the IDF is built over the same mapped codes in the same run. Follow-up: MeSH entry terms (cancer → Neoplasms, brain disorders → Brain Diseases, infectious diseases → Communicable Diseases, microbiome → Microbiota, gene therapy → Genetic Therapy, substance use → Substance-Related Disorders, human genome → Genome, Human, translational research → Translational Research, Biomedical, HIV/AIDS → HIV Infections, cardiovascular → Cardiovascular Diseases) would recover most of the unmapped top terms. |
| D28 | `fit_results` storage and the nightly sweep | 2026-09-06, coordinator — PR 2.2 builder + validator | One row per (investigator, candidate notice), every tier stored so "Why not?" can read `why_not`; Poor rows keep `components`, `caps`, `flags`, `gap`, `why_not` but only `{engine, E, P}` as provenance; pairs failing eligibility (E = 0) are not stored — under fit-v1 the Outreach "excluded" list comes from a pure eligibility pass over every investigator profile, so it is complete rather than whichever ineligible investigators happened to sit in a recall net. Retrieval = structured candidates (E passes, P ≥ `paradigm.gates.poor_below`) ∪ embedding top-N (`compose.retrieval.embedding_top_n`), near-miss set P ≥ 0.45 and T < 0.35. The nightly `fit-results` cron (09:35 UTC) orders the roster stalest-first (`investigator_fit_profiles.fit_results_at`), the 240 s budget is the stop, so the roster is covered every few nights; the notice mirror is read-only by default. A stored `next_due` in the past is replaced by the next computed cycle before the runway check. |
| D29 | Feature flag semantics | 2026-09-06, coordinator — PR 2.2 builder + validator | `teams.fit_engine` (`legacy` / `fit-v1`) is read once per request; no team, an unknown value or an unapplied column means `legacy`; the nightly Outreach refresh applies each team's own flag; the community fit cache is not team-scoped until PR 2.3 unifies the surfaces (superseded by D30: it stays unscoped, every writer uses the every-team rule), and the one-day lag between `refresh-outreach-suggestions` (09:15) and `fit-results` (09:35) is accepted until 2.3 reorders them. |
| D30 | Community fit cache engine rule | 2026-09-06, coordinator (decision defaults delegated by Vincent Chan) — PR 2.3 builder + validator | The community fit cache is an institution-level object (one row set per community, `pipeline_communities` has no team column, RLS is every signed-in user), so it is not team-scoped. Every writer — the nightly and the screen's refresh button — resolves its engine with `loadCronFitEngine`: `fit-v1` only when every non-archived team is on it, else legacy; `refreshCommunityFits` takes no `teamId`. A flipped team sees legacy community counts until every team is flipped, and the screen caption names the engine. Supersedes D29's "not team-scoped until PR 2.3 unifies the surfaces". |
| D31 | Legacy-team "Best fit in your directory" after D8 | 2026-09-06, coordinator default (decision defaults delegated by Vincent Chan; put to him twice, unanswered at merge time — reversible with one argument to `loadFundingOpportunityPeek`) | D8 retires the quick-match code for every team in PR 2.3, so a legacy team has no engine behind that surface; the replacement display is flag-gated. Legacy teams see one sentence pointing to Outreach plus the existing "Review in Outreach" button on the opportunity page card and the peek; fit-v1 teams see tiers from `fit_results`. No fit-v1 output reaches a team that is not flipped. |
| D32 | Order and shape of list surfaces | 2026-09-06, coordinator — PR 2.3 builder + validator | Every list surface ranks tier (Strong > Moderate > Exploratory) → score desc → id; Poor is hidden until PR 3.2's "Why not?"; reads are never score-limited before the sort (per-tier reads with the display limit, strong first, or every surfaced row); render paths read the six summary columns only, never `provenance` or `components`. |
| D33 | Tier vocabulary across surfaces | 2026-09-06, coordinator — PR 2.3 builder + validator | One label set (`TIER_LABEL`: Strong match / Potential match / Exploratory) everywhere; a fit-v1 Moderate is the "Potential match" pill until PR 3.2 decides on "Moderate"; fit-v1 tooltips (`FIT_TIER_HELP`, floors) wherever the surface knows the engine, the cosine `TIER_HELP` elsewhere. |
| D34 | Gold-set construction and the feedback vocabulary (PR 2.4) | 2026-09-06, coordinator (decision defaults delegated by Vincent Chan) — PR 2.4 validator | The "current" 80 are legacy-only: what the investigator page shows (the top 5 of `match_opportunities`' top 20 by cosine over every open embedded notice) plus stored Outreach snapshots, Strong and Potential half each; fit-v1 Strong / Moderate pairs not otherwise drawn form a supplementary stratum `fit_v1` beyond the 200 so labeled metrics split by source. Adversarial cells the pilot roster cannot fill (no population or health-systems investigators — a D1 re-open trigger) are covered by synthetic investigators built from the §13 fixture (cases 2 and 7a) against real notices, flagged `synthetic`, counted in the wrong-type rate and the confusion matrix, excluded from legacy numbers, precision@k and the Strong ratio. The legacy reproduction carries the page's rank; a pair beyond the shown window is `not_shown`. The versioned set is frozen: the export refuses to overwrite without `--force` and refuses once gold labels exist unless the version is new; a taxonomy bump does not redraw it. Slots: first-label order assigns A and B only; the adjudicator comes from `labelers.json` (D4); the adjudicator's row wins when present; no derived rows are written. Feedback reasons live in `taxonomy.json › feedback` with ids shared with PR 3.2 (`wrong_research_type`, `not_relevant`, `not_eligible`, `wrong_person`, `wrong_mechanism`, `thin_evidence`, `already_aware`, `do_not_contact`, `other`) and §12's wrong-research-type sub-reason presets; a gold label at Exploratory or Poor needs a reason. |
| D35 | Blind-pass masking | 2026-09-07, coordinator (decision defaults delegated by Vincent Chan) — PR 3.1 builder + validator | Topic terms are replaced by placeholders `[DISEASE]` / `[PATHWAY]` / `[POPULATION]` / `[TOPIC]` chosen by MeSH tree; the paradigm and axis vocabulary (`PROTECTED_TERMS`, incl. materials, specimens, samples, biospecimen(s), methods) is never masked so the blind pass keeps the words the taxonomy is written in; in Call A the notice number and grant ids carry their institute letters masked (`RFA-··-27-136`, `5R01··120003`) with aliases so citations map back to canonical ids; the leak check runs over the rendered evidence and notice texts only. |
| D36 | Judge inputs and evidence selection | 2026-09-07, coordinator — PR 3.1 builder + validator | `max_tokens` 1,200 / 1,500 / 600 / 2,000 for blind A / blind B / skeptic / reconciler (the spec gives none); the biosketch statement and, on Clinical-Trial-Required notices, the investigator's best trial are always among the evidence; items ranked by notice similarity with an unembedded item at the band's lower edge (0.35) and at least two slots reserved for the highest-cosine embedded items, so the blind pass sees notice-dependent evidence. |
| D37 | Scout set | 2026-09-07, coordinator — PR 3.1 builder | The scout set is the first 10 near-miss pairs by score (of ≈ 250 per investigator); the reconciler runs on a scout pair only when the blind verdict exceeds the structure or latent fit is found. |
| D38 | Adjudication cache and the table's baseline | 2026-09-07, coordinator — PR 3.1 validator | A pair is judged once per profile version: the cache key is the content hash of both stored profiles excluding `computed_at` and `sources` (so the monthly exemplar refresh does not re-key every notice) plus the taxonomy and judge versions. S0 for the reconciliation table is the stored investigator profile after auto (provisional) corrections, recomputed fresh by `scorePair`, in both the judge and the sweep's re-derivation, so the two never disagree; the pre-correction tier is kept for audit. The one-tier-per-cycle hold applies to provisional raises only and always carries a `pending_confirmation` review item. A pair with any call refused for budget or deadline, or whose model call threw, is not persisted and the run stops with the reason named (`stopped_by`: deadline / budget / error); a stopped investigator is not stamped, so the next run resumes him. The sweep reads the live `fit_corrections.status`: a rejected provisional correction is never re-applied. |
| D39 | Reconciliation-table completions where the spec is silent or inconsistent | 2026-09-07, coordinator — PR 3.1 builder + validator | R4 stands as written (Strong / Exploratory-or-Poor blind / ungrounded objection → Strong stands at low confidence with a review item), and a grounded emphasis objection in that branch lowers to Moderate as R2 does — the R4-vs-post-rule-5 wording conflict in `reconciler.md` is for the spec's author. R6 never demotes a Moderate. An eligibility objection implies the `eligibility_unknown` cap tier; paradigm / unit objections → Poor; design → Exploratory. Below Strong a lower blind verdict stands at low confidence. `high` needs two usable variants (so `--variants 1` cannot yield a high-confidence Strong); an ungrounded objection caps at medium. Post-rule 6 applies wherever an inexpressible insight exists, the tier is ≤ Exploratory and no gate fired; R7 raises a review item only for a blind Strong or Moderate. Notice references ground a skeptic objection. |
| D40 | `fit_corrections.proposed_by` values | 2026-09-07, coordinator — PR 3.1 builder | `'judge' | 'investigator' | 'strategist'`; `reconciler.md` post-rule 2's stored enum `'llm'` is renamed `'judge'` (a spec line, edited by the builder and ratified here). The paradigm-weight correction's evidence bar is "≥ 2 verified items carrying the corrected category's family at ≥ `thin_evidence.cap`". |
| D41 | Judge capacity and the first pass | 2026-09-07, coordinator — PR 3.1 validator (measured prompt sizes) | A top pair costs ≈ 6 `gpt-4o` calls and 32–38 k input tokens; at the organization's 30,000 TPM the nightly `fit-judge` cron (09:45 UTC, 240 s) starts calls only in its first 150 s (240 s budget minus the 90 s margin) and judges ≈ 2–3 pairs, so time, not the call budget, is its stop and it cannot cover the roster. The first pass is `scripts/fit-judge.ts --write` from a terminal at concurrency 1: ≈ 17–18 k calls, ≈ 110 M input tokens, ≈ $350–380 and ≈ 60–65 h at two blind variants, ≈ $240 / ≈ 40 h at one. **Vincent (2026-09-07): the full first pass is parked** — the coordinator runs a 3-investigator slice (≈ $7) for inspection after PR 3.1 lands, and the roster pass waits until the taxonomy has been recalibrated against the labeled gold set (`scripts/fit-recalibrate.ts`; D4 labels first), since profile rebuilds re-key adjudications anyway. The judge client uses `timeout 90 s` with no SDK retries and stops starting calls 90 s before the deadline, so no call can outlive the function's 300 s; it paces itself under `FIT_JUDGE_TPM` (default 25,000 tokens per sliding minute, estimated from prompt length plus `max_tokens`), waiting only while the wait fits before the deadline margin, and absorbs one 429 whose retry-after is ≤ 30 s — a second 429 stops the run as `rate_limit` (the first real slice on 2026-09-07 died on its first 429 before this rule existed), and it retries once after two seconds on a transient transport failure — a dropped connection, a request timeout, a 5xx — which cost the third investigator of that slice 16 of his 25 pairs; a second failure on the same call, a wait that would cross the deadline, or any 4xx still stops the run; Call A also masks every participating institute's acronym (`nih_ic_tokens`) and notice numbers quoted in prose. |
| D42 | Tier vocabulary settled (closes D33) | 2026-09-07, coordinator (decision defaults delegated by Vincent Chan) — PR 3.2 builder + validator | Under fit-v1 the pills read Strong match / Moderate match / Exploratory (`FIT_TIER_LABEL`); legacy teams keep "Potential match" and the cosine tooltip, so nothing changes for them. |
| D43 | Who is "the investigator" on a fit surface (D7) | 2026-09-07, coordinator — PR 3.2 builder + validator | The viewer is the investigator exactly when the auth session's email equals the directory record's email (case-insensitive) — never `profiles.email`, which a user can edit; an investigator sees Recommended only (no Exploratory, no "Why not?") and can confirm a correction as `proposed_by: 'investigator'` only on that path, and only as a member of the dismissing team (row-level security on the dismissal). Everyone else is a strategist and sees all three groups. |
| D44 | Render-path reads on `fit_results` (amends D32) | 2026-09-07, coordinator — PR 3.2 builder + validator | List surfaces read the six summary columns plus slim JSON paths (`provenance->T->top_items`, `provenance->P->best_pair`, `adjudication->>judged_at`, `adjudication->reconciliation->>tier|tier_structured|confidence`, `adjudication->evidence`), never the provenance or components blobs; the Outreach evidence view reads `components, caps` once per notice for the suggested people, chunked by 200 ids, every tier so a dismissed person keeps their bars. The strategist investigator page is at most ten bounded reads; the investigator's own page fewer. |
| D45 | Citation fallback chain | 2026-09-07, coordinator — PR 3.2 builder + validator | A shown tier's rationale cites an evidence item through: ids named in the stored rationale → the topic provenance's `top_items` → the judge's cited evidence → the profile's paradigm provenance for the best pair. On 2026-09-07 data every surfaced row resolves an item; "No rationale stored." never appears without one. |
| D46 | Person-proposed corrections | 2026-09-07, coordinator — PR 3.2 builder + validator | A "wrong type of research" dismissal that names a category proposes a `fit_corrections` row (`target investigator_profile`, `kind profile_weight`, `status proposed`, `proposed_by` investigator or strategist by D43) whose evidence is the dismissal itself (`via: "dismissal"`), not D40's ≥ 2-item bar for the judge — PR 3.3's review is the check. One proposal per path; the newest dismissal wins; a rejection blocks re-proposal only from the same dismissal. The dismissal and correction actions are engine-agnostic (§12: the signal is a label on an institution-level profile); the UI offers the new reasons under fit-v1 only; the inspector's corrections card is admin-only for every team. |
| D47 | Proposed weight and the two views | 2026-09-07, coordinator — PR 3.2 validator | The proposed value is `feedback.correction_step` (0.10) below the axis's Poor gate (`paradigm.gates.poor_below`, `unit.gates.poor_below`; design / materials / objective borrow `feedback.correction_gate_fallback_axis`'s), or one step below the current value when already under. A paradigm correction is proposed on the recent view and, whenever the career view carries the category, on the career view too — the engine falls back to the career view when the recent one is thin, and a recent-only correction was silently undone (2026-09-07). Re-applying `applied` corrections after a profile rebuild remains the PR 3.1 follow-up; the 24-month decay §12 mentions is not modeled yet. |
| D48 | Dismissal reasons and `axis_reason` | 2026-09-07, coordinator — PR 3.2 builder + validator | `outreach_suggestions.dismissed_reason` gains a CHECK (there was none): the legacy five plus `wrong_research_type` and `not_eligible`; `axis_reason` (`<axis>` or `<axis>:<category>`, taxonomy-validated in code, format-checked in SQL) only with `wrong_research_type`; the dialog offers the `feedback.reasons` marked for dismissal and the wrong-research-type presets, with `topic` excluded from the axis picker. |
| D49 | Exploratory and "Why not?" on the surfaces | 2026-09-07, coordinator — PR 3.2 builder + validator | A fit-v1 recipients tab always shows Exploratory under its own heading (legacy keeps its toggle); the investigator page's "Why not?" lists the five highest-scoring hidden Poor pairs with their one-line reasons and the exact count, strategists only, collapsed by default; a judged row shows the judged tier with a marker carrying confidence, date and the engine's prior tier. |
| D50 | Who may decide a fit correction | 2026-09-07, coordinator (decision defaults delegated by Vincent Chan) — PR 3.3 validator ruling | The review queue's writes require `team_memberships.role >= admin`; the page renders read-only for a plain member with the reason shown, exactly as `/team/data-sources` does. Approving a correction mutates an institution-wide profile that every team reads and re-scores the roster, which is the class the repo already gates at admin (data sources, community configuration, institution roles). "Strategist" stays an audience (D43), never a membership role. Live roles when this was taken: one owner, three admins, no plain members, so the gate locks nobody out. |
| D51 | D6 needs an identity that exists | 2026-09-07, coordinator — PR 3.3 validator | The "not your own profile" refusal compares the auth session's email with the directory record's, and no `investigators` row carries an email today, so it cannot fire and must not be cited as what makes a looser role gate safe. Until directory emails or a linked user id exist, a correction on an investigator profile whose subject has no identity on file and whose `proposed_by` is `investigator` is refused with that reason. |
| D52 | Approval re-queues, it does not judge | 2026-09-07, coordinator — PR 3.3 builder | Approving clears `fit_judged_at` for the affected investigators so the nightly takes them first; a page write never calls the model (a top pair costs about six calls). |
| D53 | A changed profile drops its judged marker | 2026-09-07, coordinator — PR 3.3 builder | `fit_results.adjudication` is cleared for the subject on approval, including pairs the judge had lowered, so no surface shows a verdict derived from a profile that has since changed. |
| D54 | A grouped decision is all or nothing | 2026-09-07, coordinator — PR 3.3 validator | A 3.2 dismissal proposes one row per paradigm view; the queue decides them as one item and pre-flights every row, so a stale `from_value` on either leaves the profile untouched and both rows proposed. Lowering one view alone would leave the other to re-open the gate. |
| D55 | The evidence hash and where it is enforced | 2026-09-07, coordinator — PR 3.3 validator | The hash covers the target, path, proposed value and the evidence, and excludes the confidence and the pair, so the same argument raised from a second pair is blocked by an earlier rejection. Every writer stamps it at proposal time — the judge and the one-click confirmation alike — and both check it before inserting. |
| D56 | A reversal is refused, never forced | 2026-09-07, coordinator — PR 3.3 builder | Rejecting an applied correction restores the previous value only while the stored value is still what the correction wrote, within the weight tolerance; otherwise the row stays applied and the strategist is told the current value. |
| D57 | The nightly re-score prelude | 2026-09-07, coordinator — PR 3.3 validator | It runs before the roster order within half the time budget and sweeps only strategist-decided corrections: the judge stamps `rescored_at` on its own auto-applications, and the migration backfills the ones it applied before this rule. A subject whose re-score errors is demoted rather than left at the head of the order, so one failing subject cannot hold half the nightly budget forever. |
| D58 | What the queue lists | 2026-09-07, coordinator — PR 3.3 builder | Only the AI-flagged leads and the unsupported dissents; the other review kinds are decided through their correction rows. A reviewed item leaves the queue but is counted, and a pair judged again at new profile versions returns unreviewed. |
| D59 | The queue reads the durable table | 2026-09-07, coordinator — PR 3.3 validator | The review sections read `fit_adjudications` (newest row per pair) and join `fit_results` for tier and score, not the derived `adjudication` blob, because the nightly sweep drops that blob when either profile version moves: 22 of the 36 review items the first judge slice raised were already invisible to a blob-based read the same day. |
| D60 | §12 recalibration: parameter set, objective, constraints, never auto-apply | 2026-09-07, coordinator (decision defaults delegated by Vincent Chan) — Phase 4 builder | Forty-four parameters, every one a prior already in `taxonomy.json`: the eighteen numeric tier floors (±0.15), the fifteen off-diagonal family-compatibility cells and ten off-diagonal unit cells (±0.20, symmetric so one parameter writes both cells), and the paradigm Poor gate (±0.10), all at a step of 0.05. Not fitted: the MeSH depth rule, the allowed-gap count, the within-family values, the matrix diagonals, the Exploratory gate, the design weights, the composition weights and the confidence caps. The objective counts agreeing minus disagreeing recommendations over the adjudicated labels, because maximizing precision alone is satisfied by recommending nothing, and Strong agreement counts only labeled-Strong pairs so that promoting a Moderate to Strong is never free. The spec's 0.85 Strong and 0.70 Moderate targets are penalties scaled by each tier's labeled count, so they bind as the label set grows rather than staying a constant tilt of seventeen and fourteen pair-equivalents, and an emptied tier scores as a full shortfall. The wrong-type rate is priced in full, both the forbidden family cells and a Clinical-Trial-Required notice shown to a bench investigator. The Strong-list rule is NOT in the objective: it is a month of side-by-side traffic in the spec, not a label count, and the label set over-samples what the legacy engine shows — it is reported instead, next to the grid reading that is primary. Recall is reported against its target. The nine adversarial cases are folded into every fit at their expected tiers and their pass or fail is printed on the proposal's face. The first version of this objective was measurably gameable: on constructed labels two thirds of its apparent gain came from the Strong-list penalty falling while tier precision exerted no force at all. The consequence of removing that term is deliberate and must be understood by whoever applies a proposal: nothing in the objective now defends the length of the Strong list, and on a label set that is mostly Moderate the fit will shorten or empty it. The recommended-list length is the rollout comparison's gate, judged over a month of side-by-side traffic, not something a few hundred labels should decide; the script prints the ratio against both readings, primary and secondary, and the reviewer holds that line. The constraints are hard, never merely penalised: floors ordered across tiers, the aspiration floor at or above the Exploratory paradigm floor, the gate at or below it, and every forbidden family cell strictly under the gate, so no fit can reopen a cell the adversarial set forbids. Coordinate descent, deterministic, capped and traced; at least fifty labels or it refuses to fit. It never edits the taxonomy: it writes a proposal and a textual patch for a person to review, apply, re-test and record here. |
| — | Scoring is a gated-multiplicative core × additive relevance; tiers by conjunctive floors | spec §8, §10 | Values live in `src/lib/fit/taxonomy.json` |
| — | The model never emits a score; it proposes corrections to inputs | spec §16 | — |
| — | Feature flag per team: `teams.fit_engine in ('legacy','fit-v1')` | plan | — |
| — | One engine serves all three surfaces (investigator page, Outreach recipients, opportunity "Best fit") | spec §7, §15 | — |
