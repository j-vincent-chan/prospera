# Fit engine — implementation plan for Claude Code

Companion to `docs/MATCHING_REDESIGN.md` (the spec). This document is the work order: one PR per item, in order, each with acceptance criteria and a kickoff prompt you can paste into Claude Code. Phases correspond to spec §15.

**Ground rules** (also in `/CLAUDE.md`): scoring code is pure; thresholds live in `src/lib/fit/taxonomy.json`; migrations are written, never applied by scripts; backfills are idempotent and resumable; every PR runs `npm test` and `npx tsc --noEmit`; the adversarial fixtures stay green from PR 2.1 onward. Stop and ask when a step depends on an **OPEN** item in `DECISIONS.md`.

**Human checkpoints** (do not proceed past these without a person): after PR 0.1 (inventory numbers reviewed), after PR 1.6 (30-investigator spot check), after PR 2.4 (gold set labeled, baseline recorded), before PR 2.2's flag is flipped for any team.

---

## Phase 0 — Capture (no behavior change)

**Status (2026-09-05).** Phase 0 complete: 0.1, 0.1b, 0.2 (+0.2a/b/c), 0.7, 0.3, 0.4, 0.5a, 0.6 and 0.5 merged, every migration applied before its merge, every backfill run and inspected — MeSH on 13,367 rows, 14 trials, 818 grants, 2,929 RePORTER exemplars over 657 notices, self-declared columns live (no data until people use the onboarding step), Guide sections on the posted NIH notices (see PR 0.5). Open: D4; a person's browser click-through of the 0.7 onboarding step. Seven reissued notices remain outside PR 0.6's NIH-like filter (no `posted_date` or non-Guide numbers) and are not fetched.

### PR 0.1 · Inventory
**Goal.** Establish the baseline numbers the rest of the plan depends on.
**Files.** `scripts/fit-inventory.ts` (runs the statements in `docs/fit-engine/queries/inventory.sql` with the service-role client and prints a Markdown table); `docs/fit-engine/INVENTORY.md` (the output, committed); `package.json` alias `fit:inventory`.
**Acceptance.** INVENTORY.md answers: investigators and identity coverage; median verified publications; share with any grant / trial / biosketch; open notices, NIH share, Guide coverage, clinical-trial designation counts, reissue count, activity-code mix; embedding coverage; existing dismissals by reason; the two raw-row field checks (RePORTER: abstract/phr/RCDC/study section/PIs present? CT.gov: studyType/phases/primaryPurpose/officials present?).
**Checkpoint.** A person reads INVENTORY.md and confirms the pilot community (D1) before PR 0.2.

> **Kickoff prompt.** Read `/CLAUDE.md`, then `docs/fit-engine/IMPLEMENTATION_PLAN.md` § PR 0.1 and `docs/fit-engine/queries/inventory.sql`. Write `scripts/fit-inventory.ts` following the pattern in `scripts/backfill-nih-guide.ts` (dotenv, service-role client). It must be read-only. Run it against `.env.local` only if I confirm; otherwise produce the script and a dry `--print-sql` mode. Output a Markdown table to `docs/fit-engine/INVENTORY.md`. Do not modify any other file.

### PR 0.1b · PubMed identity recovery
**Goal.** Every pilot investigator has a PubMed identity before PR 0.2 enriches publications. Diagnosis (2026-09-04): 7 of 144 had zero publication rows; the strict `Last First M[Author] AND UCSF` term returned 0 for six of them because PubMed indexes initials only (Ansel KM, Weiss A), because the person publishes under another given name, or because `full_name` parsing produced a phantom middle initial for two-token first names (`Cho Nam Woo W`). The initials variant + UCSF returned 65 (Ansel), 74 (Weiss), 25 (Barcellos-Hoff), 8 (Cho).
**Schema.** Migration `20260911100000_pubmed_identity_ladder.sql`: `identity_method` CHECKs gain `initials` (sources) and `reporter_link` (sources + publications).
**Code.**
- `src/lib/community/pubmed-query.ts`: fix the phantom middle initial; add `pubmedInitialsAuthorVariant`, `buildInitialsPubmedTerm`, `buildOrcidPubmedTerm`, `withUcsfAffiliation`.
- `src/lib/community/pubmed-ingest.ts`: `runPubmedIdentityLadder(input, deps)`. Name rungs, tried in order until one yields ≥ 1 verified item: (a) `pubmed_query_override` → `manual`; (b) strict full name + UCSF → `affiliation`; (c) initials + UCSF → `initials`. Additive sources that always run and union with the name result (D11): (d) `<orcid>[auid]` → rows `orcid` (verified, no affiliation clause); (e) RePORTER v2 `/publications/search` by the investigator's core project numbers → rows `reporter_link` (verified; a PMID is kept only when the investigator's last name is among its authors, because P01/T32 linkages carry trainee papers). Name rungs keep the per-author UCSF check and the verified/unverified semantics. `identity_method` is recorded per publication row (orcid > affiliation > reporter_link); the source row records the name rung that matched (else the first additive source) plus `meta.contributing` and `meta.attempts`.
- `src/lib/community/reporter-ingest.ts` guard: when a profile-id fetch returns projects, the returned principal investigator's name must match the investigator — surnames equal, or one containing the other as a whole word (a compound RePORTER surname such as "Prakash Budde" matches "Prakash"; "Leech" does not match "Lee", "Roy-Chowdhury" does not match "Cho"), plus first name exact or initial; on a mismatch nothing is upserted and the reporter source row is set to error with "profile id <id> resolves to <name>". Found because three pilot ids pointed at other people (Cho → Namita Roy-Chowdhury, He → Peng-Sheng Chen, Lee → James Leech) and a fourth was a compound-surname false alarm (Prakash → Arun Prakash Budde, kept); all three intake sheets carry the same ids.
- `scripts/fit-fix-profile-ids.ts` (`fit:fix-profile-ids`, dry-run by default, `--apply` to write): `--set` sets the confirmed `nih_profile_id` or NULL, marks the existing grant rows `rejected` with a `provenance_note` naming the wrong id and the PI it resolves to (migration `20260911110000_nih_grants_provenance_note.sql`), and resets the reporter source row; `--set-orcid` records an ORCID iD from the intake form (investigators.orcid + the orcid source row, provenance in `meta.note`); `--swap-names` exchanges first and last name when an intake sheet stored the surname first (Rajalingam), provenance in the pubmed source row's `meta.name_swap`. Stale grant embeddings are dropped by the normal `syncInvestigatorEmbeddings` pass.
- `scripts/fit-seed-pubmed-overrides.ts` (`fit:seed-pubmed-overrides`): reads the intake CSV's PubMed search URL, extracts `term=`, proposes `pubmed_query_override` for people whose PubMed state is `unavailable` or whose strict count is < 30% of the initials count; `--apply` writes. `--report` prints strict vs initials vs unaffiliated counts per investigator, flags ratios < 0.3, and writes the summary into `INVENTORY.md` § 10.
**Acceptance.** After `scripts/retry-pubmed-investigators.ts`, Weiss, Ansel and Barcellos-Hoff each have verified counts ≥ 0.8 × their initials + UCSF baseline from the diagnosis table (74, 65, 25); investigators with zero UCSF-affiliated hits under any term are listed as "manual override needed" in the coverage report; a test per ladder rung with mocked esearch; tsc clean; tests green. Coverage report reviewed before PR 0.2.

### PR 0.2 · PubMed: MeSH, publication types, abstracts, author position
**Goal.** Stop discarding the highest-precision paradigm signal.
**Scale (counted 2026-09-05, after the PR 0.1b identity ladder).** 15,375 verified publication rows over 13,567 distinct PMIDs (1,808 rows are the same paper on several ImmunoX rosters); the earlier ~9,000 estimate is superseded. The backfill fetches by distinct PMID at 200 per efetch call, so a full pass is 68 calls at the 350 ms politeness interval, and every row sharing a PMID gets the same MeSH. It covers every verified row regardless of `identity_method` — `affiliation`, `orcid`, `profiles`, `reporter_link`, and the override / initials rungs, which write their rows as `affiliation` — so the selection carries no identity_method clause at all.
**Schema.** Migration `supabase/migrations/20260912100000_fit_pubmed_capture.sql`:
- `investigator_publications` + `mesh JSONB DEFAULT '[]'` (array of `{ui, name, major: boolean, qualifiers: string[]}`), `publication_types TEXT[] DEFAULT '{}'`, `abstract TEXT`, `author_position TEXT CHECK (author_position IN ('first','last','corresponding','middle','unknown'))`, `author_position_method TEXT CHECK (IN ('orcid','name','absent'))` (how the author entry was found — the `identity_method` principle, so a scorer can discount name-derived positions), `mesh_fetch_outcome TEXT NOT NULL DEFAULT 'pending' CHECK (IN ('pending','indexed','no_mesh','not_returned','not_returned_terminal'))`, `mesh_fetched_at TIMESTAMPTZ`; indexes on `(mesh_fetch_outcome, mesh_fetched_at)` and `pmid`.
- New table `mesh_descriptors (ui TEXT PRIMARY KEY, name TEXT NOT NULL, tree_numbers TEXT[] NOT NULL, is_check_tag BOOLEAN NOT NULL DEFAULT false, year INTEGER NOT NULL)` with an index on `name`.
**Code.**
- `src/lib/community/pubmed-record.ts` (pure, unit-tested): parse `MeshHeadingList` (descriptor UI, name, `MajorTopicYN` on the descriptor or any qualifier, qualifier names), `PublicationTypeList`, `Abstract/AbstractText` (labeled sections joined as `LABEL: text`; `OtherAbstract` and the copyright line ignored), and the matched author's position — first / last / middle; `corresponding` when that author's affiliation carries the e-mail (PubMed's "Electronic address:" marker); co-first via `EqualContrib` counts as first; `unknown` when the investigator cannot be located on the list. The entry is located by the investigator's ORCID on the author entry when there is one (`author_position_method = 'orcid'`), else by name — strict name + UCSF affiliation first, then name-only for ORCID- and RePORTER-verified records (`'name'`; a common surname on a long author list is the Cho / Lee failure mode, so the method is stored) — else `'absent'`.
- `src/lib/community/pubmed-ingest.ts`: efetch is POSTed in batches (≤ 200); the identity ladder keeps the XML it fetched and the refresh fetches only what is still missing (ORCID hits), then stores the capture fields on upsert. Callers are unchanged. In-process records with no MeSH yet: store the abstract and publication types anyway, leave `mesh` empty, and set `mesh_fetched_at` so the backfill retries them after 30 days.
- `scripts/fit-backfill-pubmed-mesh.ts`: batch efetch (200 PMIDs per POST call, honor `NCBI_CONTACT_EMAIL`, ≥ 350 ms between calls) over publications where `identity_status = 'verified'` and `mesh_fetch_outcome = 'pending' OR (mesh_fetch_outcome IN ('no_mesh','not_returned') AND mesh_fetched_at < now() - interval '30 days')`; deduplicates PMIDs across investigators and writes to every row sharing one; records `mesh_fetched_at` and `mesh_fetch_outcome` for **every PMID it touches** — `indexed`, `no_mesh` (returned without MeSH, in-process), `not_returned` — so a rerun is resumable and never re-fetches the corpus. A PMID efetch does not return — confirmed by the PR 0.2a targeted retry — is `not_returned` once and **`not_returned_terminal` on the second miss** — it is a bad linkage or a withdrawn record, not an in-process one, and is never re-requested. `--report` writes rows by outcome and the terminal PMIDs (a PMID we hold that PubMed does not have deserves a person's eyes) to `INVENTORY.md` § 11. `--dry-run`, `--limit`, `--investigator <id>`, `--pmid <list>` (`--force` re-fetches listed PMIDs even if stamped — after a parser fix). Prints the pending-row count and the expected number of efetch calls before fetching.
- `scripts/fit-load-mesh-descriptors.ts`: download the current NLM descriptor file (D10; `desc<year>.gz`, ~17 MB, streamed), parse `DescriptorUI`, `DescriptorName`, `TreeNumberList`, and load `mesh_descriptors`; mark check tags from an explicit list (NLM's `DescriptorClass` flags only Male and Female, so it cannot be used). Then **validate `src/lib/fit/signal-mapping.json` against the loaded table** — every `mesh*`, `check_tag*` and `pubtype*` name and every `mesh_tree_under*` / `triangle_of_biomedicine` prefix — and exit non-zero listing each unmatched value with the rule ids that would go dark. `--validate-only` re-runs the check against the table without downloading; `--write-fixture` writes the descriptor subset the unit tests use.
- `src/lib/fit/classify/mesh.ts`: `buildMeshIndex(rows)`, `resolveDescriptor(nameOrUi)`, `treeNumbers(ui)`, `isUnder(ui, prefix)`, `triangleClass(uis)` per `signal-mapping.json › triangle_of_biomedicine` — Humans matched by **UI** (`human_uis`), subtrees by prefix (D12) — and `validateSignalMapping(index)` over names, UIs and prefixes. Unit tests with a fixture subset generated from the real file, including one that asserts every name in `signal-mapping.json` is present in the fixture — so a mapping edit without a regenerated fixture fails CI.
**Acceptance.** (1) Backfill dry-run on 20 real PMIDs prints MeSH names and pub types that match PubMed's web view for 3 spot-checked records; (2) `resolveDescriptor` fails loudly on an unknown name; (3) after the descriptor table loads, every MeSH name, UI and tree prefix used as a key in `signal-mapping.json` resolves — the loader fails with the list of unmatched values otherwise (23 of 70 rules have MeSH names as their only `when` signal and would go dark with no signal that they had), and the check runs on **every** descriptor reload, not only the first, and in CI against the fixture (D12); (4) `mesh_fetched_at` and `mesh_fetch_outcome` are set for every PMID the backfill touched, including no-MeSH and not-returned ones, and a second run fetches nothing; (5) a PMID not returned twice is `not_returned_terminal`, never re-requested, and listed in `INVENTORY.md` § 11 by `--report`; (6) `author_position_method` is stored alongside every `author_position`; (7) tests green, tsc clean.
**Note for PR 1.2.** `Zebrafish`, `Primates` and `Macaca` in `tag_animals_only` are ordinary `B01` descriptors, not MeSH check tags; the rule classifier must match `check_tag*` clauses by descriptor name in `MeshHeadingList`, never by `is_check_tag`.

> **Kickoff prompt.** Read `/CLAUDE.md`, spec §5 (Sources table), Appendix A row for `pubmed-ingest.ts`, and this plan § PR 0.2. Implement the migration, the ingest changes, the two scripts, and `src/lib/fit/classify/mesh.ts` with tests. Keep existing ingest behavior unchanged for callers. Do not run the backfill; provide a `--dry-run --limit 20` mode that prints what it would store and stop after showing me its output.

### PR 0.2a · Confirm a miss before stamping `not_returned`
**Why.** A partial efetch response under load — 200 asked, 100 returned, HTTP 200 — would stamp 100 real publications `not_returned` in one call, and a second hiccup 30 days later would make them `not_returned_terminal`. The first run's stamps have to be trustworthy or run two's terminal marks are not. Found 2026-09-05 from a 2-requested / 1-returned curl during the PR 0.2 dry run.
**Rule.** PMIDs absent from a batch response are re-requested once, in one call, together with a **canary** — a PMID this run already received. The retry returning nothing (canary included) is the service failing, not the data: nothing is stamped, the run exits 2 and prints the batch index and the resume point (a rerun of the same command; earlier batches are stamped and skipped). The retry returning anything proves the service healthy, so what is still missing is a real absence and is stamped — unless more than 25 are still missing in one batch, which aborts the same way (that many dead PMIDs in one 200-batch is not a plausible corpus). `decideBatchMisses` in `pubmed-mesh-backfill.ts` is pure and unit-tested; the script wires it before any write.
**Acceptance.** Tests for: no misses; isolated miss with canary-only retry (stamped); hiccup partially recovered (only the rest stamped); retry returns nothing (abort, nothing stamped); 26 still missing (abort), 25 (stamped). Dry run prints per-batch first-response / re-requested / recovered / confirmed-absent counts. Lands before the first real backfill run.

### PR 0.7 · Self-declared axes *(runs here, directly after PR 0.2 — moved 2026-09-04)*
**Why now.** INVENTORY.md shows biosketches are `not_requested` for every investigator and only 5 of 144 have any trial, so self-declared paradigm and materials are the only near-term signal for the clinical, population and implementation families. Keeps its number so D5 and later references stay valid.
**Schema.** `investigators` + `self_declared_axes JSONB` (`{paradigm: {family: 0–3 rating}, materials: string[], capabilities: string[], updated_at}`), `aspirations TEXT[]`, `do_not_suggest TEXT[]`, `title_series TEXT`, `degrees TEXT[]`.
**Code.** Onboarding (`components/onboarding/onboarding-client.tsx`) gains one step, "How do you do research?": a 7-row grid (Discovery/mechanistic · Preclinical/animal · Translational human biology · Clinical (patients, trials) · Population/epidemiology · Health systems/implementation · Computational/methods) with ratings *Not my work / Some / Core*, a materials checklist, and free text "Directions I'm moving toward". Edit-profile sheet (`investigator-form-sheet.tsx`) exposes the same fields plus `do_not_suggest`. Import wizard maps intake CSV columns: `Rank Series → title_series`; `Clinical Samples`/`Biobanks` affirmative text → materials (per signal-mapping `self_declared_*` rules). Server action with Zod schema.
**ORCID (added 2026-09-05).** The onboarding step and the edit-profile sheet also capture the ORCID iD (`investigators.orcid`, format `0000-0000-0000-000X`, ISNI checksum validated; the orcid source row is updated the way `fit-fix-profile-ids.ts --set-orcid` does it), and the import wizard maps the NOFO sheet's `orcid` column. Rationale: the affiliation clause drops pre-UCSF papers by design, and ORCID is the durable identity anchor — in PR 0.1b it added 12 verified rows for Barcellos-Hoff and 5 for Lee.
**Acceptance.** Round-trips through the UI, ORCID included; import wizard maps the 115-row pilot CSV, `orcid` column included, without errors; wording approved (D5).
**Built (2026-09-05, branch `fit/0.7-self-declared-axes`).** Migration `20260913100000_fit_self_declared_axes.sql` (written, not applied; `self_declared_axes` NULL until answered, the arrays `NOT NULL DEFAULT '{}'`). Pure modules with tests: `src/lib/fit/self-declared.ts` (family rows keyed by `taxonomy.json › paradigm.families`, materials checklist keyed by `materials.kinds` — a test fails when either drifts; Zod schemas; `buildSelfDeclaredColumns` restamps `updated_at` only when ratings or materials change; `isAffirmative` / `mineMaterials` / `intakeMaterials` driven by the `intake_field` rules in `signal-mapping.json`), `src/lib/investigators/orcid.ts` (`parseOrcid` with format / checksum reasons; `normalizeOrcid` moved here from the ORCID client, which re-exports it), `src/lib/investigators/import-mapping.ts` (`mapImportRow`, `autoMapHeader`, the row schema). `record-orcid.ts › syncOrcidSource` writes the orcid source row the way `--set-orcid` does (state available, identity self, `external_url`, provenance in `meta.note`: "entered in onboarding" / "entered in edit profile" / "entered on the profile page" / "imported from <file>"); every surface that writes `investigators.orcid` calls it. Onboarding: the step appears only for a viewer whose email matches a directory record (email is the only user ↔ investigator link) and saves through `saveSelfDeclaredAction` (`requireUser` + email match, else team membership); it never writes `do_not_suggest` and a blank ORCID keeps the one on file. Edit sheet: grid, materials, directions, do-not-suggest, title series, degrees, validated ORCID. Import: `Rank Series` → `title_series`, `Rank Title` → `rank`; intake materials are unioned into `self_declared_axes.materials` without touching ratings; an invalid ORCID is a warning (row imports, iD not stored; `ImportResult.warnings`). Rating scale: Not my work = 0, Some = 1, Core = 3 (2 unused, left for a later "Substantial"). Pilot sheets (read-only, `PROSPERA_PILOT_CSV_DIR` or `../`): Form Responses 115 rows → 0 errors, 0 warnings, `title_series` on 115, materials on 93 (tissue 76, blood/fluids 56, biobank 38, primary cells 3); NOFO 128 rows → 0 errors, 0 warnings, 1 valid ORCID. Browser round-trip not verified by the builder (auth gates the preview); action + schema + mapping are unit-tested.

### PR 0.3 · ClinicalTrials.gov: design fields and investigator role
**Schema.** `investigator_clinical_trials` + `study_type TEXT`, `phases TEXT[]`, `primary_purpose TEXT`, `allocation TEXT`, `intervention_model TEXT`, `observational_model TEXT`, `time_perspective TEXT`, `enrollment INTEGER`, `intervention_types TEXT[]`, `investigator_role TEXT` (PRINCIPAL_INVESTIGATOR | STUDY_CHAIR | STUDY_DIRECTOR | RESPONSIBLE_PARTY_PI | LISTED | UNKNOWN), `design_parsed_at TIMESTAMPTZ`.
**Code.** `src/lib/community/clinicaltrials-design.ts`: `parseDesign(study, investigatorName)` reading `protocolSection.designModule.*`, `enrollmentInfo.count`, `armsInterventionsModule.interventions[].type`, and role from `contactsLocationsModule.overallOfficials[]` (name-match using the existing `studyMatchesInvestigatorName` logic) and `sponsorCollaboratorsModule.responsibleParty` (type `PRINCIPAL_INVESTIGATOR` / `SPONSOR_INVESTIGATOR` with `investigatorFullName`). Treat `phases = ["NA"]` as a real value (non-drug interventional, e.g. behavioral or device), not as missing; in that case derive the design from `designInfo.allocation` and `designInfo.interventionModel` rather than from phase. Call it from `refreshInvestigatorClinicalTrials` on upsert. `scripts/fit-backfill-ctgov-design.ts`: parse from stored `raw_json` for all rows (no network), idempotent.
**Rules (implemented 2026-09-05).** Migration `supabase/migrations/20260913110000_fit_ctgov_design.sql`; `investigator_role` carries the CHECK constraint, `phases` and `intervention_types` are `NOT NULL DEFAULT '{}'`, everything else nullable — NULL `study_type` / `investigator_role` with `design_parsed_at IS NULL` means *not parsed yet*; a parsed record with nothing to read stores NULLs, `{}` and `UNKNOWN` with the stamp set. Enums are stored as CT.gov spells them (case and separators folded, `N/A` → `NA`; classic-API labels are not remapped — every row came from v2) because the ctgov rules in `signal-mapping.json` key on those strings. **Role precedence** when a record names the person in several places is the enum order: `PRINCIPAL_INVESTIGATOR` > `STUDY_CHAIR` > `STUDY_DIRECTOR` (from `overallOfficials[].role` on the matching entry) > `RESPONSIBLE_PARTY_PI` (`responsibleParty.type` PRINCIPAL_INVESTIGATOR or SPONSOR_INVESTIGATOR with a matching `investigatorFullName`); the classifier treats all four alike (`investigator_role_any`), so the order only affects the stored label. `LISTED` = the record names officials or a responsible-party investigator and this person is not among them in a recognised role (a matched official with a missing role is also `LISTED`); `UNKNOWN` = nothing to match against (no `protocolSection`, no officials and no responsible-party investigator, or no name to look for). The name test is the ingest's own (`personNameMatches`, extracted from `studyMatchesInvestigatorName`: equal after folding, or one contains the other), so a middle initial on one side only — "Susan M. Chang, MD" vs roster "Susan Chang" — misses on the official and is recovered through `responsibleParty` when the person is the responsible party. `phases = ["NA"]` is stored literally; `phasesAreInformative(phases)` is false for `[]` and `["NA"]` so PR 1.2 reads `allocation` / `intervention_model` for those instead of the phase (`signal-mapping.json` has no clause for either yet — see the follow-up below). The ingest stores the fields on every upsert via `designCaptureFields`; **apply the migration before deploying the ingest change** — an upsert against the old schema fails on the unknown columns and `refreshInvestigatorClinicalTrials` counts it as not inserted. `scripts/fit-backfill-ctgov-design.ts` (`npm run fit:backfill-ctgov-design`): parses every row's `raw_json`, `--dry-run` prints each row as it would be stored plus the coverage line, `--pending` restricts to `design_parsed_at IS NULL`, `--limit`, `--investigator`, `--nct`; upserts on `(investigator_id, nct_id)` in chunks of 200.
**Acceptance.** Backfill over existing rows fills `study_type` for ≥ 95% of rows that have a `protocolSection`; unit tests on 4 fixture study records (interventional phase 2 with PI role; observational cohort; interventional without role; interventional with `phases = ["NA"]` and a randomized parallel design), plus role precedence, the middle-initial recovery, enum folding and a record with no `protocolSection`.
**Result (dry run 2026-09-05, all 14 rows).** 14 of 14 have a `protocolSection`, `study_type` filled on 14 of 14 (100%); 9 INTERVENTIONAL (phases: 5 × PHASE4, 1 × PHASE2, 3 × NA — all three NA rows are randomized or non-randomized designs with allocation and intervention model present), 5 OBSERVATIONAL (COHORT 2, CASE_CONTROL 1, CASE_ONLY 1, OTHER 1); roles 13 PRINCIPAL_INVESTIGATOR, 1 STUDY_CHAIR, no LISTED / UNKNOWN (the ingest filter only keeps studies whose officials match the name, so every stored row names the person). Two records are thin at the source, not in the parser: NCT00187499 (2005) carries no `enrollmentInfo`, NCT00812994 no `designInfo.allocation` / `interventionModel`.
**Follow-up for PR 1.2.** `signal-mapping.json` has no clause for `allocation` / `intervention_model`; for `phases = ["NA"]` (3 of 9 interventional rows here) the `ctgov_interventional_pi` refine never fires and the design stays at the rule's default `rct .7`. Proposed refine keys: `allocation RANDOMIZED` → `rct .85`; `intervention_model SINGLE_GROUP` → `single_arm_interventional .8`, `rct .2`; `CROSSOVER` / `FACTORIAL` → `rct .8`. Record in DECISIONS.md when PR 1.2 picks it up.

> **Kickoff prompt.** Read `/CLAUDE.md`, spec Appendix A row for `clinicaltrials-ingest.ts`, Appendix B ctgov rules, and this plan § PR 0.3. Implement the migration, `clinicaltrials-design.ts` with tests, wire it into the ingest, and write the raw_json backfill script. Run the backfill only on request.

### PR 0.4 · RePORTER: materialize structured fields
**Scale (counted 2026-09-05, read-only dry run over every row).** 818 rows in `investigator_nih_grants` (804 verified, 14 rejected by `fit:fix-profile-ids`), every one the full projects API v2 record (45 keys, all present). 508 (62.1%) carry `spending_categories_desc` — RCDC began in FY2008; the uncategorized rows are the 225 pre-FY2008 awards, 51 FY2025–26 awards not yet categorized, 7 VA awards (RCDC is NIH-only) and 27 NIH FY2008–2024 awards RePORTER carries no categories for. 801 (97.9%) name a study section, 803 (98.2%) have an abstract, 531 (64.9%) a PHR. The investigator's profile id resolves to a PI entry on every verified row (718 contact PI, 86 not — MPI awards); the 14 rejected rows have no id left to match.
**Schema.** Migration `supabase/migrations/20260913120000_fit_reporter_fields.sql`: `investigator_nih_grants` + `activity_code TEXT`, `rcdc_categories TEXT[]`, `study_section TEXT`, `study_section_code TEXT`, `is_contact_pi BOOLEAN`, `abstract TEXT`, `phr_text TEXT`, `fields_parsed_at TIMESTAMPTZ`. Nulls carry meaning: `rcdc_categories` is NULL (never `{}`) when RePORTER has no categories for the award; `is_contact_pi` is NULL when the profile id is unknown or not on the PI list and false when the investigator is an MPI who is not the contact.
**Code.** `src/lib/community/reporter-fields.ts` (pure; unit tests on two fixture rows built from real raw_json shapes): `parseReporterRow(row, investigatorProfileId)` → activity code from `project_num`; `spending_categories_desc` split on `;` (trimmed, deduplicated) into `rcdc_categories`; `full_study_section.name` with the trailing bracketed code removed ("Immunobiology Study Section[IMB]" → "Immunobiology Study Section", bare-code names on older awards kept as they are) and `srg_code` as `study_section_code`; `principal_investigators[].is_contact_pi` for the entry whose `profile_id` is the investigator's; `abstract_text` and `phr_text` with the hard line wraps collapsed and blank-line paragraphs kept. The existing `grantCode` moved here from `suggest.ts` verbatim and is re-exported there — but it reads only letter(s) + two digits and misses the letter-digit-letter codes (DP2 ×12, UG3 ×4, OT2 ×3, RM1 ×3, and one each of UM1, UG1, UH2, RC1, RC2, RF1, UC2, UC4, IK2, IK6, IS1, DP1, DP3, DP5: 36 rows, 4.4%), three of which (UG3, UM1, UG1) `reporter_trial_mechanisms` in `signal-mapping.json` keys on; so `parseActivityCode` parses the NIH project-number format (a letter and two alphanumerics standing before the IC code) and RePORTER's own `activity_code` is the fallback. `suggest.ts` keeps calling `grantCode` — switching it changes `heldCodes` and is a Phase 2 scoring change, not a capture one. `reporter-ingest.ts` spreads the parsed fields into the upsert; callers unchanged. `src/lib/community/reporter-fields-backfill.ts` (pure, tested): the RCDC names the mapping keys on (read from `signal-mapping.json`, so a test pins them to D9's seven), the corpus tally, coverage and the § 12 markdown. `scripts/fit-backfill-reporter-fields.ts` (`npm run fit:backfill-reporter-fields`): from raw_json, no network, every identity_status, updates by primary key where `fields_parsed_at IS NULL` (resumable; `--all` re-parses after a parser change), `--dry-run` (prints 20 rows, then a read-only coverage scan over every row and the RCDC tally as INVENTORY § 12 markdown), `--limit`, `--investigator`.
**D9 (logged to `docs/fit-engine/INVENTORY.md` § 12).** 184 distinct RCDC values over the 508 categorized rows. Of the seven mapped names five occur — Clinical Research 210, Prevention 83, Behavioral and Social Science 6, Basic Behavioral and Social Science 3, Health Services 1 — and Clinical Trials and Comparative Effectiveness Research do not. **"Clinical Trials" is not a value in this corpus; "Clinical Trials and Supportive Activities" (19 rows) is**, so `reporter_rcdc_clinical_trials` never fires as written — for D9 to record and the mapping to fix. None of the three `_verify_name` names (Epidemiology and Longitudinal Studies, Dissemination and Implementation Research, Translational Research) appears on any row. The other 179 values are disease / topic categories.
**Acceptance (dry run 2026-09-05).** `activity_code` parsed for 818 of 818 (100%), agreeing with RePORTER's own `activity_code` on every row; `rcdc_categories` for 508 of 818 (62.1%) — every row whose raw_json has the field; study section 801; `is_contact_pi` non-null on 804; tests on 2 fixture rows; tests green, tsc clean. The real backfill runs by hand after the migration is applied.

**Follow-up for PR 1.2 (validator, 2026-09-05).** 355 of the 801 panels (44%) are Special Emphasis Panels: `study_section` is the bare string `Special Emphasis Panel` and `study_section_code` the IC prefix (`ZRG1`, `ZAI1`, `ZCA1`), which carries no topic. The topical designator lives only in `raw_json.full_study_section.sra_designator_code` (IDM 15, IMM 13, MOSS 10, CVRS 6, … plus administrative CSR / GRB / SRB). The study-section family table must read `sra_designator_code` for SEP rows, or a follow-up adds `study_section_designator TEXT`. Also: `rcdc_categories` is NULL when RePORTER has no categories (never `{}`); PR 1.2's `rcdc_any` must treat NULL and `[]` alike.

### PR 0.5a · Guide fetch diagnostics (read-only)
**Goal.** Explain the Guide coverage gap before PR 0.5 builds on Guide text. INVENTORY.md (2026-09-04): of 1,304 open notices, `guide_fetch_status` is `ok` for 313, `not_found` for 341 and never fetched for 650.
**Files.** `scripts/fit-guide-diagnostics.ts` (read-only; `--sample 30`), `docs/fit-engine/GUIDE_DIAGNOSTICS.md` (findings note).
**Method.** Sample 30 `guide_fetch_status = 'not_found'` rows; report their `opportunity_number` prefixes (PA / PAR / PAS / RFA / NOT / non-NIH) and the URL `guideUrlFor()` produced from `opportunity_number` and `additional_info_url`. Fetch 10 by hand and classify each: (i) the page exists but fails the `/Key Dates|Application Due Date/i` heuristic in `fetchNihGuideHtml`; (ii) the URL pattern is wrong (e.g. `PAS-` prefixes, `additional_info_url` pointing somewhere other than the Guide); (iii) genuinely no Guide page (non-NIH). Propose the fix for each class.
**Acceptance.** The findings note lists the 30 prefixes, the 10 hand-checked URLs with their class, and a proposed fix. **No change to sync behavior in this PR.**
**Run 2026-09-05** (`GUIDE_DIAGNOSTICS.md`). Corpus 1,306 open: ok 313 / not_found 341 / never fetched 652. Class (i) heuristic failures: 0 of 10 — every miss is a hard 404. Class (iii) no Guide page: 215 of 341 (199 Simpler forecasts, 15 CDC `-18-000` placeholders, 1 HHS-OPHS `PA-FPT-` number). Class (ii): 124 posted NIH notices (FY27 105, FY26 15, FY25 3, FY28 1), all posted since 2025-11-26, whose NOFO exists only as a `<number>-Full-Announcement.html` attachment on Simpler.Grants.gov in the Guide's own markup — `parseNihGuide()` parses it unchanged. Of the 652 never fetched, 649 are non-NIH; 1 (`PAS-27-028`) is recoverable. Fixes proposed for PR 0.5 (Fix A: skip forecasts / placeholders / non-Guide numbers; Fix B: `PAS-` in `guideUrlFor()` + Simpler-attachment fallback via the existing `getOpportunity()`); expected ok ≈ 438 of ≈ 440 posted NIH-Guide notices.

### PR 0.5 · NIH Guide: sectioned full text and designation
**Prerequisite (found by the 0.5a validator, 2026-09-05).** The nightly sync's re-queue predicate `updated_at > guide_fetched_at` is defeated by the `set_updated_at` BEFORE UPDATE trigger, which stamps the Guide sync's own row 16–181 ms after the JS-side `guide_fetched_at`: every fetched row is due every night, and with `order posted_date desc` + `limit 400` the same newest ~400 rows (all 341 `not_found`) are re-fetched nightly (~341 wasted 404s a night, 85% of the budget). Fix this first: carry a Simpler-owned timestamp into `funding_opportunities.source_updated_at` from `hitToFundingRow` and re-queue on `source_updated_at > guide_fetched_at` (anything the trigger cannot touch). Until then "weekly retry", "7-day cadence" and `--force` mean nothing and `guide_html_hash` is the only thing preventing nightly re-parse.
**Sources (`GUIDE_DIAGNOSTICS.md`, PR 0.5a).** Zero heuristic failures — every miss is a hard 404. Of 341 `not_found` open notices, 215 have no Guide page (199 Simpler forecasts, 15 CDC `RFA-XX-18-000` placeholders, 1 HHS-OPHS `PA-FPH-27-001`) and 124 are posted NIH notices that since ~Nov 2025 exist only as a `<number>-Full-Announcement.html` attachment on Simpler.Grants.gov — the Guide's own markup, which `parseNihGuide()` reads unchanged (18 and 4 receipt cycles on PAR-27-064 / RFA-CA-27-020; RFA-DK-26-308/309 already hold the attachment in `raw_payload_json`).
**Schema.** `funding_opportunities` + `guide_sections JSONB` (array of `{part, section, heading, text}`), `clinical_trial_designation TEXT CHECK (… IN ('required','optional','not_allowed','besh_required','unknown'))`, `program_division TEXT`, `guide_html_hash TEXT`, `guide_source TEXT CHECK (guide_source IN ('grants_nih_gov','simpler_attachment'))`, `source_updated_at TIMESTAMPTZ`; widen the `guide_fetch_status` CHECK with `'not_applicable'` (forecasts, placeholders, ill-formed numbers) so INVENTORY § 4 and the data-sources page stop reporting them as failures (`inventory.sql`: split `not_found` by `forecasted`).
**Code.**
- Fix A — skip what has no page: `syncNihGuide` excludes `forecasted = true` rows (they re-queue when posted: the nightly upsert flips `forecasted` and bumps `source_updated_at`); `guideUrlFor()` requires the Guide number shape `^(RFA-[A-Z]{2}-\d{2}-\d{3}|PA[RS]?-\d{2}-\d{3})$` (zero of the 313 `ok` rows fail it; the 16 ill-formed / placeholder rows are exactly the `not_applicable` set); keep the `NOT-` branch. The sync's SELECT must add `forecasted`, `agency_code`, `source_opportunity_id`, `guide_url`.
- Fix B — Simpler attachments: `guideUrlFor()` maps `PAS-` → `pa-files/` (`/^PA[RS]?-/`); on a classic-path 404 for a posted NIH row, call the existing `createSimplerGrantsClient().getOpportunity(source_opportunity_id)`, pick the `attachments[]` entry `^<number>-Full-Announcement\.html$` (text/html), fetch it with the same `fetchNihGuideHtml` + `parseNihGuide`, persist the URL in `guide_url` with `guide_source = 'simpler_attachment'`, and merge `attachments` into `raw_payload_json` (`mergeHitWithDetail` already preserves it; add `attachments: { file_name, mime_type, download_path }[]` to `SimplerOpportunityHit`). On later runs prefer a stored `files.simpler.grants.gov` `guide_url` before the classic path; a 404 on a stored Simpler URL means re-resolve via `getOpportunity` (attachment UUIDs change on re-upload), not `not_found`. One Simpler GET per affected notice, once.
- Reissue lineage (found by the 0.6 builder, confirmed by its validator): `parseNihGuide`'s reissue regex at `parse.ts:179` (`[A-Z]{2,3}-[A-Z]{2}-\d{2}-\d{3}`) and the companion at `:181` demand an IC segment, so "Reissue of PAR-xx-xxx" is never captured (46 rows carry `reissue_of`, every predecessor an RFA, only 2 PARs). Make the IC segment optional (`[A-Z]{2,3}-(?:[A-Z]{2}-)?\d{2}-\d{3}`), add a `Reissue of PAR-22-181` test, re-parse stored rows, and null `exemplars_fetched_at` where `reissue_of` changed so PR 0.6 re-fetches those lineages.
- Then the original scope, with one parser for both hosts:
- `src/lib/ingestion/nih-guide/parse.ts`: add `parseGuideSections(html)` returning the sectioned text (Part 1 Overview/Purpose; Part 2 Section I with sub-headings Background / Research Objectives / Specific Areas of Research Interest / Non-Responsive; Section II; Section III incl. III.3; Section IV clinical-trial and human-subjects items; Section VII contacts with division). Robust to the Guide's two HTML layouts (`datalabel`/`datacolumn` rows and free headings); strip boilerplate; keep heading text verbatim. Add `parseClinicalTrialDesignation(title, html)` (title suffix first, then Section II text; BESH detection) and `parseProgramDivision(sectionVII)`.
- `src/lib/services/nih-guide-sync.ts`: store the new fields in the existing update (same fetch; no new requests). Set `guide_html_hash` so unchanged pages skip re-parsing.
- Extend `scripts/backfill-nih-guide.ts` with `--sections-only` to re-parse for rows already fetched? Not possible — HTML is not stored. Instead the normal sync re-fetches on its 7-day cadence; add `--force` support to re-fetch open notices within the rate limit over a few nights (`limit` 400/night is existing behavior).
- Backfill: `scripts/backfill-nih-guide.ts --force` over the 124 posted `not_found` rows first (print the attachment hit rate), then open `ok` rows over a few nights within the existing 400/night limit.
**Acceptance.** (1) After the predicate fix a nightly run re-fetches only rows Simpler changed — report the due count before and after; (2) forecasts, placeholders and ill-formed numbers are `not_applicable` and never fetched; (3) the attachment hit rate over the 124 posted `not_found` rows is printed and ≥ 90% resolve to `ok`; (4) `reissue_of` captures PAR predecessors (count before / after); (5) sections parsed for ≥ 90% of `guide_fetch_status = 'ok'` notices in a 50-notice sample; designation agrees with the title for 100% of titled notices; tests on 3 saved Guide HTML fixtures (an RFA, a PAR, a notice with BESH), at least two of them Simpler attachments (PAR-27-064, RFA-CA-27-020).
**Built 2026-09-05** (branch `fit/0.5-guide-sections`; migration `20260914100000_fit_guide_sections.sql` written, not applied; dry runs only). *Predicate:* `source_updated_at` = the later of Simpler's `summary.updated_at` (on every search hit) and top-level `updated_at` (detail records), written by `hitToFundingRow`; `planGuideFetch` / `isGuideFetchDue` (pure, tested) never read `updated_at`. Due tonight over the 920 candidates: **920 before** (every row; the first 400 by posted date, 393 of them `not_found`) → **1 after** (`PAS-27-028`, never fetched) plus a one-time `not_applicable` stamp of 365 rows (344 forecasts, 21 placeholder / non-Guide numbers — 16 of them open, the 0.5a set exactly). *Fix A:* `guideUrlFor()` requires the Guide shape and rejects `-000`; `PAS-` → `pa-files/`; the candidate filter uses `PAS-__-___` so State Department `PAS-JAKARTA-…` numbers are never candidates. *Fix B:* on a 404 (or a stored Simpler URL that 404s) one `getOpportunity` GET resolves `<number>-Full-Announcement.html` **or `<number>-Revised-Full-Announcement.html`** (the revised name is what NIH uploads after a change — 3 of the first 25 misses and RFA-DK-26-309 were this); `attachments` are merged into `raw_payload_json`, the attachment URL is stored in `guide_url` with `guide_source = 'simpler_attachment'` and read directly on later runs. Dry run over the newest 25 open posted `not_found` rows: **23 of 25 resolved to `ok` (92%)**; the two misses are CDC `RFA-CK-26-035/036`, which have no attachment on Simpler either. The classic page of `PAR-27-026`, `PA-27-034/035/036`, `RFA-DK-26-308` is the Guide's plain-text template (no Key Dates rows, no headings); the sync now prefers the styled Simpler attachment for those too (5 of 5 resolved; PA-27-03x gain 18 receipt cycles each). *Reissue:* both regexes take `[A-Z]{2,3}-(?:[A-Z]{2}-)?\d{2}-\d{3}`; before: 46 rows with `reissue_of`, 0 PA/PAR; in the dry-run sample 18 of 23 re-parsed notices carry a predecessor, 9 of them PA/PAR/PAS. `--reparse-reissue` can re-derive only from stored text — Simpler's `description` (3 rows: PAR-25-352/353 → PAR-20-118/119, PAR-25-291 → RFA-20-351) and, once written, the Part 1 "Announcement Type" block in `guide_sections`; every other row is re-derived by the next fetch. `--reset-exemplar-stamps` probes `exemplars_fetched_at` (present on the database today) and nulls it where `reissue_of` changed. *Sections:* `parseGuideSections` keeps Part 1 (Announcement Type, Components of Participating Organizations, Funding Opportunity Purpose), Section I split on `<h4>` / `heading4` / bold-only-paragraph sub-headings, Section II rows, Section III minus the standard eligibility lists (numbered items become `III.1`, `III.3`), Section IV items matching human subjects / clinical trial / research plan (`IV.2`), Section VII scientific and peer-review contacts; the plain-text template falls back to section-level text. `parseClinicalTrialDesignation`: title suffix, then the "Clinical Trial?" row; `parseProgramDivision`: first "Division of … / Center for … / … Branch / … Program (ABBR)" line under the scientific contact that is not the IC, a person, a job title or a phone/e-mail line, null when ≥ 3 ICs are listed (parent notices). `guide_html_hash` = SHA-256 of the visible text with comments / scripts / styles removed. Fixtures: `PAR-27-064` and `RFA-CA-27-020` (Simpler attachments), `PA-25-303` (classic, BESH), `PAR-27-026` (plain template). Left for the coordinator after the migration: `backfill-nih-guide --force --status not_found --open` (126 rows, prints the hit rate), then `--force --status ok --limit 400` a night at a time; acceptance (5) is measured on the stored rows after that pass.

### PR 0.6 · RePORTER exemplars by announcement
**Schema.** New `opportunity_exemplars (opportunity_number TEXT, project_num TEXT, fiscal_year INTEGER, title TEXT, abstract TEXT, activity_code TEXT, rcdc_categories TEXT[], study_section TEXT, item_profile JSONB, fetched_at TIMESTAMPTZ, PRIMARY KEY (opportunity_number, project_num))`.
**Code.** `src/lib/ingestion/reporter/exemplars.ts`: query RePORTER v2 `/v2/projects/search` with `criteria.foa = [numbers]` for the notice's `opportunity_number` and its `reissue_of` lineage (walk `reissue_of` up to 4 steps through `funding_opportunities`); store up to 60 exemplars per notice, newest fiscal years first. Cron route `/api/cron/fit-exemplars` (monthly; `vercel.json`), batched with the existing `AsyncRateLimiter`. Verify `criteria.foa` behavior against the RePORTER API docs first and record the finding in DECISIONS.md.
**Acceptance.** For 10 reissued PARs in the corpus, exemplars are fetched and counts recorded; for 3 new RFAs, zero exemplars and no error.
**Built (2026-09-05, branch `fit/0.6-reporter-exemplars`).** Migration `supabase/migrations/20260913130000_fit_reporter_exemplars.sql`: `opportunity_exemplars` as above plus `core_project_num`, `appl_id`, `awarded_under` + `lineage_depth` (which lineage number the award actually cites), `award_type`, `study_section_code`, `pi_names`, `org_name`; and the resume stamp on `funding_opportunities` — `exemplars_fetched_at`, `exemplars_fetch_status` (`ok` | `error` | `skipped`), `exemplars_count`, `exemplars_lineage` — stamped for zero-exemplar notices too, so a rerun never re-fetches the corpus (refresh after 30 days, errors after 7). `src/lib/ingestion/reporter/exemplars.ts` (pure, unit-tested: number normalization, lineage walk, request body, response → rows, newest-first cap) and `exemplars-sync.ts` (Supabase orchestration shared by the cron and the script); `/api/cron/fit-exemplars`; `scripts/fit-reporter-exemplars.ts` (`npm run fit:reporter-exemplars -- --dry-run | --limit N | --opportunity PAR-25-122,…`). RePORTER's filter is **`criteria.opportunity_numbers`** (the documented name; `criteria.foa` from the plan text is an undocumented swagger alias that returns identical results): exact per number, case-insensitive, several numbers OR together, and the reissue lineage is *not* followed by the API — it is walked here. One row per core project (a project's continuation years collapse onto its newest award; a supplement loses to its parent), newest fiscal year first, cap 60, `exclude_subprojects: true`, one request per 100 award-years and at most 5 pages per notice. The cron is scheduled **daily** (07:00 UTC, inside NIH's off-peak window) with the 30-day staleness predicate — that is the monthly refresh per notice; a single monthly run cannot cover 657 open NIH notices in 300 s. 80 notices per run, oldest stamp first, no new notice started after 240 s.
**Acceptance run (dry run, 2026-09-05).** 13 notices, 14 requests, 7.9 s, no errors. Reissued: PAR-25-122 60 of 79 projects (21 under its own number, 39 under RFA-TR-22-030), PAR-24-127 15 (3 + 12 under RFA-DC-20-002), RFA-HL-26-012 40 (2 pages), RFA-FD-25-020 20, RFA-TR-25-002 14, RFA-HG-25-009 13, RFA-DA-27-004 9, RFA-AG-26-010 7, RFA-DC-25-002 5, RFA-MH-26-170 2. New RFAs RFA-RM-28-002, RFA-AI-27-005, RFA-CA-27-005 (posted 2026-08/09): 0 exemplars. Only two PARs in the corpus carry `reissue_of` (46 rows in all, every predecessor an RFA, none present as a row): `parseNihGuide`'s regex `[A-Z]{2,3}-[A-Z]{2}-\d{2}-\d{3}` requires an IC segment, so "Reissue of PAR-22-181" is never captured — a PR 0.5 parser fix; until it lands, PAR lineage is depth 0 and the blend for reissued PARs rests on awards under the notice's own number.

---
**First pass (2026-09-05).** 657 NIH-like notices stamped in eight `--limit 80` passes (654 `ok`, 3 `skipped`, 0 errors), 2,929 exemplar rows over 225 notices; the newest-posted-first order yields almost nothing until the reissued PARs/RFAs are reached (70 rows in the first 240 notices, 2,800 in the rest). Daily cron takes over with the 30-day staleness stamp.

## Phase 1 — Classify

### PR 1.1 · Taxonomy module and types
**Files.** `src/lib/fit/taxonomy.ts` (typed accessors over `taxonomy.json`: `familyOf(cat)`, `familyCompat(a,b)`, `categoryCompat(a,b)`, `levelCompat(a,b)`, `designGroupOf(design)`, `floors(tier)`), `src/lib/fit/types.ts` (`ItemProfile`, `InvestigatorFitProfile`, `OpportunityFitProfile`, `Components`, `Tier`, `FitResult`, `Correction`), `src/lib/fit/taxonomy.test.ts` (matrix symmetry, every category has a family, every design has a group, floors monotone Strong ≥ Moderate ≥ Exploratory).
**Acceptance.** Tests green; `tsc` clean; no runtime code beyond accessors.
**Built (2026-09-05, branch `fit/1.1-taxonomy-types`).** `src/lib/fit/types.ts` — id unions derived from the JSON two ways: where the JSON keys ids as object properties (`ParadigmFamily`, `ParadigmCategory`, `UnitLevel`, `DesignGroup`, `MaterialsGroup`, `Tier`, `EvidenceSource`, `EvidenceRole`, `ExploratoryExceptionId`, `ConfidenceCapId`, `ClinicalTrialDesignation`) the union is `keyof typeof` a type-only JSON import and cannot drift; where the JSON lists ids in arrays (`UNIT_IDS`, `DESIGN_IDS`, `MATERIALS_KIND_IDS`, `OBJECTIVE_IDS`) TypeScript infers `string[]`, so the union is an `as const` tuple that `taxonomy.test.ts` pins to the JSON in order. `MatrixFamily` = every family but `cross_cutting` (pinned to `family_compat.order`); `Tier` excludes the JSON's `_comment` key; `CapId` = the gate caps (`paradigm_gate`, `unit_gate`, `design_required_unsupported`) + `confidence_caps` keys minus `_max_tier` + `paradigm_gate_relaxed_<exception>`, which is what the adversarial fixture's `expect.caps` uses. `ItemProfile` carries the identity and weighting fields aggregation needs (`id`, `kind`, `source`, `year`, `role`) beside the five axis vectors, `topic` (MeSH UIs per D12, RCDC, terms), `confidence`, `decided_by` per axis, `rules_fired`, `justification`. `InvestigatorFitProfile` follows the spec §5 record (`paradigm.career` + `paradigm.recent` both required; `aspirations` as classified categories per D5; `do_not_suggest` as family ids; `collaborators` with `dominant_family`, as the fixture has them; `evidence_summary.biosketch` typed by `SourceState`). `OpportunityFitProfile` follows the §6 record and the notice-extractor fields verbatim — `design.required_any` / `required_any_2` (any-of within, all-of across), `unit.required` / `required_any`, `materials.expected` / `required` / `required_any`, `mechanism.clinical_trial` = designation or `unknown`, `provenance` keyed by field path, `sources.text` ∈ full_text / synopsis / none. `Components` = E P U D T M O K A; `TierFloors` mirrors `taxonomy.tiers` with absent keys meaning "any"; `FitResult` = components, `caps: CapId[]`, `score`, `tier`, a `FitProvenance` shaped by the §7 stage outputs (E flags, P best pair / excluded hit / exception, U best pair, D unmet groups / dominant prohibited, T top items / coded matches, floors unmet), `flags`, `gap`, `why_not`, `rationale`; `Correction` is the reconciler prompt's object (`target`, `path`, `from`, `to`, `evidence_ids`, `quote`, `section`, `kind`, `confidence`) plus `CorrectionAuthor` / `CorrectionStatus` for PR 3.1's table. The adversarial fixture is not assignable as-is — it relies on `defaults` and omits `paradigm.career`, `required_any_2` and most eligibility fields — so PR 2.1's loader normalizes it. `src/lib/fit/taxonomy.ts` — `TaxonomyError(table, id, hint?)` names the JSON key path and the id; every accessor throws it for an unknown id, including prototype keys. Accessors: `familyOf`, `categoriesOf`, `familyLabel`, `categoryLabel`, `familyCompat`, `categoryCompat`, `withinFamily`, `paradigmGates`; `levelOf`, `unitsOf`, `levelLabel`, `levelCompat`, `unitGates`; `designGroupOf`, `designsOf`, `designScoreWeights`, `designGates`; `materialsGroupOf`, `materialsOf`; `reliability`, `roleWeight`, `recency`, `saturationExponent`, `thinEvidence`, `confidenceThresholds`; `composeExponents`, `relevanceWeights`, `topicWeights`, `actionabilityParams`; `floors`, `poorTier`, `exploratoryException`, `confidenceCap`; `exemplarBlend`, `activityCodePrior`, `clinicalTrialOverlay`; id lists (`PARADIGM_FAMILY_IDS` … `TIER_IDS`, `TAXONOMY_VERSION`) and `isX` type guards for validating ids that arrive as strings (rule tables, model output). Decisions: the JSON has no category-level matrix, so `categoryCompat` is derived — same category `within_family.same_category`, sibling `within_family.sibling_category`, else the family matrix; `familyCompat` / `categoryCompat` **throw** for `cross_cutting` (not in `family_compat.order`) with the sqrt(U·D) hint rather than returning a number, so PR 2.1 must branch on `isMatrixFamily` explicitly; `floors("poor")` throws (no floors); `activityCodePrior(code)` returns `null` for a code the prior table does not mention (it is a list of priors, not a registry) and `{}` for a listed neutral code. `self-declared.ts` now imports `ParadigmFamily` / `MaterialsGroup` / `MaterialsKind` from `types.ts` (re-exported) and `MATERIALS_LABEL` is `Record<MaterialsKind, string>`, so a missing label is a tsc error. `taxonomy.test.ts` checks: tuples ↔ JSON; both matrices square over `order`, symmetric, unit diagonal, in [0,1]; every category ↔ family both ways, every unit / design / kind in exactly one parent; floors monotone Strong ≥ Moderate ≥ Exploratory over every numeric key a lower tier carries; the cross-table invariants the engine will lean on (`tiers.exploratory.P` = `paradigm.gates.poor_below`, `P_with_aspiration` = `exploratory_below`, `tiers.exploratory.U` = `unit.gates.poor_below`, `tiers.moderate.D_required_group_min` = `design.gates.required_group_unsupported_below`, `tiers.strong.T_specific_depth` = `compose.topic.min_specific_depth_for_strong`); every `opportunity_profile` overlay and prior id resolves; and the signal-mapping ↔ taxonomy contract — every id in every `assign` and `refine` block resolves on its axis with a probability in [0,1], every rule assigns in exactly one way, every `assign_notice` path points at an existing `opportunity_profile` table, the self-declared rule's `reliability` equals `aggregation.reliability.self_declared_current`, `author_position` maps onto `aggregation.role` keys. Findings: `taxonomy.json` is internally consistent (no asymmetry, orphan or non-monotone floor) and every signal-mapping id resolves; the spec's §4 counts are stale against the JSON (§4 says 27 designs / 16 materials kinds / 14 units, the JSON has 38 / 24 / 15 — `gwas` and the split animal kinds), `preclinical` and `translational` are both a family id and a category id (namespaced by table; pinned by a test so a third one is noticed), and the §5 sample record uses `human_blood` and RCDC `Clinical Trials`, neither of which exists (`human_blood_fluids`; D9). Not built: no scoring, no `FitContext` (PR 2.1 defines `scorePair`'s context; `ActionabilityInputs` is here for it), no stage-8 verdict types (PR 3.1).

> **Kickoff prompt.** Read `/CLAUDE.md`, spec §4, and `src/lib/fit/taxonomy.json`. Create `src/lib/fit/types.ts` and `src/lib/fit/taxonomy.ts` as typed, side-effect-free accessors over the JSON, with the tests listed in this plan § PR 1.1. Do not add scoring logic yet.

### PR 1.2 · Rule classifier
**Files.** `src/lib/fit/classify/normalize.ts` (`NormalizedItem` from a publication row + mesh_descriptors, a grant row, a trial row, a biosketch statement, a profiles record, self-declared data); `src/lib/fit/classify/rules.ts` (evaluate `signal-mapping.json › rules` against a `NormalizedItem`; noisy-OR merge; `refine` blocks; `not` clauses; `assign_from_table` for `study-sections.json` and `program-divisions.json` — create both tables seeded from values found in INVENTORY.md); `src/lib/fit/classify/rules.test.ts` (one test per rule id with a minimal item; plus the six item-classifier fixtures expecting rules to fire where MeSH is present).
**Acceptance.** Every rule in `signal-mapping.json` has a test; unknown MeSH names fail loudly; the six fixtures produce the expected dominant categories.
**Built (2026-09-05, branch `fit/1.2-rule-classifier`).** `src/lib/fit/classify/normalize.ts` — the contract PR 1.3 imports: `NormalizedItem { id, kind, title, text, year, role, mesh, publication_types, signals }` with `NormalizedItemKind` = publication · grant · trial · biosketch_statement · biosketch_contribution · profiles_narrative · self_declared, and one pure builder per source (`normalizePublication` validates every stored MeSH UI against the descriptor index and throws `MeshUnknownDescriptorError` on one it does not carry; `normalizeGrant` keeps `rcdc_categories` NULL vs `[]` as stored and reads the Special Emphasis Panel designator from `raw_json.full_study_section.sra_designator_code`; `normalizeTrial` stores `phases` literally with `phases_informative` false for `[]` and `["NA"]`; `normalizeBiosketch` yields the statement plus one item per contribution; `normalizeProfiles` reads `investigator_sources.meta.narrative` and carries the directory columns (`title_series`, `home_department`, `division`, `degrees`) as signals; `normalizeSelfDeclared` carries the D5 record, the intake answers keyed by normalized header, and aspirations in `signals` — never in `text`). Text is cut at the prompt spec's 6,000 characters with `text_truncated` / `text_chars` recorded. `src/lib/fit/classify/rules.ts` — `evaluateRules(item, { mesh, tables })` → `{ axes, fired, firedAxes, refinedBy, unknownTableKeys }` (the last is an addition to the shared contract, optional so PR 1.3 is unaffected). Clause keys are parsed generically as `<family>[_any|_all|_present][_<n>]`: `mesh` / `mesh_major` / `check_tag` / `pubtype` by descriptor **name** in the stored MeshHeadingList (never `is_check_tag` — Zebrafish, Primates and Macaca are B01 descriptors), `mesh_tree_under` by tree prefix, `triangle_class` by the Weber class, CT.gov / RePORTER enums folded the PR 0.3 way, `rcdc_any` with NULL and `[]` alike, `enrollment_min`, `title_series_any` (word-bounded substring), `department_match` (department or division), `intake_field` + `affirmative` through `isAffirmative`, `self_declared_present`, and the two `*_table` clauses; `when` is a conjunction, `not` (top-level or nested in `when`) a disjunction of hard negatives; `refine` overrides the rule's own values for the categories it names (max over several matching values, e.g. PHASE1 + PHASE2) before the cross-rule noisy-OR p = 1 − Π(1 − pᵢ); `assign_from_table` spreads a family prior over every category of the family (max with an explicit `categories` prior); `assign_from_self_declared` maps the D5 grid as rating / 3 (Core → 1.0, Some → 0.33, Not my work → nothing) and every ticked material → 1.0; `assign_notice` rules never fire on an item — `matchNoticeRules(subject)` evaluates their `when` for PR 1.5, taking a `RuleSubject` (`mesh`, `publication_types`, `signals`) so a notice record needs no fake item. Every category id in every block and table is validated with the 1.1 guards at first use (`TaxonomyError`), every MeSH name is resolved at first use (`MeshUnknownDescriptorError`), and a clause kind the evaluator does not implement throws `RuleClauseError` — a new key in the mapping cannot silently never fire. **Tables.** `src/lib/fit/study-sections.json` (184 `by_code` entries keyed by CSR `srg_code` or, for SEPs, by `sra_designator_code`; `by_name` for codes an IC shares across panels — NCI, CHHD; `families: {}` = known panel with no paradigm signal, matched but not fired and not reported) and `src/lib/fit/program-divisions.json` (27 entries keyed by abbreviation with spelled-out `names`; the `program_division` column is not on the database yet, so no corpus counts), both seeded from the corpus (read-only) plus the spec's examples, every entry with a `_source`. Corrections from the stored records: `RPRB` is NCI's Research Programs Review Branch (every row `ZCA1`), not CSR's Risk, Prevention and Health Behavior (`RPHB`) — the first seed had it at population .6; `GMBB` (28 rows) is an NHGRI intramural branch code on four Z01 projects, not a review panel. SRO-initial SEP designators (NIAID `ZAI1` QV, JBS, JTS, MMF, MP, RRS, …) stay absent on purpose and the report lists them. **D5 pick-one.** When the stored `self_declared_axes.materials` already carries a kind an `intake_field` rule would assign, the intake rule is skipped — the record wins, the intake text is the fallback for rows imported before PR 0.7. **PR 0.3 follow-up.** `phases = ["NA"]` still leaves `ctgov_interventional_pi` at its default `rct .7`; the refine machinery is generic over any signal, so the proposed `allocation` / `intervention_model` refine keys are a mapping edit only (exact JSON in the PR report; `signal-mapping.json` was not edited by this PR). **Tests** (`rules.test.ts`, `normalize.test.ts`, `rules-report.test.ts`): one test per rule id with a must-not-fire case for every `not`, pinned to the mapping by a coverage test (69 rules); the six item-classifier fixtures; noisy-OR arithmetic; refine incl. PHASE1 + PHASE2 and `["NA"]`; both tables incl. SEP designator, unknown key, no-signal panel and `by_name` override; every loud failure. **Report** (`scripts/fit-rules-report.ts`, `npm run fit:rules-report`, read-only, pages the non-archived roster's verified rows; pure aggregations in `rules-report.ts`): 144 investigators, 15,375 publications, 804 grants, 14 trials; ≥ 1 rule fired on 75.1% of publications (paradigm 73.9%, unit 55.3%, design 55.1%, materials 42.1%, objective 16.1%), 73.3% of grants (paradigm 72.1% — study-section family 479 + RCDC), 100% of trials; the corpus reads as the § 11 inventory predicted — `triangle_animal_human` 6,744, `tag_animals_only` 2,951, `mesh_disease_models_animal` 2,581, `mesh_mechanism` 2,122, `mesh_cell_culture` 1,723 versus `pt_rct` 242 and `mesh_cohort_epi` 71; dominant paradigm over rule-decided publications is translational 3,619 / animal_model 3,127 / molecular_cellular_mechanistic 3,100. 18 of 69 rules never fire on this corpus: the five notice rules (no notice items yet), the five profiles / directory / self rules (`title_series` NULL and `home_department` = "UCSF" on all 144 rows, no self-declared grid answered, no intake answer on `raw_profile_json`, all 144 biosketch rows `not_requested` — data, not code), and eight corpus absences (`mesh_cbpr`, `ctgov_interventional_listed`, `ctgov_hsr_purpose`, `ctgov_observational_cohort_large`, `reporter_r18`, `reporter_rcdc_cer` / `_di` / `_translational`). Fixture outcomes vs the prompt spec: (1) grant — only the panel prior fires (discovery), nothing clinical, unit / design / materials left to the model; (2) biosketch — nothing fires; (3) epidemiology .955 / L4 / retrospective_cohort / ehr — no clinical_trials; (4) implementation_science .97, L5 .98 beside L3 .984 from the RCT / pragmatic publication types, hybrid .5; (5) genetic_epidemiology .9 / L4 / gwas / genomic_datasets, `Cohort Studies` keeps the `Biological Specimen Banks` heading out of `human_biospecimen`; (6) molecular_cellular_mechanistic .85 tied with translational .85 (the CH bridge), human_biospecimen .8, L1 .96, perturbation .7, no clinical_trials, no L4 — the rules assign tissue / blood, never `human_primary_cells`, which the model supplies. Not built: no mapping or taxonomy edit, no model call, no cache (PR 1.3), no aggregation (PR 1.4).
**Validator (2026-09-05).** `role` now carries taxonomy `aggregation.role` ids (author_position and a new `investigator_role` map in signal-mapping.json, D18) so PR 1.3's provenance and PR 1.4's role weights read them; `signals.rcdc` aliases `rcdc_categories`; the D5 rating scale maximum lives in the JSON; `mesh_primary_human_cells` added (fixture 6 now gets `human_primary_cells`); `Biological Specimen Banks` removed from `human_biospecimen`. Deferred, recorded here: (a) the allocation / intervention_model refine — the refine merge is max-over-values, so `rct .2` from SINGLE_GROUP can never lower a phase refine's `rct`; move `rct` from the PHASE2/3/4 refines into `allocation` (RANDOMIZED .85–.95, NON_RANDOMIZED / NA .2), add SINGLE_GROUP → `single_arm_interventional .8`, with tests; (b) `intakeRuleSuperseded` should skip only the materials axis, keeping the intake rule's paradigm prior; (c) `matchNoticeRules` selects `assign_notice` rules only — PR 1.5 must call `lookupProgramDivision` for `notice_program_division` or the selector should key on `source === "notice"`; (d) unidentified CSR SEP designators (CN, CTH, DCAI, RCCS …) are listed by `fit:rules-report` for a person to name.

### PR 1.3 · LLM item classifier and cache
**Schema.** `fit_item_profiles (content_hash TEXT PRIMARY KEY, kind TEXT, ref_id TEXT, taxonomy_version TEXT, rules JSONB, llm JSONB, merged JSONB, llm_model TEXT, created_at TIMESTAMPTZ)`.
**Files.** `src/lib/fit/classify/llm.ts` (prompt from `docs/fit-engine/prompts/item-classifier.md`; JSON mode; validation per that spec), `src/lib/fit/classify/index.ts` (`classifyItem(item)`: rules → llm when needed → merge with rules overriding → cache), `src/lib/fit/classify/llm.test.ts` (validation and merge logic with a mocked model).
**Acceptance.** The six fixture items produce outputs matching the prompt spec's expectations (paste in PR); cache hit on second call; D2/D3 answered.
**Built (2026-09-05, branch `fit/1.3-llm-item-classifier`).** D2 and D3 recorded as taken: `FIT_MODEL_CLASSIFY` defaults to `gpt-4o-mini` (the `MODEL` constant in `src/lib/outreach/profile.ts`) and the only network path is the OpenAI-compatible endpoint that module already calls (`openaiModel()` in `llm.ts`: JSON mode, `temperature: 0`, `max_tokens` 2,500). Migration `20260914110000_fit_item_profiles.sql` (written, not applied): the table as specified with `kind` / `taxonomy_version` / `merged` NOT NULL and `created_at DEFAULT now()`, an index on `ref_id`, COMMENT ON every column, RLS like its neighbours. `src/lib/fit/classify/contracts.ts` — the PR 1.2 contract (`NormalizedItem`, `NormalizedItemKind`, `Axis`, `AxisWeights`, `RuleClassification`, `RulesFn`) declared structurally so this branch compiles before PR 1.2 lands (the coordinator swaps it for re-exports at landing), plus `AXES` and `noRules`. `llm.ts` — `SYSTEM_PROMPT` verbatim from the prompt spec (a test pins each vocabulary line to the taxonomy id lists), `buildUserPrompt` from the user template (`(none)` / `(unknown)` placeholders; `mesh_names` added after `Year:` as "MeSH headings (context only; rules already used them): …" only when present, because the spec lists them as an input but its template has no line for them; text cut at 6,000 chars); `ModelFn` is injectable (`ModelReply` = content or `{ content, finish_reason }`); `validateModelOutput` is pure: unknown top-level keys and non-vocabulary ids dropped (PR 1.1 guards; unit keys are the levels L1–L5), non-numbers dropped, values clamped to [0, 1] with the clamp logged, explicit zeros omitted, top four per axis by value (ties in the model's order), `confidence` outside the vocabulary → `medium` (logged), `low` halves every value (`halved: true`), `topic_terms` strings / trimmed / case-insensitively deduplicated / at most 8, `justification` axis keys and strings only — every removal or change is a line in `dropped`, stored with the raw reply for audit; `classifyWithModel` logs `output: truncated by max_tokens` when `finish_reason` is `length`, returns empty axes with the parse error logged when the reply is not JSON, and lets a model-function failure (auth, rate limit) propagate so nothing empty is cached. `cache.ts` — `ItemProfileCache { get, set }` over rows shaped like the table; `InMemoryItemProfileCache` (counts reads and writes) and `supabaseItemProfileCache(db)` (`maybeSingle` by primary key, upsert on `content_hash`, errors thrown rather than treated as misses; exercised by PR 1.4, not here). `index.ts` — `classifyItem(item, { rules, model?, modelName?, cache?, now? })`: rules first; **model needed ⇔ the trimmed text is ≥ 40 chars and at least one axis is not in `rules.firedAxes`** (`modelNeeded` returns the reason in words); the cache is read before the call and a cached `llm` is reused, skipping the model; **merge: on every axis in `firedAxes` the rule values are taken and the model's discarded (recorded in `discarded_model_axes`), elsewhere the model's; an axis neither decided is empty and absent from `decided_by`**; cache key **`contentHash(TAXONOMY_VERSION + "\n" + kind + "\n" + text)`** (`itemCacheKey`); the row is written after a model call and on first sight of a rules-only item, never on a hit; a cached row without `llm` when the model is now needed (rules changed) is re-run and rewritten. The `ItemProfile`: `source` from `signals.source` when it names a reliability key, else by kind (publication → `pubmed_verified`, grant → `reporter`, trial → `ctgov_pi` when `role` is `trial_pi` else `ctgov_listed`, biosketch kinds → `biosketch`, `profiles_narrative` → `profiles`, `self_declared` → `self_declared_current`); `role` only when it is an `aggregation.role` key; `topic.mesh` / `mesh_major` from `item.mesh` UIs, `rcdc` from `signals.rcdc`, `terms` from the model; `confidence` = the model's when any axis is the model's, `high` when rules alone decided, `low` when nothing did; `justification` only for model-decided axes; `rules_fired` from `rules.fired`. `fixtures.ts` + `__fixtures__/item-classifier-fixtures.json` — the six spec items with text written for the fixture (synthetic), expectations per axis (`min`; `present` ≥ 0.3; `any`; `absent` ≤ 0.15) and a mocked `model_output` each; `checkAxes` is shared by the tests and the script. Tests: prompt pinned to the spec and the taxonomy; every validation rule; a reply cut off at `max_tokens` (`finish_reason` `length`) logged whether or not what came back still parses, and a non-JSON reply returned empty with the parse error; the model-needed rule; merge; cache hit on the second call with the model called once; the rules-only path (the model stub throws if called); the six fixtures through `classifyItem` with the mocked model, plus fixture 1 with a paradigm rule firing. `scripts/fit-classify-fixtures.ts` (`npm run fit:classify-fixtures`; `--dry-run` prints the prompts; `--only n,m`): the six items against the real endpoint with `noRules` and an in-memory cache, one agreement line per axis, then a cached re-run of item 1; hard cap 12 calls; the database is never read or written.

**Acceptance run (2026-09-05, `gpt-4o-mini`, 16 model calls in all: a first pass of 10, then 6 for the final run of the committed code).** Final run, spec prompt over all six: items fully agreeing 3/6 (2, 3 and 5), axes agreeing 19/23; the re-run of item 1 was a cache hit with no further call. Disagreements: fixture 1 `molecular_cellular_mechanistic` 0.50 against the spec's ≥ .7 (`animal_model` 1.00) and design `animal_in_vivo` 1.00, `wet_lab_experiment` 0.50, `single_cell` 0.50 with no `perturbation`, although the text names conditional knockouts and CRISPR editing; fixture 4 design `rct` + `mixed_methods` with no `hybrid_effectiveness_implementation`, although the model's own justification says "a hybrid type 2 effectiveness-implementation trial"; fixture 6 no `human_biospecimen` (`basic_discovery` 0.5, `molecular_cellular_mechanistic` 0.5, materials `human_primary_cells` 1.0). The first pass, same prompt, also scored 19/23 with a different pair of misses — fixture 1 had `perturbation` 0.50 and fixture 2 gave materials `patients_under_care` 1.00 without `enrolled_participants` (the final run lists both at 1.00) — so at `temperature: 0` the model still varies between runs on its 0.5-level entries, and a single run's agreement should be read as ±1 axis. Findings: the model quantizes to 0.5 / 1.0 rather than graded probabilities, so a `≥ .7` expectation turns on whether a category lands on 1.0 or 0.5; on fixture 5 it pretty-printed the whole vocabulary with zeros and the first pass's `max_tokens` 900 cut the reply mid-string (raised to 2,500 with `finish_reason` logged; fixture 5 then agreed on all four axes in both later runs, ~8 s each). A three-call experiment, not committed, appended a gloss block to the system prompt (`human_biospecimen` = mechanistic work on human-derived cells or tissue; `enrolled_participants` versus `patients_under_care`; name `hybrid_effectiveness_implementation` alongside the trial design; use graded values): it fixed the three targeted misses but lost others (fixture 2 unit empty, fixture 4 `implementation_science` gone, fixture 6 `perturbation` and `human_primary_cells` gone) — 7/11 axes against the spec prompt's 8/11 (first pass; 9/11 in the final run) on the same items — so the committed prompt stays verbatim to the spec and the proposed edits are in the PR report for the spec owner. In production two of the four final-run misses sit on axes PR 1.2's rules cover for indexed publications (fixture 4 MeSH *Implementation Science*; fixture 6 Humans + cells); both fixture 1 misses are on the RePORTER abstract, which is model-only beyond its RCDC and activity-code priors — where a stronger `FIT_MODEL_CLASSIFY` would show first, as would the biosketch (2), whose materials flipped between runs. Not built: no live Supabase path (PR 1.4), no cron, no re-classification beyond the taxonomy version being part of the key.
**Validator (2026-09-05).** A reply that did not parse or was cut off at `max_tokens` is now `usable: false`: it is returned with empty axes but never written to `fit_item_profiles` and never reused, so the next run calls the model again; items the model was not needed for write no row (rules are recomputed every call; `merged` in the table is a snapshot, not the source of truth — PR 1.4 consumes `classifyItem`'s return). `title`, `year` and MeSH shape the prompt but are not in the cache key (a publication classified while in-process keeps its model output after indexing; rules then override the MeSH-covered axes). Fixture agreement with `gpt-4o-mini` is 18–19 of 23 axes across runs — the model's raw choices (values quantized to 0.5 / 1.0; missing `human_biospecimen`, `enrolled_participants`, `hybrid_effectiveness_implementation`), not code; a stronger `FIT_MODEL_CLASSIFY` (`gpt-4o`) was tried first: 3/6 items, the same vocabulary misses plus `epidemiology` / `health_services` dropped on fixture 3 — so the spec was amended (D19). The first D19 attempt (definitions + graded-value guidance) measured 13/23 axes twice and was reverted the same day; definitions only, with the parser fix, measure 18/23 and 18/23 over two runs (baseline 18–19), and the array-shaped-reply parser fix that run exposed is in `llm.ts` (D19).

### PR 1.4 · Investigator fit profile
**Schema.** `investigator_fit_profiles (investigator_id UUID PRIMARY KEY, taxonomy_version TEXT, profile JSONB, confidence JSONB, item_count INTEGER, computed_at TIMESTAMPTZ)`.
**Files.** `src/lib/fit/profile/investigator.ts` — `collectItems(db, investigatorId)` (publications verified only, grants not rejected, trials, biosketch, profiles, self-declared, directory), `aggregate(items, now)` (pure: weights per `taxonomy.aggregation`, saturation, thin-evidence cap, career and recent views, confidence, provenance top-3 ids per category), `buildInvestigatorFitProfile(db, id)`; `investigator.test.ts` (aggregation math: 80/20 split → ≈ 0.87/0.38; thin-evidence cap; recency half-life; recent vs career). Cron `/api/cron/fit-profiles` (nightly, 09:00 UTC, after `embed-opportunities`) with a resume cursor and `limit`; also called from `refreshInvestigatorSources` completion.
**Acceptance.** Profiles built for the pilot roster; `scripts/fit-profile-report.ts` prints dominant paradigm, top designs and evidence counts per investigator for the spot check.
**Built (2026-09-05, branch `fit/1.4-investigator-profile`; fix pass the same day after the validator — B1–B3, S1–S6, N1–N6).** Migration `20260915100000_fit_investigator_profiles.sql` (written, not applied): the table as specified plus `pending_items INTEGER NOT NULL DEFAULT 0` (D20; also an `ADD COLUMN IF NOT EXISTS` for a database that took the first draft), every column NOT NULL, `computed_at DEFAULT now()`, an index on `computed_at`, COMMENT ON, RLS like its neighbours. **Taxonomy accessors** added to `src/lib/fit/taxonomy.ts` for the keys the coordinator's commit put in `taxonomy.json`: `priorSources()` / `isPriorSource()` (`aggregation.thin_evidence.prior_sources`, validated as reliability keys), `sourceOrigin(source)` (`aggregation.source_origin`; the `_comment` key and an unknown id throw `TaxonomyError`), `provenanceTop()` (`aggregation.provenance_top`) and `r01EquivalentCodes()` (`characteristics.r01_equivalent_codes`); the code constants `PRIOR_SOURCES`, `PROVENANCE_TOP` and `R01_EQUIVALENT_CODES` are deleted. `src/lib/fit/profile/aggregate.ts` — **pure** (`aggregate(items, now, ctx?)` and `aggregateWithDiagnostics`, accepting `ItemProfile[]` or PR 1.3 `ClassifiedItem[]`); every number and set is read through the accessors. Formulas as implemented: **w_item = reliability(source) × role × recency**, `aggregation.reliability[source]`, `aggregation.role[role]` (an item whose source carries no role — biosketch, Profiles, self-declared, directory — is 1.0; a publication / grant / trial with no role on file is `role.unknown`), recency = `max(recency.floor, 0.5^(age / recency.half_life_years))` with age = now.year − item.year, an undated publication / grant / trial at the floor and an undated current-state item at 1.0; **share_c = Σ w·p(c|i) / Σ w over the items on which the axis was decided** (`decided_by[axis]` present — an item that says nothing about an axis neither supports nor dilutes it, which is what makes the per-axis confidence honest; D20); **weight_c = share_c ^ `saturation_exponent`**, capped at `thin_evidence.cap` when fewer than `thin_evidence.min_items` non-prior items (p > 0; the `prior_sources` are "a prior, not evidence") and fewer than `thin_evidence.min_grants` grants support c; **career** = every item, **recent** = age ≤ `recency.recent_view_years` (undated current-state items in both); **confidence per axis** (all six keys, career view) = high when Σ w ≥ `confidence.high_min_mass` and distinct source **origins** ≥ `confidence.high_min_sources`, medium at the `medium_*` pair, else low — an origin is `aggregation.source_origin[source]` (`ctgov_pi` + `ctgov_listed` = one registry) and a prior never counts (`distinctOrigins`; `AxisView.origins` beside the informational `sources`) — topic the same over items carrying any MeSH / RCDC code or model term; the scale low < medium < high is pinned as `CONFIDENCE_ORDER`; **provenance** = the top `provenance_top` (3) item ids per (axis, category with weight > 0) by w·p, ties by id so a rerun is byte-identical, career view, so PR 1.6 has per-category top evidence without a second query; weights stored to 4 decimals, largest first, zeros dropped. The 80/20 test: ten equal-weight first-author publications from this year, eight `clinical_trials` 1.0 and two `translational` 1.0 → shares 0.8 / 0.2 → 0.8^0.6 = 0.8747, 0.2^0.6 = 0.3807, asserted against `Math.pow(share, saturationExponent())` so a JSON edit moves the expectation with it. `topic.mesh_major` = major-topic tree numbers resolved from the UIs through the descriptor index, ordered by weighted frequency (all of them; PR 2.1 applies depth and IDF); `rcdc` likewise; `free_text` null. **MeSH index** `src/lib/fit/classify/mesh-db.ts` — `loadMeshIndex(db)`: a head count, then 1,000-row ranges eight at a time (2.6 s for 31,110 descriptors against ~9 s for the serial page walk), memoized per process, a failed load not memoized, `MESH_EMPTY` for callers that skip quietly; `investigator.ts`, `sync.ts`, `fit-rules-report.ts` and the backfill import it (PR 1.5 created the same file for the same purpose; one copy lands). `src/lib/fit/profile/investigator.ts` — `collectEvidence` / `collectItems(db, id)`: publications `identity_status = 'verified'`, grants `<> 'rejected'`, trials `= 'verified'`, the biosketch and Profiles source rows, the self-declared record, and the directory row as its own item (`normalizeDirectory` in normalize.ts, kind `directory`, `signals.source = directory_metadata`, no text; `department` / `division` move off the Profiles item with `splitDirectorySignals` so `directory_epi_dept` fires once at 0.3 while `title_series` stays on the Profiles item per the spec's table — asserted end to end with "Epidemiology & Biostatistics"); a row that fails to normalize is listed and counts toward `pending_items`, never dropped silently. `characteristicsFrom` (pure): `mechanisms_held` = distinct activity codes over the non-rejected grants, `active_awards` = distinct core project numbers still active (RePORTER `project_end_date`, else fiscal year ≥ last year — the directory's rule), `esi` = false once a code in `characteristics.r01_equivalent_codes` was held else null, `career_stage` ∈ trainee / early / mid / senior from the rank string, `clinical_role` ∈ md_clinician_investigator (MD/DO with a clinical title series or a trial as PI) / md_investigator / phd_investigator / other, `trial_pi_count` from the CT.gov role. Aspirations (D5): one `self_declared`-kind item per line, classified by the item classifier when its text is long enough and the budget allows (top category of the reply), and always by `aspirationCategoriesByLabel` (category label, id-as-words, a small alias list still in code — `ASPIRATION_ALIASES`, proposed as `paradigm.categories[*].aliases` — then a family label → its categories), the union stored as `profile.aspirations`; `do_not_suggest` passes through validated as family ids; `collaborators` from `investigator_relationships` co-authors whose stored profile has a dominant paradigm (empty until profiles exist). `buildInvestigatorFitProfile(db, id, deps)`: collect → prefetch the item cache for every key in reads of `PREFETCH_CHUNK` = 100 keys (200 sixty-four-character hashes in one `in (...)` was past the usual 8 KB URL limit) → `classifyWithBudget` per item through `runWorkerPool` at `deps.modelConcurrency` (default 1, cap 16; results in item order, aspiration items in the same pool) → aggregate → upsert. `classifyWithBudget`: rules → cache → PR 1.3's `classifyItem` only when the model is needed, nothing usable is cached, `deps.deadline` (epoch ms) has not passed and `deps.modelBudget` has a call left (**`modelBudget` defaults to 0**; a `ModelBudget` object is shared across a run; `take()` is synchronous, so the pool cannot overspend); a call whose reply is `usable: false` (not JSON, cut off, every value rejected — never cached by 1.3) is `model_unusable` and counts as skipped. **Partial profiles are written (D20):** `pending_items` = model-skipped items + rows that failed to normalize goes on the row (`profileRow(profile, itemCount, pendingItems)`), `writeIncomplete` is gone, `incomplete` = `pending_items > 0`; the row exists from the first pass, stays due, and converges as the cache fills. Cron `src/app/api/cron/fit-profiles/route.ts` (vercel.json `0 9 * * *`, after embed-opportunities at 08:45; no other schedule touched) → `src/lib/fit/profile/sync.ts`: due = no row, `pending_items > 0` (`pending`), other taxonomy version, newer `investigator_sources.last_refreshed_at`, newer `investigators.updated_at` (the self-declared edit), or older than `FIT_PROFILES_REFRESH_DAYS` (7) — strictly older; equal stamps are not due — labelled with the most urgent reason and ordered by tier (`DUE_REASON_TIERS`: never built / pending / old taxonomy, then refreshed sources / edited record, then merely stale) and id within a tier; `cursor` resumes after that entry's position in the ordering (`dueAfterCursor`; after its id when the entry is no longer due — the same thing for `force` and explicit-id runs, where every reason is equal); `limit` (default 40); the run stops starting investigators after 240 s or once the shared model budget is spent (`maxModelCalls` ← env `FIT_PROFILE_MODEL_CALLS_PER_RUN`, default 300) and passes the same instant as every build's `deadline` — an investigator started before it finishes from the cache with no new call and is written partial — with `modelConcurrency` 4 (`FIT_PROFILES_MODEL_CONCURRENCY`, D20) and ONE `openaiModel()` for the whole run (`classifyWithModel` used to build an OpenAI client per call; the model function is threaded through `ClassifyDeps.model`); `nextCursor` = the last id taken on, or the one before it when a budget left it pending, for manual `POST { cursor }` reruns (the nightly needs none: what it built is no longer due unless pending, and pending rows come first); `sync_job_logs` job_type `fit_profiles` with one line per investigator and `outcome` success / partial / error in the details and the message — `status` is CHECK-constrained to started / success / error, so a partial run is `success` + `details.outcome = "partial"` (a run with nothing written and any pending or skipped item is partial, not success); `POST` validates `cursor` and every `investigatorIds` entry as a UUID (400 otherwise) before an id can reach the `.or(...)` filter string in `loadCollaborators`. **Refresh hook** `src/lib/fit/profile/refresh-hook.ts` — `rebuildFitProfileAfterRefresh(db, id)` (rules + cache, `modelBudget: 0`, never the model in a request path) ends `refreshInvestigatorSources` as outcome `source: "fit"` (`SourceRefreshOutcome.source` widened to `RefreshableSource | "fit"`; `ALL_REFRESHABLE` unchanged; `bulk-refresh-investigator-caches.ts` tallies it as `fitProfilesOk / Skipped / Err`, never as a PubMed error): `ok: true, skipped: true` with a neutral message when `investigator_fit_profiles` or `fit_item_profiles` is not on the database or the descriptor index is empty, `ok: false` on its own line for anything else, never thrown. `scripts/fit-profile-report.ts` (`npm run fit:profile-report`): `--report` from stored rows (reads `pending_items`; prints "0 profiles stored" before the migration, does not crash), `--dry-run` builds the roster in memory with `modelBudget: 0` and writes nothing; `--investigator`, `--limit`, `--json`; pure aggregations in `report.ts` — `[PENDING n]` per line, "pending (partial profiles)" in the roster summary, and the thin approximation from a stored row counts non-prior provenance ids only (`isPriorItemId`). `scripts/fit-rules-report.ts` builds the Profiles item with `splitDirectorySignals` and adds the `directory` item, so `directory_epi_dept` counts once, under `directory`. **Backfill** `scripts/fit-build-profiles.ts` (`npm run fit:build-profiles`) — phase 1 collects evidence for the selected investigators (`--investigator <uuid>` repeatable / comma list, `--limit N`, `--cursor <uuid>`; non-archived roster in id order; one `loadMeshIndex`; four investigators' reads in flight), evaluates rules + `modelNeeded`, dedupes by `itemCacheKey` (a co-authored paper appears under several investigators and is classified once), prefetches `fit_item_profiles` (`content_hash, llm` only, chunks of 100), and classifies the keys with no usable `llm` through `runWorkerPool` at `--concurrency` (default 6, max 16) with one `openaiModel()` and `supabaseItemProfileCache` (a known-miss `get`), a progress line every 50, `--max-model-calls` (default 20,000) as a hard cap, a thrown model error recorded and the item skipped, exit 3 when the first 10 calls all throw (auth, quota); phase 2 builds each profile with `modelBudget: 0` and prints the build line, upserting only with `--write` (`--classify-only` stops after phase 1; `--dry-run` makes no call and no write of any kind and prints the unique-key worklist and the expected call count; `--json`); resumable by content hash and by `investigator_id`; `sync_job_logs` `fit_profiles` with `details.mode = "backfill"` when writing; exit 0 complete / 2 partial / 3 aborted (missing table, empty index, no key, the model refusing every call) / 1 unexpected. **Backfill dry run over the roster (read-only, 69.6 s):** 144 investigators, 16,625 items (+0 aspiration lines), 13,450 model-needed, **11,844 unique keys** after dedupe (1,606 co-authored duplicates), 0 cached → 11,844 expected calls, ≈ 264 min at 8 s a call and 6 in flight (≈ 100 min at 16). Tests (`investigator.test.ts`, 66; fake Supabase client that is thenable, answers a missing table the way PostgREST does, records writes and can fail one investigator's read): the 80/20 split, weighting by reliability × role × recency, per-axis denominators, the thin-evidence cap (single collaborative paper, min items, one grant, priors never lift it, a lone Core self-report), the half-life and floor, undated items, role factors, recent vs career, the confidence scale and thresholds, origin counting (`ctgov_pi` + `ctgov_listed` = one source; a prior never a third), all six keys, topic confidence and tree resolution, provenance top-n and the id tie-break, a `cross_cutting` item, the `modelBudget: 0` path (rules-only, skipped, cache hit, unusable cached row, shared budget through `classifyItem`, past the deadline, an unreadable reply → pending and uncached), characteristics and vocabularies, aspiration matching, the directory split and `directory_epi_dept` on the directory item alone, `profilesDue` (tiers, `pending`, the most urgent label, equal stamps not due) and `dueAfterCursor`, the report labels and the thin approximation, the builder end to end (partial written with `pending_items`, complete with 0, `write: false`, a budgeted call cached, the worker pool at concurrency 4 giving the same profile as serial), `syncInvestigatorFitProfiles` (time stop, model-budget stop with the cursor and a resumed rerun, a pending last investigator, error isolation and an all-error run, a dry run writing nothing, before-the-migration behaviour, explicit ids), the refresh hook (missing tables, empty index, the written partial, a failing read), the MeSH loader (count, parallel ranges, memoization, failures not memoized) and the taxonomy accessors. **Profile dry run (rules + existing cache only, no model calls, nothing written) over the roster, first build:** 144 investigators, 16,625 items (15,375 verified publications, 804 grants, 14 trials all as PI, 0 biosketches on file, 0 self-declared records, one Profiles, self-declared and directory item each), 0 errors, 165 s (the MeSH load is now 2.6 s of that). The item cache is empty, so every model-needed item — 13,450, prose with an axis no rule covered — was skipped and 142 of 144 builds are pending: once the migration is applied these are written as partial profiles with `pending_items`, and the backfill moves them. Dominant career paradigm by family: translational 77 (53.5%), discovery 34 (23.6%), preclinical 25 (17.4%), clinical 5 (3.5%), population 1, none 2; by category translational 77, molecular_cellular_mechanistic 33, animal_model 25, clinical_observational 5, basic_discovery 1, epidemiology 1. Recent view: translational 89 (61.8%), discovery 23, preclinical 23, clinical 4, population 1, none 4 (no item within five years); career and recent dominant family differ for 24. Thin evidence (dominant career category under the cap): 1 (0.7%). Confidence — paradigm low 40 / medium 100 / high 4; unit 93 / 47 / 4; design 97 / 43 / 4; materials 135 / 9 / 0; objective 112 / 32 / 0; topic 43 / 101 / 0: high needs three distinct origins and only the four trial PIs have a third beside PubMed and RePORTER, since no biosketch or self-report is on file (origin counting changes none of these numbers — no investigator had both CT.gov roles as separate sources). Reading: rules alone reproduce the INVENTORY § 11 picture — the CH bridge reads as translational, the animal block is a sixth to a quarter — and the model pass will mostly fill unit, design, materials and objective, where 65–94% of the roster sits at low confidence today. Decisions (D20): per-axis denominators; priors excluded from the thin-evidence support count and from the source count; partial profiles written with `pending_items`; trials verified-only, the same gate as publications; `confidence.high_min_sources` = 3 means a person with only PubMed and RePORTER tops out at medium — the biosketch, a trial or the self-report is the third origin; a partial cron run is logged `success` with `details.outcome = "partial"` because `sync_job_logs.status` is CHECK-constrained (relaxing it is a separate migration). Not built: no `free_text` topic summary, no `esi_eligible_until` (no degree date on file), no re-classification of aspirations on edit beyond the nightly due predicate, no inspector (PR 1.6), and the real backfill pass (the coordinator runs it).

### PR 1.5 · Opportunity fit profile
**Schema.** `opportunity_fit_profiles (opportunity_id UUID PRIMARY KEY, taxonomy_version TEXT, profile JSONB, confidence TEXT, sources JSONB, computed_at TIMESTAMPTZ)`.
**Files.** `src/lib/fit/profile/opportunity.ts` — `deterministicOverlays(notice)` (activity-code priors, clinical-trial designation, division table), `extractWithModel(sections, priors)` per `prompts/notice-extractor.md` (three groups, quote verification, merge), `exemplarPrior(exemplars)` (classify exemplar abstracts with `classifyItem`, aggregate), `blend(text, exemplar, n)` per `taxonomy.opportunity_profile.exemplar_blend`, `buildOpportunityFitProfile(db, id)`; tests for overlays, merge rules (excluded beats required), blend weights, quote verification. Add to the nightly `fit-profiles` cron (open notices, new or changed `guide_html_hash`).
**Acceptance.** Profiles for all open NIH notices with Guide sections; the six fixture notices in the prompt spec produce the expected fields (paste in PR); synopsis-only notices are `confidence ≤ medium`.
**Built (2026-09-05, branch `fit/1.5-opportunity-profile`).** D2 recorded as taken for the extractor: `FIT_MODEL_EXTRACT` defaults to `gpt-4o` (`DEFAULT_EXTRACT_MODEL`), the strongest approved model on the endpoint `outreach/profile.ts` already calls, through PR 1.3's `openaiModel` (JSON mode, `temperature: 0`, `max_tokens` 4,000 — group replies carry evidence arrays). Migration `20260915110000_fit_opportunity_profiles.sql` (written, not applied): `opportunity_fit_profiles` as specified plus `guide_html_hash` (the notice version the row was built from; NULL when built without Guide text) with CHECK on `confidence`, an index on `computed_at`, COMMENT ON, RLS as its neighbours; `fit_notice_extractions (content_hash PK, opportunity_id, section_group, taxonomy_version, output, model, created_at)` so a notice version is extracted once per taxonomy version. `src/lib/fit/profile/opportunity-extract.ts` — the three section groups of the prompt spec routed by section id and heading (group 1: Part 1 Purpose + Section I minus sub-headings matching `non-?respons|not respons|will not be reviewed|out of scope`; group 2: those non-responsive sub-sections + every Section II row + the Section IV items matching `Human Subjects|Clinical Trial`; group 3: Section III with its items + Section VII + Section I sub-headings matching team / collaboration / consortium / partner / leadership / structure / multi-PI; a synopsis is group 1 only), packed into ≤ 30,000-character chunks (an over-long section splits at line boundaries; 11 of 511 stored notices need a second chunk); the system prompt verbatim from the spec with the item classifier's vocabulary block spliced in (`VOCABULARY_PROMPT`, now exported by `classify/llm.ts`), the user template per group with the spec's group-1 schema and the group-2 / group-3 field lists as their own return schemas, a `Section group: n` line, and one added sentence (see the dry run) telling the model that quotes are copied verbatim or the claim is discarded. Validation: only the group's own fields are read (anything else is logged as ignored); every evidence quote must appear in the provided sections after whitespace collapsing and typographic quote / dash folding, and the model's elision marker (`...` / `…`) splits a quote into fragments that must all appear in order in one section; a quote that verifies in a section other than the one cited is kept with the section corrected and logged; a non-empty entry is kept only when a verified quote's `field` is its path or an ancestor (`paradigm.required.clinical_trials` ← `paradigm.required.clinical_trials`, `paradigm.required` or `paradigm`), else it is dropped as "no verified quote"; verbatim lists (`non_responsive`, `eligibility.investigator_rules`, `clinical_trial_text`) are their own quotes and verify item by item; `prior_overrides` are kept only when their quote verifies; ids through the PR 1.1 guards, weights clamped to [0, 1], `confidence` outside the vocabulary → medium; a reply that is not JSON or was cut off is `usable: false`, returned empty and never cached (the 1.3 validator's rule). Cache key `contentHash(TAXONOMY_VERSION, group, chunk/of, priors JSON, section labels + texts)` — the priors are in the key because `prior_overrides` depend on them. `ModelBudget` is one counter for every call of a run (extractor and exemplar classifier). `src/lib/fit/profile/opportunity.ts` — `deterministicOverlays(notice)`: the activity code normalized with the app's `normalizeActivityCode` (`" r18 "` → R18, `K99/R00` → K99; an unlisted code adds nothing and is noted; a listed neutral code is reported "(neutral)"), the designation and activity-code overlays selected through the mapping's `assign_notice` rules with `matchNoticeRules` over a notice-only view of signal-mapping.json (no MeSH clauses, so no descriptor index), `notice_program_division` applied directly with `lookupProgramDivision` + `familyPriorBlock` (validator note c), `r` → `required`, `a` → `allowed`, `objective` → weight 1, `career` → `training_capacity` 1, the BESH row's `required_any` sets (D14) carried into `paradigm.required_any` / `unit.required_any` / `materials.required_any` and into the priors line; inside the overlays the designation beats the activity code beats the division (a K23 under Clinical Trial Not Allowed keeps `clinical_observational .7` and drops `clinical_trials .6`, logged). **Precedence, merge.** (a) overlay entries, unless a verified `prior_override` removes or changes that entry (a `to` of null / 0 / false / [] removes, a number sets the weight, a list replaces a prior list); (b) text `excluded` / `prohibited` from any group; (c) text `required` / `required_any` from group 1 only (group 2's are logged and ignored); (d) text `allowed` / `expected`. A category both excluded and required: when both are text, or the exclusion is an overlay, excluded wins and the requirement is dropped; when the requirement is an overlay and the exclusion is text, the overlay stands and the exclusion is dropped (the model must contradict a prior through `prior_overrides`); either way `needs_review` is set. `allowed` / `expected` entries that collide with an exclusion or repeat a requirement are dropped silently (logged); a category required outright leaves `required_any`. A verified override on a field the priors never set is taken as a quoted text claim on the group's own fields (gpt-4o used one to add `paradigm.excluded`). Provenance keeps the first verified quote per field path across groups. **Exemplars, blend.** `exemplarPrior`: each exemplar becomes a `grant` item through `normalizeGrant` (RCDC, study section, activity code, abstract) and goes through `classifyItem` — rules first, the model only when a usable `fit_item_profiles` row exists or the injected budget allows a call, else rules alone (`buildItemProfile(item, rules, null)`); rows without an abstract are not classified; the axis vectors are averaged and scaled so the top category is 1; `topic.rcdc` is the distinct exemplar RCDC categories by frequency. `blend(text, exemplar, n)` reads `opportunity_profile.exemplar_blend` (the highest `min_exemplars` at or below n: ≥ 15 → 0.6 / 0.4, 5–14 → 0.4 / 0.6, else text only) with **n = classified exemplars** (rows with an abstract; `sources.exemplar_count` keeps the D13 row count): paradigm `required` and `objective` become w_t · text + w_e · exemplar over the union (an exemplar category the text excludes is not blended, logged); a list axis (unit / design `allowed`, materials `expected`) gains an exemplar category only when w_e · share ≥ w_t, i.e. when its exemplar mass alone is worth a text mention — derived from the blend row, no new threshold. **Confidence** = the group-1 reply's (min over chunks; a usable reply without one counts as medium) over full Guide text; capped at medium when only the Simpler synopsis was read (`sources.text = synopsis`); low when no text was read or group 1 produced nothing usable. `mechanism.issuing_ic` = the RFA's IC code, else the single `nih_ic_tokens` entry; `topic.free_text` = the Part 1 Purpose (or the synopsis); `topic.mesh` stays empty (no descriptor mapping in this PR). `buildOpportunityFitProfileFrom(input, deps)` is the pure assembly; `buildOpportunityFitProfile(db, id, deps)` loads through `OpportunityProfileStore` (Supabase implementation; caches default to `fit_notice_extractions` / `fit_item_profiles`) and upserts unless `dryRun`; `runOpportunityProfiles(db, { limit 40, timeBudgetMs 240,000, modelBudget 150, onlyChanged true, dryRun }, deps)` selects open NIH-like notices (the Guide sync's set, `PAS-__-___` included) with sections and a hash, and `profileDue` makes a notice due when it has no row, its `guide_html_hash` changed or the taxonomy version moved (`selectDue`: never-profiled first, newest posted first); the cron route is PR 1.4's — the coordinator wires the call with `rules: (item) => evaluateRules(item, { mesh: await loadMeshIndex(db), tables: DEFAULT_RULE_TABLES })` (`classify/mesh-db.ts`, new). Fixtures `__fixtures__/notice-extractor-fixtures.json` + `profile/opportunity-fixtures.ts`: the six spec notices with real corpus numbers (RFA-DK-26-315 CT-required R01; PA-25-303 BESH; PA-25-172 with DCCPS as the division and six synthetic exemplars; PAR-25-178 for the hybrid D&I case — the corpus has no R18 D&I PAR, the R18 prior is covered by the overlay tests; RFA-DK-27-120 P30; PAR-28-007 as the synopsis-only case — Simpler carries no foundation notices), synthetic section text, a mocked reply per group and expectations; `opportunity.test.ts` (46 tests): every overlay case, grouping / chunking / cache key, quote verification incl. whitespace, typography, elision and section correction, every validation rule, the cache-and-budget path, each merge rule above, exemplar prior with rules-only and with a budgeted mocked model, blend weights read from the JSON with the arithmetic, confidence caps, `profileDue` / `selectDue`, and the six fixtures end to end (all expectations met; every provenance quote re-verifies). `scripts/fit-opportunity-report.ts` (`npm run fit:opportunity-report -- --dry-run [--limit N] [--extract A,B,C] | --fixtures | --report`; ≤ 12 model calls, in-memory caches, reads only).
**Dry run (2026-09-05, 12 model calls in all).** 20 newest-posted open NIH notices with sections, rules only: every profile `low` (no extractor), 9 with a paradigm requirement — the designation overlay on 15 (8 Not Allowed, 7 Required incl. the UM1 / UG3 / U01 priors), P30 / P50 / U54 → `resource_infrastructure` on 6, a division prior on 5 (DEM, KUH ×3, DNB), the activity-code table silent on K76, X01, U24, U19, R61, RM1, Si2/R00; zero exemplars on all 20 (newest-first, as PR 0.6 found). Three notices through `gpt-4o` (9 calls, ~8 s per notice): with the spec's prompt alone **1 of 21 group-1 quotes verified** — every failing quote was shortened with a trailing `...` (`"Applications must include a hybrid trial design..."`), so the profiles fell back to the overlays; group 2 still delivered 5 / 7 / 0 verbatim non-responsive items and `clinical_trial_text` on each. After the elision handling and the one-sentence reminder, RFA-DK-26-315 re-run (3 calls): **8 of 8 quotes verified**, the profile gained `required_any clinical_trials` (folded into the overlay's `required`), `excluded molecular_cellular_mechanistic 1` from "Mechanistic studies, including mechanistic clinical trials are not responsive", `materials.expected digital_wearable`, `objective treatment_evaluation_efficacy 1 / implementation_dissemination 1`, eight distinguishing terms, confidence high; four claims still dropped for having no quote (`paradigm.allowed.implementation_science`, `design.allowed.implementation_evaluation`, `human_required`, `population`). Exemplar blend on the corpus: PAR-25-122 (R03, 60 exemplars, w_e 0.6) → `required translational .60, basic_discovery .31, molecular_cellular_mechanistic .31` from the rules-only exemplar prior (study-section table + RCDC), exemplar `clinical_trials .045` dropped as excluded; PAR-25-178 (9 exemplars, w_e 0.4) → the rules fire on nothing in those abstracts, so `required clinical_trials .6` is the text alone at w_t. Left for the coordinator: apply the migration, wire the runner into the fit-profiles cron, and run it over the 435 open NIH notices with sections (3 extractor calls and ≈ 8 s each → ≈ 1,300 calls, several nightly runs at the defaults or one long script run); the proposed spec / DECISIONS edits are in the PR report.

**Fix pass (2026-09-05, the validator's list; D21 and D22 implemented on the same branch).** *Incomplete builds (B1, S5).* `sources.complete` / `sources.incomplete` are computed in `buildOpportunityFitProfileFrom`: complete iff every group run has `skipped === null` and a usable reply and no exemplar is `budget_skipped` (set when the model was needed, no usable `fit_item_profiles` row existed, and the budget or the deadline stopped the call). `loadExistingProfiles` selects `sources`; `profileDue` returns `incomplete build` for such a row (ranked after "no profile" in `selectDue`; rows without `sources.complete` count as complete); the runner defers a notice when fewer than `MIN_CALLS_PER_NOTICE` (3) calls remain and passes `deadline = started + timeBudgetMs` into the build, past which a chunk is `skipped: "time budget"` and an exemplar budget-skipped (a cache hit is still served). *Exemplar prior and blend (B2, D21).* `aggregateExemplarAxes` is the plain mean (sum / classified exemplars; no rescaling); `ExemplarPrior.informative` (classified rows with a non-empty paradigm vector) is the blend's n and `sources.exemplars_informative`, beside `exemplar_count` (D13 rows) and `exemplars_classified`. `blend`: a category the text or an overlay requires becomes w_t · text + w_e · share, a designation requirement never below its overlay weight (`max(overlay, blended)`, the overlay layer after verified overrides — `MergeResult.overlay_required`, with `MergeResult.origin`); an exemplar-only category enters `paradigm.allowed` at w_e · share and never `required` (a `required_any` member stays where it is); `objective` blends over the union; excluded categories never re-enter; a list axis gains an exemplar category when share ≥ `exemplar_blend[row].list_min_share` (read through `exemplarBlend()`, whose type gained the field; the w_e · share ≥ w_t rule is gone). *Quotes (S1).* An elided quote — text on both sides of `...` / `…` — is rejected outright (`elided quote ("..."); not verbatim`); a marker at either end is stripped; the dropped-claim log says `no verified quote (no evidence entry)` or `(evidence quote failed)`. *Conflicts (S3, D22).* Only a title-level designation requirement survives a text exclusion; an activity-code or division prior yields (excluded wins, `needs_review`) — a K23 under an unknown designation against "clinical trials are not responsive" is the test. *Cache key (S4).* `extractionCacheKey(input) = contentHash(TAXONOMY_VERSION + system prompt + user prompt)`, so a reminder, schema or header-line edit re-extracts; the `section_group` column stays; the migration comment says so. *Shared models (S6).* `resolveModelFns` builds the extractor and the classifier once per run on one OpenAI client (`openaiExtractor({ client })` forwards it); the runner and the backfill pass them down. *`ModelBudget` (S7)* is API-identical to PR 1.4's (`new ModelBudget(remaining)`, `take()`, `remaining`, `exhausted`) so the coordinator can point the import at 1.4's class at landing; `classify/mesh-db.ts` is unchanged (1.4's memoized loader replaces it). *N1* `issuingIc` prefers the single `nih_ic_tokens` entry and falls back to the RFA's two-letter code (the corpus's RFAs carry no tokens, so the fallback is what fires today). *N2* `contacts` is gone from group 3's schema and fields (a stray key is logged as ignored, like any other). *Schema (N4).* `opportunity_fit_profiles.opportunity_id` is a foreign key to `funding_opportunities(id) ON DELETE CASCADE`; `fit_notice_extractions.opportunity_id` deliberately is not (a reusable extraction must outlive a deleted notice; the hash is the key). *Evidence-entry recall (item 10; measured, 6 calls, `gpt-4o`).* The validator found the dominant loss was fields listed without any evidence entry, not bad quotes. One sentence was added to the user template beside the quote reminder — "Every non-empty field needs its own evidence entry with a verbatim quote; a field without one is discarded." — and the two validator notices re-run: RFA-DA-26-055 group 1 went from 18 of 23 entries dropped for no evidence entry to 12 of 32 (6 of 6 quotes verified; 11 of 12 over all three groups, the one miss a re-typed non-responsive paragraph, not an elision); RFA-DK-26-308 from 10 of 14 to 6 of 13 (5 of 5; 10 of 10 over all groups). Dropped-for-no-evidence fell on both and verification stayed ≥ 90 %, so the sentence stays (`QUOTE_REMINDER`, now in the spec's template). *Spec.* `prompts/notice-extractor.md` rewritten so spec and code are identical (the 1.3 precedent): the model paragraph (`max_tokens` 4,000, the prompt-hash cache), the priors with the D14 any-of sets, the three routing regexes verbatim and the chunk rule, the system prompt with the `VOCABULARY_PROMPT` placeholder line (the D19 definitions are not spliced in), the user template with `Section group: {n}` and the reminder, the group-2 / group-3 return blocks pasted from `GROUP_SCHEMAS`, the validation rules as implemented (folding, section correction, elision, claim-on-field-or-ancestor, verbatim lists, guards, precedence (a)–(d) with override semantics, the conflict rule, confidence, completeness), the corpus fixture substitutions and that section text is synthetic; a byte-identity test pins `EXTRACTOR_SYSTEM_PROMPT`, the user-template skeleton and both return blocks to the spec file. *Backfill.* `scripts/fit-build-opportunity-profiles.ts` (`npm run fit:build-opportunity-profiles`): `--dry-run` (no model calls, no writes: candidates, due, chunk and exemplar-abstract counts, the expected call count), `--write` (required for the `opportunity_fit_profiles` upserts; the two caches are written after any model call, and a run with neither flag refuses to start), `--limit N`, `--only NUM,...`, `--cursor <number>` (resume in `opportunity_number` order), `--concurrency N` (default 4, max 8), `--max-model-calls N`, `--no-exemplar-model`, `--force` (rebuild the selection even when up to date — the way to re-run after a prompt edit without a taxonomy bump), `--json`; `profileDue` is the resume predicate, one shared extractor and classifier, a worker pool over notices, a summary line per notice and a final summary; exit 0 complete, 2 partial (deferred / incomplete / errors — rerun to continue), 3 aborted (missing table or credentials), 1 unexpected. Dry run (2026-09-05, read-only; the profile and extraction tables not yet applied, so every notice is "no profile"): **435 candidates, 435 due, 1,313 extractor chunks (8 notices need a second chunk in a group), 2,920 exemplar abstracts of which 2,919 need the item classifier, expected model calls 4,232**, 168 s to plan. At the ≈ 3.5 s per extractor call measured today (10.3 s for a notice's three groups) the chunks alone are ≈ 1.3 hours single-threaded; the nightly runner at its defaults (150 calls) would need weeks, so the backfill seeds the table and the cron keeps it current. *Tests* `opportunity.test.ts` 46 → 62 (elided and trivial-fragment rejection, incomplete builds not "up to date", the runner deferring at < 3 calls and at the deadline, deadline skips inside a build, the cache key against header / reminder / schema, `prior_overrides` end to end in fixture 1 — a verified override lowering `paradigm.required.clinical_trials` to 0.9, an unverified one changing nothing — the K23 conflict, the designation floor, `issuingIc`, the `NIH_NOTICE_FILTER` shape, the byte identity); suite 803. Fixture 3's exemplar expectations are the numbers the code computes under D21 (5 informative of 5 classified of 6 rows, w_e 0.4; health_services 0.6 · 0.7 + 0.4 · 0.42 = 0.588, epidemiology 0.48, exemplar-only clinical_trials into allowed at 0.4 · 0.26 = 0.104).
**Landing (2026-09-05, rebased onto PR 1.4).** Nightly route `src/app/api/cron/fit-opportunity-profiles/route.ts` (`vercel.json` `15 9 * * *`, after `fit-profiles` at 09:00; the eleventh cron), the same shape as 1.4's: `authorizeCronRequest`, `createServiceRoleClient`, `maxDuration` 300, `sync_job_logs` job_type `fit_opportunity_profiles`; GET with the defaults (40 notices, 240 s, model budget from `FIT_OPPORTUNITY_MODEL_CALLS_PER_RUN`, default 150 = `OPPORTUNITY_PROFILES_MODEL_BUDGET`, read by `opportunityModelCallsPerRun`), POST `{ limit?, cursor?, only?, force?, dryRun?, maxModelCalls? }` (cursor a notice UUID, `only` opportunity numbers, 400 otherwise). The rules closure is built in the route over the shared `loadMeshIndex(db)` (1.4's memoized parallel-range reader; 1.5's serial pager is gone). While `opportunity_fit_profiles` is not on the database the route logs a skipped run and answers 200 `{ skipped: "table missing" }`, so the merge can deploy before the migration is applied. `runOpportunityProfiles` gained `only` and `cursor` for the manual POST and reports `next_cursor` (the last notice attempted, or the one before it when that build was incomplete — it is due again and must not be skipped) and `not_candidates`. `ModelBudget` is one class in `src/lib/fit/profile/model-budget.ts`, re-exported by `investigator.ts` and `opportunity-extract.ts` (1.5's copy deleted). Suite 876 (2 skipped).

**PR 1.5b (2026-09-06, branch `fit/1.5b-extractor-boilerplate`) · Extractor: drop NIH boilerplate eligibility rules; paradigm-requirement audit.** PR 2.2's dry run found every investigator–notice pair under the `eligibility_unknown` cap: the extractor stores NIH's template sentence "Any individual(s) with the skills, knowledge, and resources necessary to carry out the proposed research as the PD(s)/PI(s) is invited to work with his/her organization…" as a verbatim `eligibility.investigator_rules` entry (338 of the 436 stored profiles; the only entry on 264) and stage 1 counts every verbatim rule as an unknown. *Filter.* `signal-mapping.json › notice_boilerplate` lists the template sentences as normalized prefixes or patterns (matched after `normalizeForMatch` and lower-casing): the skills / knowledge / resources sentence with its K, T and C06 variants, the multiple-PD/PI policy pointer, the two diversity sentences, and — a judgment call, one entry to delete — the R25 / T32 / K12 program-director sentence "The PD/PI should be an established investigator in the scientific area in which the application is targeted…" (21 notices; "should", not a rule stage 1 can fail on). `noticeBoilerplate()` (signal-mapping.ts, loud on a malformed block) and `noticeBoilerplateId()` (opportunity.ts); `mergeExtractions` drops a matching `eligibility.investigator_rules` or `non_responsive` item and logs it once in the merge log (`eligibility.investigator_rules: dropped NIH boilerplate (skills_knowledge_resources): "…"`, stored in `sources.merge_log`) — after the `fit_notice_extractions` read, so cached replies keep the sentence and `fit:build-opportunity-profiles --write --force` rebuilds every profile with no model calls. Real recurring rules stay: the K-award citizenship sentence (12), "Current and former PDs/PIs on NIH research project (R01)… are not eligible" (11), "Multiple PDs/PIs are not allowed" (10 — a team rule; 6 of the 10 have `team.multi_pi_allowed` null, so the group-3 prompt should route it there), the K99/R00 sentences (7, 5). Applied to the stored profiles (read-only) the filter drops 359 of 767 entries (338 + 21) and nothing from `non_responsive` (1,092 entries on 228 notices); 277 profiles lose every entry, 159 keep at least one verbatim rule (172 if the program-director entry were removed), and 84 carry a structured eligibility flag. Tests: the listed sentences dropped, a real rule kept, whitespace / case / typographic variants, the loader's failures, and a cache-seeded rebuild with a model that throws (post-cache proof). *Audit (read-only, no changes).* The D24(2) cap "notice names no paradigm requirement" fires only when `required`, `required_any` and `allowed` are all empty (`paradigmSupport` → `requirement: "none"`): **79 of 436** stored profiles, not the ~299 with an empty `required` (333; 184 also without `required_any`) — a `required_any` set scores as a requirement and an `allowed` set, quoted or exemplar-blended, scores as the any-of fallback. Twelve from the empty-`required` set (6 Optional, 6 Not Allowed; no Required-designation profile has an empty `required`): (a) genuinely broad 5 (competing revisions, parent K01, ACE K32, REAP R15, CTSA SIPs RC2); (b) stated but missed or under-filed 2 (PAR-24-167 "must … using animal model species" filed as `required_any {animal_model, translational}`; RFA-DE-27-001 CEED hub, `community_based` allowed 0.4 only); (c) dropped for a failed quote 0; (d) captured as `required_any` with `required` correctly empty, not capped 5 (PAR-25-024, PAR-25-310, PAR-27-056, RFA-DC-26-001, PAR-25-096). Twelve from the 79 the cap bites (5 Optional, 7 Not Allowed): (a) 9 (emergency revisions, parent SBIR and STTR, Lasker Si2/R00, parent K02, NIDCD K01, NIDCR K22, COBRE-D P30, Clin-STAR U24); (b) 1 (PAR-25-437 ECHO X01 data / biospecimen access: nothing filed); (c) 2 (PAR-25-069 lost `required_any {preclinical, early_phase_human_experimental, molecular_cellular_mechanistic}` — the quote skipped the "- " bullet marker between a lead-in and its list item; PAR-25-282 lost `required_any {translational, molecular_cellular_mechanistic}` — a paraphrase, "psychosis" for "psychotic disorders"). On 5 of the 24 the model cited the prompt's header line ("Clinical trial: not_allowed" [Notice …]) for `paradigm.excluded` / `design.prohibited` — harmless (the designation overlay carries both) but noise. Proposed for a later prompt PR, not changed here: (1) group-1 schema comments — `required` is what every responsive application must be ("must", "only"), `required_any` at least one of a set, never a single stated requirement — plus a merge rule folding a one-member text `required_any` into `required`; (2) a `QUOTE_REMINDER` sentence — one contiguous span from one section including its bullet markers, no joining of a lead-in with a list item, no re-wording; the header lines above `Sections:` are not citable and the designation is already applied in code; (3) for resource-access and hub / coordinating-center notices, name the paradigm of the studies the resource enables as `required_any` and set `objective.resource_infrastructure`.

### PR 1.6 · Profile inspector
**Files.** `/investigators/[id]/fit` and `/opportunities/[id]/fit` admin-only pages rendering the fit profile with per-category top evidence, and a "flag as wrong" button that writes to `fit_labels` (kind `profile_flag`). Reuse existing `SectionCard`/`Pill` components.
**Checkpoint.** Strategists spot-check 30 investigators (dominant paradigm agrees ≥ 90%) and 20 notices before Phase 2.
**Built (2026-09-05, branch `fit/1.6-profile-inspector`).** Migration `20260916100000_fit_labels.sql` (written, not applied): `fit_labels` exactly as § 2.4 — `id UUID PK DEFAULT gen_random_uuid()`, `investigator_id` / `opportunity_id` nullable foreign keys `ON DELETE CASCADE`, `tier`, `reason`, `axis_reason`, `labeler UUID`, `engine_version`, `source TEXT NOT NULL CHECK (source IN ('gold','override','profile_flag','dismissal'))`, `created_at` — plus a CHECK that at least one subject id is set, indexes on both ids, COMMENT ON, `IF NOT EXISTS`, RLS as its neighbours; the inspector is its first writer, PR 2.4 reuses it. **Routes** (all under the `(app)` group, so the app shell and login redirect apply): `/investigators/[id]/fit`, `/opportunities/[id]/fit` and the spot-check index `/admin/fit`, each admin-only through `requireAdmin` (`profiles.role = 'admin'`, the check the admin server actions already use); a non-admin gets the library queue's treatment — an `EmptyState` "Admins only", no redirect; a malformed id or an unknown / archived entity is `notFound()`; a missing profile row renders "No fit profile yet" with the reason (table not applied vs. not built yet); the header carries `computed_at`, `taxonomy_version`, `item_count` and `pending_items`. **View models** `src/lib/fit/inspect/` (pure, no Supabase, 40 tests in `inspect.test.ts` with fixtures shaped like the first real rows): `labels.ts` — the six inspector axes (five + topic), `categoryDisplay(axis, id)` through the taxonomy accessors (paradigm label + family, `L3 · human individual`), and for design / materials / objective, which carry no labels in `taxonomy.json`, the id spelled out beside the raw id with its design / materials group; an id the taxonomy no longer knows is humanized and marked `known: false`, never thrown (`dominantSafe` replaces `dominantParadigm`, which throws on it); `evidence.ts` — parses every id shape `collectEvidence` writes (`publication:<inv>:<pmid>`, `grant:<uuid>`, `trial:<inv>:<nct>`, `biosketch:…`, `profiles:`, `directory:`, `self_declared:`, `aspiration:`), resolves publications / grants / trials through lookups the page passes in (title, journal · year, project number · FY · code, NCT · start year; PubMed and ClinicalTrials.gov links; an unresolved row keeps its identifier visible), names the current-state items by kind and marks the priors; `investigator-view.ts` — per axis the categories by career weight with recent beside it on the paradigm (a recent-only category shows career 0), the confidence badge per axis, provenance top-3 resolved per category with the report's thin-evidence approximation, the partial banner when `pending_items > 0` ("n of m items are still waiting for the classifier…"; the row stores the count, not which items), evidence summary plus provenance ids by kind, characteristics, aspirations, `do_not_suggest`, collaborators; `opportunity-view.ts` — `required` / `required_any` / `allowed` / `excluded` per axis with one-line semantics (any-of = max over the set, D14), design `prohibited`, materials `expected` / `required` / `required_any` and `human_required`, and for every entry **where it came from** (`origin`, shown as a marker beside the entry so a quote is never the only signal): `text` with the verified quote on its own path ("verified quote") or the list's quote marked `inherited` ("quote is for the whole list" — the quote itself sits under the list heading as `listQuote`, exact key, no ancestor walk); `overlay` when a deterministic overlay set it ("overlay: clinical-trial designation required", "overlay: activity code K08", "overlay: program division …"), the set recomputed from `overlays_applied` / the blend's designation-floor lines / `overrides_applied` through `clinicalTrialOverlay`, `activityCodePrior` and the division table, since the merge's `origin` map is not stored; `exemplar` ("exemplar prior (D21), no Guide quote", `quote: null`) from the blend's `+=` lines on `paradigm.allowed`, `unit.allowed`, `design.allowed`, `materials.expected`, and for `objective` — which `blendMap` adds to without a log line — inferred exactly as no quote anywhere + no overlay + the blend ran; `unquoted` otherwise. The merge-log lines that folded entries out of a list (`mergeNotes`) sit under its heading, and an empty list with a list quote says so ("the extractor cited this; the merge folded the set"). Mechanism / eligibility / team rows with their quotes, `needs_review`, confidence, every quote with its section heading, and `sources` (text, Guide source, sections · chars, exemplar rows · classified · informative, blend weights and n, extractor model, hash; `complete` / `incomplete` → the banner; the per-group extraction log with dropped claims; overlays, overrides, merge and blend logs; a row written before the fix pass with no `complete` counts as complete); `flags.ts` — `parseFlagInput` (exactly one subject UUID, axis in the six, category a taxonomy id on that axis, topic takes none, reason ≤ 1,000 characters and required for a whole-profile flag), `flagRow`, `flagView` (`axis_reason` back to labels: "Paradigm · Clinical trials", "Whole profile"); `index-view.ts` — the two tables from JSON-path selects (`profile->paradigm`, `profile->>number`, `sources->complete`; verified against PostgREST), sorted by name / number; a dominant id the taxonomy no longer knows is marked and never counts as a family move. `load.ts` is the only Supabase code: the three page loads and the index — its three reads paged through `fetchAllRows` past PostgREST's silent 1,000-row cap (`INDEX_MAX_ROWS` 20,000, a note in `errors` when hit) — with `fit_labels` answering `available: false` (not an error) until the migration lands, and the `MISSING_TABLE` regex kept local so the pages do not import the builder. **Flag as wrong**: `FlagButton` (client) on every category row (axis + category fixed, reason optional), on every axis card (a select of that axis's categories or "the whole axis"), and on the whole profile (reason required); it calls `flagFitProfile` in `src/app/actions/fit-inspector-actions.ts` — Zod shape → `parseFlagInput` → `requireAdmin` on the session client → insert into `fit_labels` with `source: 'profile_flag'`, `labeler` = the user, `engine_version` = `TAXONOMY_VERSION`, through the same session client (RLS applies) → `revalidatePath` on the entity's `/fit` page and `/admin/fit`; a missing table answers with the migration path. Existing flags are listed on each page (who from `profiles`, when, scope, reason, engine version). **Index** `/admin/fit`: investigators (name, dominant career paradigm with family and weight, recent with a marker when the family moved, per-axis confidence shorthand `P U D M O T` with the weakest as a pill, items, pending, flags, computed) and opportunities (number, title, designation, top required paradigm — `required_any` marked "(any of)" when that is all there is — with the excluded count, confidence with the text source, complete / incomplete, needs-review, flags, computed), every row linking to its inspector. The two entity pages gained one admin-only link each ("Fit profile (admin) →" beside the back link; `requireAdmin` joins their existing `Promise.all`). Shared `SectionCard` markup lifted into `src/components/fit/inspector-ui.tsx` (the detail pages keep their local copies; the edit to them stays minimal); confidence badges reuse the status pills (high green, medium amber, low grey). **Read-only check against the database** (5 investigator and 15 opportunity rows on 2026-09-05, after the validator's fix pass; the loaders and view models run as the pages do): every provenance id resolved to a title (231 distinct ids over the five), no unknown category ids, no unresolved rows; the 315 notice entries by origin — text 23 (own-path quote 2, the list's quote inherited 21), overlay 72 (designation 62, activity code 10; 55 of them also carry the list's quote, inherited), exemplar 220 (122 of them `paradigm.allowed`, 89 `objective`), unquoted 0; 47 lists carry a list-level quote, 16 of them empty after the merge folded the set, 7 with merge notes; no entry carries an inherited quote unmarked and no exemplar entry carries one at all; `characteristics.career_stage` / `clinical_role` / `degrees` / `title_series` are null on all five (the directory rows carry no rank or degrees); `topic.mesh_major` holds 176–768 tree numbers per person (the page shows 40 and a count); PA-23-317 carries 18 exemplar-only `paradigm.allowed` entries at ≤ 0.3 (D21) and the `paradigm.required_any` quote survives in provenance after the merge folded the set into `required`; `fit_labels` absent → flags unavailable, pages render. Gates: suite 917 (2 skipped), tsc clean, lint clean, production build lists the three routes. **Fix pass (validator, same day):** entry origins and markers as above (the blocker: an entry used to carry the nearest ancestor's quote as if it were its own); `listQuote` / `mergeNotes`; the index reads paged; the partial banner reads "n item(s) pending (m aggregated)" (the pending items are on top of `item_count`, not among it); `SectionCard`'s aside is a `div` that wraps under the title (the flag form is a 520px block); the header and index mark a dominant id that is not in the taxonomy. Not built: a browser click-through (the preview needs an admin login — the coordinator's), the checkpoint itself, and any nav entry for `/admin/fit` (reached from the fit pages' "All profiles →").

---

## Phase 2 — Score

### PR 2.1 · Pure engine
**Files.** `src/lib/fit/engine/`: `eligibility.ts`, `paradigm.ts`, `unit.ts`, `design.ts`, `topic.ts` (coded overlap with depth + IDF; item-embedding top-k; BM25 — all take precomputed inputs), `methods.ts`, `track.ts`, `compose.ts` (S), `tier.ts` (floors, caps, confidence caps, exploratory exceptions), `index.ts` (`scorePair(inv, opp, ctx): FitResult` with `components`, `caps[]`, `provenance`, `tier`, `score`). Tests: `engine.adversarial.test.ts` loads `__fixtures__/adversarial-cases.json` and asserts tiers, caps, component bands and the forbidden-cell generator; unit tests per module.
**Acceptance.** All nine cases and eight forbidden cells pass; `scorePair` is deterministic and has no I/O.

> **Kickoff prompt.** Read `/CLAUDE.md`, spec §7–§10, `src/lib/fit/taxonomy.json`, `src/lib/fit/types.ts`, and `src/lib/fit/__fixtures__/adversarial-cases.json`. Implement `src/lib/fit/engine/*` as pure functions exactly as specified (formulas in §8; floors in §10; gates in §9), reading every constant from taxonomy.json. Write `engine.adversarial.test.ts` first and make it pass. If a fixture expectation seems inconsistent with the spec, stop and show me the conflict rather than changing the fixture.

**Built (2026-09-06, branch `fit/2.1-engine`).** `src/lib/fit/engine/` — pure, deterministic, no clock and no I/O: `scorePair(inv, opp, ctx): FitResult` (and `scorePairDetailed`, which also returns every stage's output for the inspector) in `index.ts`, with `ENGINE_VERSION = "engine-1"` and the `forbiddenCells()` generator; `ScoreContext` added to `types.ts` (`now`, `actionability`, `topic` = `{ idf, items, bm25, override }` with `TopicItemInput` / `IdfTable` / `Bm25Stats`, `infrastructure`, `track`, `investigator_pending_items` (D20), `notice_complete` (D22)); no profile type widened. Every constant is read through `taxonomy.ts` accessors; three spec-defined blocks the JSON lacked were added under `compose` and are marked prior: `methods` (`evidence_min` 0.3, `institutional_infrastructure_credit` 0.5 — §7 stage 6's two numbers), `track` (the readiness ladder K → R21/R03 → R01 → U01/P01 with the sibling codes, readiness values `held_or_below` 1.0 / `one_above` 0.7 / `far_above` 0.45 / `unknown` 0.7, `far_above_distance` 2, term weights 0.6 / 0.2 / 0.2, saturations 2 and 3, `sub_investigator_trial_credit` 0.5 — §7 stage 7 gives the rule in words only) and `actionability.load_penalty_factor` 0.5 ("≥ 3 active R01-equivalents lowers A"); accessors `methodsParams()`, `trackParams()`, `readinessRung(code)`. Formulas as implemented: **E** (`eligibility.ts`) fails on ESI-only / new-investigator-only against `characteristics.esi === false`, MD/DO or a named degree against `degrees` / `clinical_role` (open-ended degree wording is unknown, never a fail), independent appointment against `career_stage = trainee`, a negative runway, and a self-declared do-not-suggest family that is the notice's dominant required family; unknowns (ESI null, no degree on file, rank null, any citizenship rule, every verbatim `investigator_rules` entry) flag and cap at `confidence_caps.eligibility_unknown`. **P** (`paradigm.ts`) = Σ w_o · support(o) / Σ w_o with support(o) = max over investigator categories i of w_i · `categoryCompat(i, o)` (same 1.00 / sibling 0.85 / family matrix), the D14 any-of set as one more term (support = max over members, weight = the set's largest), the allowed set scored any-of when nothing is required and P = 1 when the axis is empty; the recent view unless no recent category exceeds `thin_evidence.cap`; a cross-cutting dominant investigator takes sqrt(U · D) and a cross-cutting category on either side contributes w_i · sqrt(U · D); the excluded rule (dominant ≥ 0.6 excluded, no term ≥ 0.4 → min(P, 0.15)); `P_with_aspiration` recomputed with a matching aspiration at `same_category` weight. **U** (`unit.ts`) the same form over `levelCompat`, required levels equal-weighted, `required_any` any-of, allowed fallback, 1 when empty. **D** (`design.ts`) = 0.6 · req + 0.3 · allowed + 0.1 · (1 − prohibited) with req the min over `required_any` / `required_any_2` of the max member weight (1 with no group), allowed the design mass share inside `allowed` ∪ the required designs, prohibited the share inside `prohibited`; share ≥ 0.6 → × 0.7 and the dominant prohibited design named; a group under 0.20 → `unmet_required`. **T** (`topic.ts`) = 0.5 · coded + 0.3 · embedding + 0.2 · BM25: coded credits each notice MeSH tree number by its deepest common prefix with any investigator `mesh_major` number, depth(ancestor) · idf(ancestor) over depth(code) · idf(code), RCDC by folded name at depth 1 with its IDF (the IDF table must carry every prefix; a missing code takes `idf.unknown`); embedding = mean of the top-`embedding_top_k` per-item cosines over the *compatible* items (stage-2 P ≥ `poor_below` on the item's paradigm vector and `designCompatible` — some required group ≥ 0.20, prohibited share < 0.6, an item with no designs contradicts nothing) rescaled from `[0.35, 0.65]`; BM25 over the notice's `topic.terms` (tokenized by the exported `tokenize`, which PR 2.2 must use for `tf`) against the compatible items' term counts with the caller's `k1` / `b` / `avg_doc_length` / `doc_count` / `doc_freq`, each item normalized by the ideal score, then the same top-k mean; `override` replaces T but the coded matches are still computed (the fixture's `topic_score_override`). **M** (`methods.ts`) = share of the notice's capability pool with evidence ≥ 0.3 — required design groups (any-of), required materials (each), `required_any` materials (one item), expected materials (each), plus `ctx.infrastructure.named` at 1 / 0.5 (institutional) / 0; an empty pool is M = 1. **O** (`objective.ts`) = Σ w_o · inv_o / Σ w_o; 1 when the notice has no objective. **K** (`track.ts`) = 0.6 · readiness + 0.2 · min(1, active_awards / 2) + 0.2 · min(1, prior awardees / 3); readiness from notice rung − highest rung held (nothing held = one below the ladder; an unlisted code is `unknown`), × 0.5 on a Clinical Trial Required notice when the only trial experience is as a sub-investigator; distance ≥ 2 → `readiness_far` cap and the "consider as project lead, not PI" flag. **A** = runway factor (1 at or above `runway_weeks_r01`, or `runway_weeks_r21` for codes on R21's rung; linear below; 0 at the deadline; 1 when no deadline is on file, which still fails the Strong A floor) × 0 in the pipeline × 0 recently dismissed × 0.5 at the load threshold; runway < `runway_weeks_r21` → `runway_short` cap. **S** (`compose.ts`) = 100 · E · P · D^0.75 · U^0.5 · (0.40 T + 0.20 M + 0.15 O + 0.15 K + 0.10 A). **Tier** (`tier.ts`): floors per `taxonomy.tiers`, conjunctive; Moderate's gap rule counts Strong floors missed among D, T (value or depth), M, K, A, confidence and E-no-unknowns — P, U and `D_required_group_min` are governed by their own Moderate values (spec case 7b: a required group at 0.35 is Moderate); Exploratory's P floor is met by P or by `P_with_aspiration` ≥ 0.45; caps `paradigm_gate` (Poor < 0.25, Exploratory < 0.45), `unit_gate`, `design_required_unsupported`, `eligibility_unknown`, `low_profile_confidence` (the weakest of paradigm / unit / design / topic confidence is low, or `pending_items` > 0), `low_notice_confidence` (low, or `sources.complete` false), `readiness_far`, `runway_short`; a paradigm-gated Poor is lifted to Exploratory by `translational_bridge` (`translational` ≥ 0.40 and a collaborator whose dominant family is one the notice requires) or `biospecimen_bridge` (`human_biospecimen` ≥ 0.40 and the notice expects or requires human-biological materials, or allows `human_biospecimen` / `biospecimen_assay`) as `paradigm_gate_relaxed_<id>`, or by an aspiration match as `paradigm_gate` at Exploratory, each waiving the Exploratory P floor; the final tier is the worst of the floor tier and every cap; the stage-8 floor counts as met until Phase 3. `explain.ts` renders the rationale (one clause per component, from provenance), the gap sentences (each missed floor of the next tier and each cap, naming the fix and the matching collaborators) and the Poor "Why not?". `fixtures.ts` hydrates the fixture cases into the stored shapes (defaults filled: six confidence keys, characteristics, `required_any`, eligibility) and builds the forbidden cells. **Tests:** `engine.adversarial.test.ts` (one `it` per fixture assertion; caps read as "must include", an empty list as "none"; the eight forbidden cells through `forbiddenCells()`; byte-identical reruns, frozen inputs, unmutated inputs) and unit tests per module pinned to hand-derived values through the accessors (`paradigm`, `unit`, `design`, `topic`, `methods`, `track`, `compose` + `objective`, `eligibility`, `tier`), with boundary values at every gate and floor and the empty-profile / empty-notice paths; suite 1,058 passed, 19 skipped. **Fixture-vs-spec conflicts, 17 assertions skipped with `// CONFLICT:` comments in the test (coordinator's decision pending):** the fixture bands follow §13's worked numbers, which take P and U as the compat of the investigator's dominant category / level alone (0.45 for case 3, 1.00 for case 5, 0.15 for 6b, 0.85 for 7b), while §7 stage 2's rule multiplies by the investigator weight and maximizes over every category. Under the literal rule case 3 is Poor (support 0.85 · 0.45 = 0.3825 < 0.4 lets the excluded-paradigm rule cap P at 0.15), case 6b's P is 0.36 (human_biospecimen 0.60 · 0.60) so the translational bridge has no Poor gate to lift, cases 5 and 7b sit under their P / U / score bands, and case 7b is Exploratory because its design-level allowed share gives D 0.443 < 0.50 (the spec's 0.55 needs a group-level allowed set, which puts 6b's D at 0.40 against its ≤ 0.25 band). Tiers, caps and bands of cases 1, 2, 4, 5 (tier), 6a, 6b (tier), 7a and every forbidden cell pass as written.

**Fix pass (2026-09-06, same branch; D23 + validator F1–F10).** **D23** — stage-2 and stage-3 support is `max over i of (w_i / w_max) · compat(i, o)`, the investigator's weights normalized to the dominant category on that axis and view (`normalizedWeights` in `util.ts`; `paradigmSupport` / `unitSupport` normalize whatever vector they are given, so stage 5's item compatibility follows); the excluded rule, D14 any-of, cross-cutting √(U·D) and the aspiration relaxation apply unchanged on the normalized support; `design.ts` reads the allowed set at group level (a listed or required design admits its group, minus prohibited designs). The `CONFLICTS` table and every `it.skip` are gone: all nine §13 cases pass with two fixture bands amended and recorded in `expect.notes` ("amended under D23"): case 2 `U ≤ 0.20` (0.19), case 6b `P ≤ 0.45` (0.42), `D ≤ 0.45` (0.40), caps `paradigm_gate` + `design_required_unsupported` — 6b is Exploratory-capped by P itself, not Poor-gated, so the translational bridge has nothing to lift there. **F1** — `exploratory_exceptions.*.notice_families` (translational_bridge: clinical; biospecimen_bridge: discovery, preclinical); `exceptionFires` requires every family the notice requires (cross-cutting excluded) to be listed, read through `exceptionNoticeFamilies()`; a second forbidden-cell sweep (`forbiddenCells({ bridged: true })`) arms both bridges — translational and human_biospecimen at their minimums, a collaborator in the notice's family, and the notice excluding the investigator's dominant category so every cell is Poor-gated — and every cell stays Poor with no relaxed cap, plus a control cell (discovery → clinical) that the bridge does lift. **F2** — `compose.track.far_above: { notice_min_rung: 3, held_below_rung: 2 }` replaces `far_above_distance`: far when the notice is on the U01/P01 row and nothing held is on the R01 row or above (nothing held included; `distance` is null then); `farAboveReadiness()` exported; the cap reason names the row. **F3** — cap id `paradigm_gate_relaxed_aspiration` (`GateCapId`) for the aspiration relaxation. **F4** — a notice with an empty paradigm axis carries `low_notice_confidence` with reason "notice names no paradigm requirement" (one cap, reasons joined, when it coincides with low confidence / incomplete). **F5** — coded overlap's per-code denominator is `max(depth(c), min_specific_depth_for_strong) · idf(c)` (RCDC codes at depth 1 included): a C04-only notice tops out at 1/3, a depth-3+ exact match stays 1. **F6** — `compose.actionability.short_runway_rungs` (`[1]`, the R21/R03 row) replaces the `readinessRung("R21")` decision (`needsShortRunway()`); the degree vocabularies live in `signal-mapping.json › eligibility` (`clinical_degrees`, `known_degrees`, `open_ended_degree_words`) behind `eligibilityVocabulary()` in the new `src/lib/fit/signal-mapping.ts` (validated: lower-case tokens, clinical ⊆ known; `parseEligibilityVocabulary` for tests); the tokenizer minimum (`MIN_TOKEN_LENGTH`) and `GATE_AXES` stay in code with a one-line decision comment. **F7** — tier.test pins the D (0.75/0.50), U (0.60/0.40/0.20), M (0.50/0.30) and K (0.40; 0.30 vs 0.21 around 0.25) floors end to end through `scorePair`, an empty investigator (Poor, no throw), the career-view fallback, and `explain.test.ts` covers `rationale`, `gapSentences`, `whyNot` and `explain` directly. **F8** — `forbiddenCells()` moved from `index.ts` to `fixtures.ts`; the engine index no longer imports the adversarial JSON. **F9** — `runway_short` fires only for codes off the `short_runway_rungs` rows (§9 "R01 scale"). **F10** — taxonomy `_comment`s: `actionability` (load_penalty_factor is a prior; short_runway_rungs), `track._comment_readiness` (readiness.unknown 0.7 alone reaches the Strong K floor), `track._comment_readiness_ladder` (ladder rows vs `characteristics.r01_equivalent_codes`). Observation for the record: under D23 the translational bridge can only apply when the dominant weight is ≥ 0.96 (translational at the 0.40 minimum against a clinical notice gives 0.40 / w_max · 0.60, which clears the 0.25 gate for any lighter dominant), and against a preclinical notice a 0.40 translational side line scores 0.31 by the matrix alone — the bridge is for near-pure basic scientists, as §9 describes.

### PR 2.2 · Retrieval, orchestration, feature flag
**Schema.** `teams` + `fit_engine TEXT NOT NULL DEFAULT 'legacy' CHECK (fit_engine IN ('legacy','fit-v1'))`; `fit_results (investigator_id UUID, opportunity_id UUID, engine_version TEXT, components JSONB, caps TEXT[], score NUMERIC, tier TEXT, provenance JSONB, adjudication JSONB, rationale TEXT, why_not TEXT, computed_at TIMESTAMPTZ, PRIMARY KEY (investigator_id, opportunity_id))`.
**Files.** `src/lib/fit/retrieval.ts` (candidates for an investigator: open notices passing E and P ≥ 0.25 from structured columns, plus embedding top-N as a recall net; near-miss set = P ≥ 0.45 and T < 0.35; candidates for a notice: mirror), `src/lib/fit/service.ts` (`rankForInvestigator`, `rankForNotice`, `refreshCommunityFits`; loads profiles, precomputed IDF and item embeddings, calls `scorePair`, persists `fit_results`), flag-aware callers in `rank-opportunities.ts`, `suggest.ts` (`runSuggestions` maps `FitResult` into the existing `outreach_suggestions` snapshot shape so the UI keeps working), `communities/fits.ts`. IDF table for MeSH/RCDC codes over open notices, refreshed nightly.
**Acceptance.** With `fit_engine='fit-v1'` on a test team, all three surfaces show the same tier for the same pair; with `'legacy'` nothing changes; page load for the investigator page does no per-candidate RPC loop (profiles precomputed).

### PR 2.3 · Unify surfaces, retire quick-match (D8)
**Files.** `funding-opportunity-peek.ts` reads `fit_results` for "Best fit in your directory"; delete `src/lib/quick-match/*` and `loadPiInvestigatorMatches`; update the opportunity page and peek to show tiers instead of a bare number; investigator page copy → "Fit · paradigm, design and topic · refreshed nightly".
**Acceptance.** No references to quick-match remain; `tsc` clean; screenshots of the three surfaces for one pair attached to the PR.

### PR 2.4 · Gold set and metrics
**Schema.** `fit_labels (id UUID PK, investigator_id, opportunity_id, tier TEXT, reason TEXT, axis_reason TEXT, labeler UUID, engine_version TEXT, source TEXT CHECK (source IN ('gold','override','profile_flag','dismissal')), created_at)`.
**Files.** `scripts/fit-goldset-export.ts` (stratified 200-pair CSV per spec §14; includes evidence summaries and the notice title/section I excerpt), `scripts/fit-goldset-import.ts`, `scripts/fit-metrics.ts` (tier precision, wrong-type rate, precision@5 per investigator, family confusion matrix, recall check; runs both engines and writes `docs/fit-engine/METRICS.md`), a minimal labeling page at `/admin/fit-labels` if the team prefers UI to CSV.
**Gold set source.** INVENTORY.md (2026-09-04) shows no dismissal labels exist (`outreach_suggestions` has zero `dismissed` rows), so nothing can be seeded from feedback. The 200-pair gold set is drawn from current-engine outputs plus constructed pairs (including forbidden-cell pairs), all labeled fresh by strategists.
**Evaluation framing for the ImmunoX pilot.** The primary metric is the wrong-type rate for population, health-systems and trial-required notices shown to discovery/preclinical investigators; tier precision and precision@5 are secondary.
**Checkpoint.** Labels collected (D4 — **on the critical path**: no labels means no Phase 2 exit); METRICS.md shows fit-v1 beats legacy on tier precision and wrong-type rate before any team flag is flipped.

---

## Phase 3 — Judge and explain

### PR 3.1 · Stage 8: blind pass, skeptic, reconciler
**Schema.** `fit_adjudications (investigator_id, opportunity_id, profile_versions JSONB, blind JSONB, skeptic JSONB, reconciliation JSONB, model TEXT, created_at, PRIMARY KEY (investigator_id, opportunity_id, profile_versions))`; `fit_corrections (id UUID PK, target TEXT, target_id UUID, path TEXT, from_value JSONB, to_value JSONB, evidence JSONB, kind TEXT, proposed_by TEXT, status TEXT CHECK (status IN ('proposed','applied','rejected')), decided_by UUID, created_at, decided_at)`.
**Files.** `src/lib/fit/judge/mask.ts` (topic masking using the item's and notice's topic terms + MeSH names), `blind.ts`, `skeptic.ts`, `reconcile.ts` (prompts from `docs/fit-engine/prompts/*.md`; validation and post-rules exactly as written there), `corrections.ts` (validate, apply, re-score, persist), `service.ts` runs stage 8 for the top 15 per investigator/notice and the near-miss scout set; tests with mocked model outputs for every reconciliation-table row.
**Acceptance.** Reconciliation table rows each covered by a test; fixture pairs' blind verdicts within one tier of expectations; corrections with invalid ids are dropped and logged.

### PR 3.2 · UI: tiers, rationale, Exploratory, "Why not?", dismissal reasons
**Files.** Investigator page "Opportunities that fit" → three groups (Recommended: Strong/Moderate; Exploratory with gap line first; "Why not?" disclosure listing hidden Poor with one-line reasons); Outreach recipients tab uses the same tiers and shows component bars in the evidence view; opportunity page "Best fit" shows tiers. New `outreach_suggestions.dismissed_reason` values `wrong_research_type` (+ `axis_reason`) and `not_eligible` (migration extends the CHECK); dismissal UI with sub-reasons; one-click profile-correction confirmation (writes `fit_corrections` with `proposed_by: 'investigator' | 'strategist'`).
**Acceptance.** D7 respected (who sees Exploratory); every tier shown has a rationale that cites at least one evidence item; "wrong type of research" dismissal produces a proposed correction visible in the inspector.

### PR 3.3 · Strategist review queue
**Files.** `/team/fit-review`: AI-flagged leads, pending global notice corrections, ungrounded dissents, profile-weight corrections awaiting confirmation; approve/reject writes `fit_corrections.status` and re-scores affected pairs.
**Acceptance.** Approving a notice correction re-scores every investigator against that notice within the nightly job; rejected items never reappear for the same evidence.

---

## Phase 4 — Learn (ongoing)

- `scripts/fit-recalibrate.ts`: fit floors and matrix values to `fit_labels` (grid search over a small parameter set; report before/after METRICS; never auto-apply — writes a proposed `taxonomy.json` diff for review).
- Learning-to-rank over component scores once ≥ 300 labels (pairwise logistic first).
- Embedding-model revisit measured on the same gold set.

---

## Follow-ups (recorded, not scheduled)

- **PubMed · re-refresh investigators with non-ASCII surnames** (2026-09-05). Until fit(0.2b), `decodeXmlEntities` did not decode hex character references, so a surname PubMed encodes as `Rajkovi&#x107;` never matched the roster; PR 0.1b's affiliation check may have rejected such records. Run `scripts/retry-pubmed-investigators.ts` for investigators whose `last_name` folds differently from its ASCII form, then `fit:backfill-pubmed-mesh` for any new rows.
- **PubMed · profiles-sourced rows carry weaker provenance** (2026-09-05, from the first full backfill). All 12 `not_returned` rows and most of the 91 `absent` author positions are `identity_method = 'profiles'`: UCSF Profiles credits PMIDs PubMed no longer serves, and consortium papers where the person appears only in a `<CollectiveName>` (SPIROMICS Investigators, COMET Consortium, PANC-PALS, REDS-IV-P) or not at all on a 56-author list. `absent` is the right value; the scorer should weight these as group-authorship evidence, and a person should read the terminal list after the second run.
- **PR 0.6 · share RePORTER field parsers with PR 0.4** (2026-09-05). `exemplars.ts` mirrors `parseRcdcCategories` / `parseStudySection` / the activity-code regex from `reporter-fields.ts` because 0.4 was not on main when 0.6 was built; import them and delete the copies once both are merged (note `activityCodeOf` prefers RePORTER's own field, `parseActivityCode` the parsed one — pick one).
- **Community-signals sync · non-affiliation rows** (2026-09-05). `src/lib/community/sync-community-signals-from-caches.ts` does not yet feed the non-affiliation verified publication rows (`orcid`, `profiles`, `reporter_link`, `manual`) into community items. Once the other session's edits to that file have landed, extend it to every `identity_status = 'verified'` row, the same gate the ingest uses. Not part of PR 0.2.
- **Backfill · batch-completeness guard** (2026-09-05; implement before the second backfill run, ~30 days out — not now). Baseline in PR 0.2a: each miss is confirmed by a targeted re-request carrying a canary, and the run aborts (exit 2) when that retry returns nothing or leaves more than 25 missing in one batch. Add the batch-level rule on top: if an efetch batch returns fewer than ~90% of the PMIDs requested, treat the whole batch as a transient failure — retry it once, and if it is still short, leave every PMID in the batch un-stamped rather than marking them `not_returned` (the run continues; a rerun picks them up through the pending predicate). Only isolated misses within an otherwise complete batch are evidence that a PMID is actually gone. Without this, one NCBI hiccup during a run can stamp up to 200 real publications, and two hiccups 30 days apart make them terminal. The first run is unaffected — nothing can reach terminal on a first attempt.
- **Backfill · corpus distribution in INVENTORY § 11** (2026-09-05; after the real backfill completes). Extend `--report` (or run it as a one-off query) with the corpus distribution and paste it into `INVENTORY.md` § 11: triangle class across the roster (`triangleClass` over each row's `mesh` UIs), the 30 most common descriptors, the publication-type mix, and the share of rows with any MeSH at all. This is the first empirical look at whether ImmunoX's evidence base is shaped the way `taxonomy.json` assumes, and it is the input to Phase 1 — if the roster is overwhelmingly Animals / Mice check tags with few human study-design publication types, that confirms the pilot's measurable win is gating population and trial notices out rather than discriminating among human-research types, and PR 1.2's rules should be calibrated accordingly. **Implemented in fit(0.2c) on 2026-09-05:** `--report` now pages every publication row and the descriptor table (read-only) and writes the distribution as `INVENTORY.md` § 11a–11h — share of rows with MeSH, triangle class per row and per investigator (modal class, H-touching share, M01-persons split), the 30 most common descriptors and major-topic descriptors, check tags with the Humans / Animals split, every publication type plus the 10-type human study-design line, author position × method — and a `not_returned` watch table beside the terminal one. Pure aggregations and tests in `src/lib/community/pubmed-corpus-stats.ts`; the section writer (`spliceCoverageSection`) regenerates § 11 whole and keeps the hand-written `### Reading for Phase 1` trailer. Result: the roster is not overwhelmingly animal — 74.6% of rows with MeSH touch the human vertex — but human study-design publication types are on only 4.6% of verified rows; see the reading in § 11.
- Item-classifier fixtures run model-only. Three misses are stable across every prompt variant (2026-09-05, 8 runs): fixture 2 `materials.enrolled_participants` (the model says `patients_under_care` for an RCT), fixture 4 `design.hybrid_effectiveness_implementation`, fixture 6 `paradigm.human_biospecimen`. In production PR 1.2's rules fire first and override the model, so candidate rules: publication type `Randomized Controlled Trial` / `Clinical Trial` → `materials.enrolled_participants`; MeSH `Implementation Science` → `paradigm.implementation_science`; revisit `human_biospecimen` via `mesh_primary_human_cells`. Decide on the 1.6 spot check, not before.
- After the one-time classification pass: report how many `fit_item_profiles` rows are `usable: false` retries and how many array-shaped replies were normalized (`llm.dropped` contains `given as an array`); the first 533-call slice had 9 unusable replies (1.7 %) and 0 array-shaped.

---
- Notice-extractor prompt (from the PR 1.5b audit, 2026-09-06; measure on the six fixtures before adopting): (1) group-1 schema comments — `required` is what every responsive application must be ("must", "only"); `required_any` is at least one of a set, never a single stated requirement, and a one-member text `required_any` folds into `required` at merge (D14); (2) one `QUOTE_REMINDER` sentence — one contiguous span from one section including its bullet markers, no joining a lead-in with a list item, no re-wording, the header lines above `Sections:` are not citable; (3) resource-access and hub / coordinating-center notices name the paradigm of the studies the resource enables as `required_any` and set `objective.resource_infrastructure`. Also: MeSH entry terms for the notice-MeSH map (cancer → Neoplasms and the other top unmapped terms).

## Working with Claude Code on this plan

**Operating model (2026-09-05).** One builder agent per PR, working architect + builder + scribe as a single loop in its own git worktree under `/tmp` branched from `origin/main` (the main checkout is never switched). An independent validator agent reviews each branch cold — plan section, acceptance criteria, tests, tsc, dry run, diff — before the PR opens. Disjoint Phase 0 items are built in parallel worktrees and merged in sequence; Phase 1 onward is sequential. Standing authorizations: PRs merge after CI and the validator are green (worktree removed first, `gh pr list --base <branch>` empty, squash, `--delete-branch`, remote ref verified gone); migrations are applied by the coordinator; backfills and loads run without asking once their dry run is clean, with the first run of each split (`--limit` one batch, stored rows inspected, then the rest). Landing sequence per PR: validator → PR → CI green → rebase the branch onto `origin/main` (its migrations directory must hold every migration the remote already has, or the CLI refuses; `package.json` `fit:*` aliases from parallel builders conflict there and are resolved as the union of both `scripts` maps) → `supabase db push` from that worktree, dry-run listing exactly the PR's file, **before** the merge (Vercel deploys `main` on merge and an ingest writing new columns fails silently until the migration lands) → remove the PR's worktrees → squash-merge with `--delete-branch` → verify the remote ref is gone → real backfill, first run split. Every DECISIONS.md default was taken 2026-09-05; D4 stays open. The three human checkpoints above are unchanged, and `.env.local` stays with Vincent.


- Start every session with: *"Read `/CLAUDE.md` and `docs/fit-engine/IMPLEMENTATION_PLAN.md`. We are on PR X.Y. Summarize what it requires and what is already done in the repo before writing code."*
- Ask for a branch per PR (`fit/0.2-pubmed-mesh`) and a commit message prefixed `fit(X.Y):`.
- Ask it to run `npm test` and `npx tsc --noEmit` and paste results before proposing the commit.
- For any PR that touches ingest, ask for the dry-run output on real rows and read it yourself.
- When it proposes changing a fixture or a threshold, that is a spec change: make it in `taxonomy.json` or the fixtures file with a one-line note in `DECISIONS.md`, never inline in engine code.
