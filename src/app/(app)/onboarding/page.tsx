import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function CreateorjoinateamPage() {
  return (
    <PlaceholderPage
      title="Create or join a team"
      step={2}
      description="Discoverable teams, invitations and the waiting room."
    />
  );
}
