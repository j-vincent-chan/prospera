import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function CuratePage() {
  return (
    <PlaceholderPage
      title="Curate opportunity"
      step={7}
      description="Curator form for internal (UCSF) programs and limited-submission overlays: provenance, review-by date, Draft → Publish."
    />
  );
}
