import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function TeamsettingsPage() {
  return (
    <PlaceholderPage
      title="Team settings"
      step={2}
      description="Members, requests, invitations, data sources and outreach identity."
    />
  );
}
