import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function TeamDataSourcesPage() {
  return (
    <PlaceholderPage
      title="Data sources"
      step={6}
      description="Per-source health, coverage, last and next run, and the recent runs log."
    />
  );
}
