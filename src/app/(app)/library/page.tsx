import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function LibraryPage() {
  return (
    <PlaceholderPage
      title="Library"
      step={7}
      description="UCSF proposal library, trust tiers and OSR-sourced rates."
    />
  );
}
